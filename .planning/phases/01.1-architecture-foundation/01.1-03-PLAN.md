---
phase: 01.1-architecture-foundation
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift
  - DeluluDetox/Sources/Features/Onboarding/View/OnboardingView.swift
  - DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift
  - DeluluDetox/Sources/Features/Denial/View/DenialView.swift
  - DeluluDetox/Sources/Features/Denial/Injection/DenialInjection.swift
  - DeluluDetox/Sources/Features/Home/ViewModel/HomeViewModel.swift
  - DeluluDetox/Sources/Features/Home/View/HomeView.swift
  - DeluluDetox/Sources/Features/Home/Injection/HomeInjection.swift
  - DeluluDetox/Sources/Features/Onboarding/OnboardingViewModel.swift  # DELETED (moved)
  - DeluluDetox/Sources/Features/Onboarding/OnboardingView.swift  # DELETED (moved)
  - DeluluDetox/Sources/Features/Denial/DenialViewModel.swift  # DELETED (moved)
  - DeluluDetox/Sources/Features/Denial/DenialView.swift  # DELETED (moved)
  - DeluluDetox/Sources/Features/Home/HomeViewModel.swift  # DELETED (moved)
  - DeluluDetox/Sources/Features/Home/HomeView.swift  # DELETED (moved)
  - DeluluDetox/Sources/App/DependencyContainer.swift  # MODIFIED (transitional shim)
autonomous: true
requirements: []  # Covers D-03, D-15, D-16 (częściowo — onAuthorized pozostaje TRANSITIONAL do Wave 4)
user_setup: []

must_haves:
  truths:
    - "OnboardingViewModel, DenialViewModel używają `@ObservationIgnored @LazyInjected private var requestAuth: RequestScreenTimeAuthUseCase` — zero importów Repository (D-16, VM→UC rule)."
    - "OnboardingViewModel.init() i DenialViewModel.init() są bezargumentowe (nie przyjmują UC przez init)."
    - "`onAuthorized: (() -> Void)?` pozostaje publiczną właściwością na obu VM jako TRANSITIONAL — AppRootView nadal wiresuje callback do `checkAuthorization()`. Callback znika dopiero w Wave 4 (`01.1-04`) gdy AppRootVM przechodzi na Combine publisher (D-11). W tym planie świadomie nie robimy atomic removal — po to żeby każdy commit kompilował się."
    - "HomeViewModel pozostaje bez zmian funkcjonalnych (D-15) — zero UC, zero `@LazyInjected`. Tylko przeniesienie do `Features/Home/ViewModel/`."
    - "Każda feature (Onboarding, Denial, Home) ma swój plik `<Feature>Injection.swift`. Denial i Home są no-op (puste `register(in:)` body — Denial konsumuje UC zarejestrowany przez Onboarding; Home nie ma UC)."
    - "Views używają `@State private var model = <Feature>ViewModel()` — child tworzy własny VM (D-03). AppRootView przechodzi na ten wzorzec w Wave 4."
    - "App nadal buduje się i testy Phase 1 zielone — `DependencyContainer` dostarcza VM przez bezargumentowe inicjalizatory (transitional shim)."
  artifacts:
    - path: "DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift"
      provides: "VM z `@LazyInjected requestAuth`, bezargumentowy init(), transitional onAuthorized callback"
      contains: "@LazyInjected private var requestAuth"
      also_contains: "@ObservationIgnored"
      also_contains: "init()"
    - path: "DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift"
      provides: "Analogiczny refactor jak OnboardingVM"
      contains: "@LazyInjected private var requestAuth"
    - path: "DeluluDetox/Sources/Features/Denial/Injection/DenialInjection.swift"
      provides: "Puste no-op `register(in:)` — Denial konsumuje UC Onboardingu (D-17)"
      contains: "enum DenialInjection"
    - path: "DeluluDetox/Sources/Features/Home/Injection/HomeInjection.swift"
      provides: "Puste no-op `register(in:)` — HomeVM nie ma UC w 01.1 (D-15)"
      contains: "enum HomeInjection"
  key_links:
    - from: "OnboardingViewModel.grantAccessTapped()"
      to: "requestAuth() call"
      via: "@LazyInjected resolve → DIContainer.shared → OnboardingInjection.register (Wave 2) → RequestScreenTimeAuthUseCaseImpl"
      pattern: "try await requestAuth\\(\\)"
    - from: "OnboardingView body"
      to: "@State private var model = OnboardingViewModel()"
      via: "child-owned VM lifecycle (D-03)"
      pattern: "@State private var model"
    - from: "@LazyInjected"
      to: "@ObservationIgnored (musi poprzedzać — CONTEXT D-10)"
      via: "property declaration order"
      pattern: "@ObservationIgnored[\\s\\n]+@LazyInjected"
---

<objective>
Migracja 3 feature'ów (Onboarding, Denial, Home) do feature-first layoutu + przestawienie Onboarding i Denial VM na `@LazyInjected` property wrapper nad `DIContainer.shared`. Home VM bez zmian funkcjonalnych (D-15) — tylko przeniesienie plików i no-op Injection.

Kluczowa decyzja dla atomic commit compile-ability: **zachowujemy transitional `onAuthorized` callback** na Onboarding/Denial VM w tym planie. Dopiero Wave 4 (`01.1-04`) wymienia callback na Combine subscription po stronie AppRootVM i usuwa pole z VM child. Gdybyśmy usunęli callback teraz, AppRootView.checkAuthorization wiring (linie 36-38, 47-54) przestałby działać dopóki AppRootVM też się nie przerobi — a przerobienie AppRootVM wymaga Observe/Refresh UC działającego przez DIContainer.shared, czego nie ma dopóki bootstrap z Wave 4 się nie uruchomi. Te 3 rzeczy są w jednym kole — Wave 4 rozcina je atomicznie.

Wave 3 (ten plan) odpowiada za: **stabilne pośrednie stadium** w którym VM child'ów są przygotowane do DIContainer, ale nadal działają z `DependencyContainer` (transitional shim produkuje je przez `()` init, bo VM same rezolwują UC).

Purpose: Rozbić atomiczny refactor na 2 stadia — Wave 3 (VM child'ów do LazyInjected, pozostawiony callback) + Wave 4 (Root+bootstrap+delete callback). Każde stadium kompiluje się, każde ma zielony test, ryzyko jest rozłożone.

Output: 6 plików refaktoryzowanych + 2 nowe Injection no-op + zaktualizowany DependencyContainer.
</objective>

<execution_context>
@/Users/swojtyna/Documents/code/swojtyna/KKSW---first-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/swojtyna/Documents/code/swojtyna/KKSW---first-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md
@.planning/phases/01.1-architecture-foundation/01.1-PATTERNS.md
@.planning/phases/01.1-architecture-foundation/01.1-02-SUMMARY.md
@.claude/guides/architecture/GUIDE.md
@.claude/guides/feature-structure/GUIDE.md
@.claude/guides/dependency-injection/GUIDE.md

<interfaces>
<!-- Stan po Wave 2 (gotowe do konsumpcji): -->

```swift
// Rozwiązywane przez DIContainer.shared (bootstrap w Wave 4):
protocol RequestScreenTimeAuthUseCase: Sendable {
    func callAsFunction() async throws
}
protocol ObserveScreenTimeAuthStatusUseCase: Sendable {
    func callAsFunction() -> AnyPublisher<AuthorizationStatus, Never>
}
protocol RefreshScreenTimeAuthStatusUseCase: Sendable {
    func callAsFunction()
}

// Transitional (żyje do Wave 4):
struct DependencyContainer {
    let screenTimeAuthRepository: ScreenTimeAuthRepository
    func makeRequestScreenTimeAuthUseCase() -> RequestScreenTimeAuthUseCase
    func makeObserveScreenTimeAuthStatusUseCase() -> ObserveScreenTimeAuthStatusUseCase
    func makeRefreshScreenTimeAuthStatusUseCase() -> RefreshScreenTimeAuthStatusUseCase
    func makeAppRootViewModel() -> AppRootViewModel
    func makeOnboardingViewModel(onAuthorized: (() -> Void)?) -> OnboardingViewModel
    func makeDenialViewModel(onAuthorized: (() -> Void)?) -> DenialViewModel
    func makeHomeViewModel() -> HomeViewModel
}

// DI property wrapper:
@propertyWrapper
public final class LazyInjected<Value>: @unchecked Sendable { /* resolve lazy from DIContainer.shared */ }
```

<!-- UWAGA: DIContainer.shared jest PUSTY do Wave 4. W tym planie VM child'ów DEKLARUJĄ `@LazyInjected`, ale access do `requestAuth` w runtime LECI DO DIContainer.shared i dostaje fatalError (brak rejestracji).
     Żeby Phase 1 testy nadal działały, mamy 2 opcje:
     (a) Test ustawia mock w DIContainer.shared przed tapnięciem grantAccess — wymaga refaktoru testów. Testy są Wave 5.
     (b) W tym planie **nie wywołujemy** VM'ów w testach przez init i grantAccess, tylko zostawiamy stare testy z init injection używając `let vm = OnboardingViewModel(requestAuth: useCase)` — ALE to jest niemożliwe jeśli init staje się bezargumentowy.

Rozwiązanie: init() jest bezargumentowy (D-16), ALE testy używają DIContainer.shared z zarejestrowanymi mockami. Żeby Wave 3 nie wymagał Wave 5, **w tym planie modyfikujemy stare testy** minimalnie, rejestrując mocki w DIContainer.shared.reset()+register() w setUp każdego test case'a. Testy nadal żyją w `DeluluDetoxTests/` (flat layout) — pełny mirror layout dopiero w Wave 5.

Szczegół: `DeluluDetoxTests/OnboardingViewModelTests.swift` — setUp robi `DIContainer.shared.reset()` + rejestruje `MockScreenTimeAuthRepository` + Impl UC. Init VM przez `()`, potem `await vm.grantAccessTapped()`. Callback `onAuthorized` nadal testowany (pozostawiony w VM).

`DeluluDetoxTests/AppRootViewModelTests.swift` — NIE dotykamy w tym planie (AppRootViewModel nie zmienia się, nadal używa `DependencyContainer`). Migruje do nowego layoutu w Wave 5. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migracja Onboarding + Denial VM do feature-first + `@LazyInjected`</name>

  <read_first>
    - `DeluluDetox/Sources/Features/Onboarding/OnboardingViewModel.swift` (33 linii — pełen plik)
    - `DeluluDetox/Sources/Features/Onboarding/OnboardingView.swift` (73 linii — pełen plik; #Preview używa `RequestScreenTimeAuthUseCaseImpl(repository: ScreenTimeAuthRepositoryImpl())`)
    - `DeluluDetox/Sources/Features/Denial/DenialViewModel.swift` (30 linii — pełen plik)
    - `DeluluDetox/Sources/Features/Denial/DenialView.swift` (73 linii — pełen plik; #Preview analog)
    - `.planning/phases/01.1-architecture-foundation/01.1-PATTERNS.md` §2.2 (`@LazyInjected` w VM — obowiązkowo @ObservationIgnored przed, snippet OnboardingVM)
    - `.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md` D-03, D-16 (init() bezargumentowy; onAuthorized transitional — w tym planie ZOSTAWIAMY, w Wave 4 usuwamy)
    - `.claude/guides/architecture/GUIDE.md` VM → tylko UC rule
    - `.claude/guides/dependency-injection/GUIDE.md` @LazyInjected szablon
  </read_first>

  <files>
    DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift (NEW — refaktor z move)
    DeluluDetox/Sources/Features/Onboarding/View/OnboardingView.swift (NEW — refaktor z move)
    DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift (NEW — refaktor z move)
    DeluluDetox/Sources/Features/Denial/View/DenialView.swift (NEW — refaktor z move)
    DeluluDetox/Sources/Features/Denial/Injection/DenialInjection.swift (NEW)
    DeluluDetox/Sources/Features/Onboarding/OnboardingViewModel.swift (DELETE)
    DeluluDetox/Sources/Features/Onboarding/OnboardingView.swift (DELETE)
    DeluluDetox/Sources/Features/Denial/DenialViewModel.swift (DELETE)
    DeluluDetox/Sources/Features/Denial/DenialView.swift (DELETE)
  </files>

  <action>
    1. Utwórz katalogi:
       - `mkdir -p DeluluDetox/Sources/Features/Onboarding/ViewModel`
       - `mkdir -p DeluluDetox/Sources/Features/Onboarding/View`
       - `mkdir -p DeluluDetox/Sources/Features/Denial/ViewModel`
       - `mkdir -p DeluluDetox/Sources/Features/Denial/View`
       - `mkdir -p DeluluDetox/Sources/Features/Denial/Injection`

    2. **Stwórz `DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift`** (D-03, D-16 częściowo — `onAuthorized` TRANSITIONAL). Treść:

    ```swift
    import Observation
    import os

    @MainActor
    @Observable
    final class OnboardingViewModel: @unchecked Sendable {
        private(set) var isRequesting = false
        private(set) var error: String?

        @ObservationIgnored
        @LazyInjected private var requestAuth: RequestScreenTimeAuthUseCase

        // TRANSITIONAL — replaced by Combine publisher subscription in 01.1-04 (Root VM refactor).
        // Keeps AppRootView.swift checkAuthorization wiring functional during Wave 3.
        var onAuthorized: (() -> Void)?

        @ObservationIgnored
        private let logger = Logger(subsystem: "com.kksw.DeluluDetox", category: "Onboarding")

        init() {}

        func grantAccessTapped() async {
            isRequesting = true
            error = nil
            do {
                try await requestAuth()
                logger.info("Screen Time authorization granted")
                onAuthorized?()
            } catch {
                logger.error("Authorization failed: \(error.localizedDescription, privacy: .public)")
                self.error = error.localizedDescription
            }
            isRequesting = false
        }
    }
    ```

    Zmiany vs Phase 1:
    - Dodano `@MainActor` + `@unchecked Sendable` (wymóg @Observable + @LazyInjected wg dependency-injection GUIDE).
    - Usunięto `private let requestAuth: RequestScreenTimeAuthUseCase` + `init(requestAuth:, onAuthorized:)` — zastąpione `@ObservationIgnored @LazyInjected private var requestAuth` + `init()`.
    - Logger oznaczony `@ObservationIgnored` (defensywnie — nie mutujemy, ale żeby nie invalidować observation przy dostępie).
    - `onAuthorized` nadal obecny (TRANSITIONAL komentarz).
    - `@MainActor` usuwa konieczność lokalnego `@MainActor` na `grantAccessTapped` — cała klasa jest MainActor.

    3. **Stwórz `DeluluDetox/Sources/Features/Onboarding/View/OnboardingView.swift`** — KOPIA istniejącego pliku z jedną zmianą w `#Preview`. Treść (body identyczne jak obecny `OnboardingView.swift` linie 6-62):

    ```swift
    import SwiftUI

    struct OnboardingView: View {
        @Bindable var model: OnboardingViewModel

        var body: some View {
            VStack(spacing: 0) {
                Spacer()

                VStack(spacing: 0) {
                    Image(systemName: "lock.iphone")
                        .font(.system(size: 72))
                        .symbolRenderingMode(.hierarchical)
                        .foregroundStyle(Theme.accent)
                        .accessibilityLabel("Phone with lock")

                    Spacer().frame(height: 16)

                    Text("Your Phone Is Winning")
                        .font(.largeTitle)
                        .bold()
                        .foregroundStyle(Theme.primaryText)
                        .multilineTextAlignment(.center)

                    Spacer().frame(height: 8)

                    Text("DeluluDetox needs Screen Time access to block distracting apps. No data leaves your device \u{2014} ever.")
                        .font(.body)
                        .foregroundStyle(Theme.secondaryText)
                        .multilineTextAlignment(.center)
                        .padding(.horizontal, 24)

                    Spacer().frame(height: 32)

                    Button {
                        Task { await model.grantAccessTapped() }
                    } label: {
                        Group {
                            if model.isRequesting {
                                ProgressView()
                                    .tint(.white)
                            } else {
                                Text("Grant Access")
                                    .font(.body)
                                    .bold()
                            }
                        }
                        .foregroundStyle(.white)
                        .frame(maxWidth: .infinity)
                        .frame(height: 50)
                    }
                    .buttonStyle(.borderedProminent)
                    .tint(Theme.accent)
                    .clipShape(RoundedRectangle(cornerRadius: 12))
                    .disabled(model.isRequesting)
                    .padding(.horizontal, 24)
                }

                Spacer()
            }
            .background(Theme.background)
        }
    }

    #Preview {
        // Preview requires DIContainer to have RequestScreenTimeAuthUseCase registered.
        // Finalized in 01.1-04 with proper mock registration; for Wave 3 we register inline.
        let container = DIContainer.shared
        container.reset()
        OnboardingInjection.register(in: container)
        return OnboardingView(model: OnboardingViewModel())
    }
    ```

    Zmiany vs Phase 1:
    - `@Bindable var model` nadal przyjmuje VM z zewnątrz (D-03 stan docelowy: View posiada `@State private var model`, ale to dopiero jak AppRootView przestaje przekazywać VM — w Wave 4). W Wave 3 `AppRootView` nadal przekazuje przez Screen enum payload. Zachowujemy `@Bindable var model`.
    - `#Preview` — stare: `OnboardingViewModel(requestAuth: RequestScreenTimeAuthUseCaseImpl(repository: ScreenTimeAuthRepositoryImpl()))` — już nie skompiluje się bo init() jest bezargumentowy i VM używa `@LazyInjected`. Nowy: zarejestruj OnboardingInjection w DIContainer.shared przed stworzeniem VM.

    4. **Stwórz `DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift`** (analog Onboarding):

    ```swift
    import Observation
    import os

    @MainActor
    @Observable
    final class DenialViewModel: @unchecked Sendable {
        private(set) var isRequesting = false

        @ObservationIgnored
        @LazyInjected private var requestAuth: RequestScreenTimeAuthUseCase

        // TRANSITIONAL — replaced by Combine publisher subscription in 01.1-04.
        var onAuthorized: (() -> Void)?

        @ObservationIgnored
        private let logger = Logger(subsystem: "com.kksw.DeluluDetox", category: "Denial")

        init() {}

        func retryTapped() async {
            isRequesting = true
            do {
                try await requestAuth()
                logger.info("Screen Time authorization granted on retry")
                onAuthorized?()
            } catch {
                logger.error("Retry authorization failed: \(error.localizedDescription, privacy: .public)")
            }
            isRequesting = false
        }
    }
    ```

    5. **Stwórz `DeluluDetox/Sources/Features/Denial/View/DenialView.swift`** — analogiczna zmiana #Preview jak OnboardingView. Body zachowane.

    ```swift
    import SwiftUI

    struct DenialView: View {
        @Bindable var model: DenialViewModel

        var body: some View {
            VStack(spacing: 0) {
                Spacer()

                VStack(spacing: 0) {
                    Image(systemName: "hand.raised.fill")
                        .font(.system(size: 72))
                        .foregroundStyle(Theme.accent)
                        .accessibilityLabel("Raised hand")

                    Spacer().frame(height: 16)

                    Text("Nice Try")
                        .font(.largeTitle)
                        .bold()
                        .foregroundStyle(Theme.primaryText)
                        .multilineTextAlignment(.center)

                    Spacer().frame(height: 8)

                    Text("DeluluDetox literally cannot work without Screen Time access. That\u{2019}s like hiring a bouncer and not letting them in the club.")
                        .font(.body)
                        .foregroundStyle(Theme.secondaryText)
                        .multilineTextAlignment(.center)
                        .padding(.horizontal, 24)

                    Spacer().frame(height: 32)

                    Button {
                        Task { await model.retryTapped() }
                    } label: {
                        Group {
                            if model.isRequesting {
                                ProgressView()
                                    .tint(.white)
                            } else {
                                Text("Let\u{2019}s Try Again")
                                    .font(.body)
                                    .bold()
                            }
                        }
                        .foregroundStyle(.white)
                        .frame(maxWidth: .infinity)
                        .frame(height: 50)
                    }
                    .buttonStyle(.borderedProminent)
                    .tint(Theme.accent)
                    .clipShape(RoundedRectangle(cornerRadius: 12))
                    .disabled(model.isRequesting)
                    .padding(.horizontal, 24)
                }

                Spacer()
            }
            .background(Theme.background)
        }
    }

    #Preview {
        let container = DIContainer.shared
        container.reset()
        OnboardingInjection.register(in: container)  // Denial konsumuje Request UC z Onboarding registration (D-17)
        return DenialView(model: DenialViewModel())
    }
    ```

    6. **Stwórz `DeluluDetox/Sources/Features/Denial/Injection/DenialInjection.swift`** — **puste/no-op** (D-17: Denial konsumuje UC Onboardingu, nie rejestruje nic własnego):

    ```swift
    // Features/Denial/Injection/DenialInjection.swift
    //
    // No-op: Denial feature consumes RequestScreenTimeAuthUseCase registered by OnboardingInjection.
    // This file exists for architectural symmetry and future registration slots.
    // Per D-17: shared components live under feature-owner's directory (Onboarding).

    enum DenialInjection {
        static func register(in container: DIContainer) {
            // No-op by design. Denial has no feature-owned dependencies in Phase 01.1.
        }
    }
    ```

    7. **Usuń stare pliki** (NIE przed utworzeniem nowych, NIE pomyl):
       - `rm DeluluDetox/Sources/Features/Onboarding/OnboardingViewModel.swift`
       - `rm DeluluDetox/Sources/Features/Onboarding/OnboardingView.swift`
       - `rm DeluluDetox/Sources/Features/Denial/DenialViewModel.swift`
       - `rm DeluluDetox/Sources/Features/Denial/DenialView.swift`

    8. `xcodegen generate` (wildcard katalogowy łapie nowe lokalizacje).

    9. Build przez XcodeBuildMCP `build_sim` scheme=DeluluDetox.
       - **Oczekuj błędów kompilacji** w:
         - `DependencyContainer.swift` linia 19-22, 26-29: `makeOnboardingViewModel(onAuthorized:)` i `makeDenialViewModel(onAuthorized:)` — stare wywołanie `OnboardingViewModel(requestAuth:, onAuthorized:)` już nie istnieje (init() jest bezargumentowy). Task 2 załata DependencyContainer.
         - `AppRootViewModel.swift` — `container.makeOnboardingViewModel(onAuthorized: nil)` nadal zwraca `OnboardingViewModel` — ale przez DependencyContainer shim, więc zależy od Task 2.
         - `DeluluDetoxTests/OnboardingViewModelTests.swift` — używa `OnboardingViewModel(requestAuth: useCase, onAuthorized:)` — nie skompiluje się. Task 3 załata testy.
       - Nie martw się tymi błędami na końcu Tasku 1 — Task 2 i 3 zamknie je.
  </action>

  <verify>
    <automated>
      # Nowe lokalizacje
      test -f DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift
      test -f DeluluDetox/Sources/Features/Onboarding/View/OnboardingView.swift
      test -f DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift
      test -f DeluluDetox/Sources/Features/Denial/View/DenialView.swift
      test -f DeluluDetox/Sources/Features/Denial/Injection/DenialInjection.swift
      # Stare zniknęły
      test ! -f DeluluDetox/Sources/Features/Onboarding/OnboardingViewModel.swift
      test ! -f DeluluDetox/Sources/Features/Onboarding/OnboardingView.swift
      test ! -f DeluluDetox/Sources/Features/Denial/DenialViewModel.swift
      test ! -f DeluluDetox/Sources/Features/Denial/DenialView.swift
      # @LazyInjected + @ObservationIgnored w obu VM
      grep -q "@LazyInjected private var requestAuth" DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift
      grep -q "@LazyInjected private var requestAuth" DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift
      grep -q "@ObservationIgnored" DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift
      grep -q "@ObservationIgnored" DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift
      # Init() bezargumentowy
      grep -qE "^\s*init\(\)\s*\{" DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift
      grep -qE "^\s*init\(\)\s*\{" DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift
      # onAuthorized transitional nadal obecny
      grep -q "var onAuthorized:" DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift
      grep -q "var onAuthorized:" DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift
      # DenialInjection struktura
      grep -q "enum DenialInjection" DeluluDetox/Sources/Features/Denial/Injection/DenialInjection.swift
      grep -q "static func register(in container: DIContainer)" DeluluDetox/Sources/Features/Denial/Injection/DenialInjection.swift
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "@LazyInjected" DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift` → **≥ 1** (D-16)
    - `grep -c "@LazyInjected" DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift` → **≥ 1** (D-16)
    - `grep -B1 "@LazyInjected" DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift | grep -c "@ObservationIgnored"` → **≥ 1** (D-10: @ObservationIgnored obowiązkowo przed @LazyInjected)
    - `grep -B1 "@LazyInjected" DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift | grep -c "@ObservationIgnored"` → **≥ 1**
    - `grep -c "import .*Repository\|: ScreenTimeAuthRepository\b" DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift` → **0** (VM→UC rule: VM nie wie o Repository)
    - `grep -c "import .*Repository\|: ScreenTimeAuthRepository\b" DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift` → **0**
    - `grep -c "var onAuthorized:" DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift` → **== 1** (TRANSITIONAL, znika w Wave 4)
    - `grep -c "var onAuthorized:" DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift` → **== 1**
    - `grep -c "requestAuth: RequestScreenTimeAuthUseCase" DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift | head -1` → **== 0** dla starego stylu init (`requestAuth: RequestScreenTimeAuthUseCase` w init paramater); **≥ 1** dla nowego (`@LazyInjected private var requestAuth: RequestScreenTimeAuthUseCase`). Sprawdź ręcznie różnicę — powinien być tylko ten drugi.
    - `grep -cE "^\s*init\(\)\s*\{" DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift` → **≥ 1** (bezargumentowy init D-16)
    - `grep -cE "^\s*init\(\)\s*\{" DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift` → **≥ 1**
    - `grep -c "OnboardingInjection.register(in: container)" DeluluDetox/Sources/Features/Onboarding/View/OnboardingView.swift` → **≥ 1** (#Preview setup)
    - `grep -c "OnboardingInjection.register(in: container)" DeluluDetox/Sources/Features/Denial/View/DenialView.swift` → **≥ 1** (#Preview setup — Denial konsumuje Onboardingową rejestrację)
    - Task 1 końcowy build MOŻE być czerwony — Task 2 + 3 załata. To jest zgodne z planem (każdy task osobno może zawodzić, ale Wave jako całość musi skończyć zielono).
  </acceptance_criteria>

  <done>
    5 nowych plików feature-first, 4 stare usunięte. Oba VM używają `@LazyInjected` + bezargumentowy init + nadal mają `onAuthorized` jako transitional. DenialInjection to no-op zgodnie z D-17. Build NIE musi być zielony na końcu Task 1 — Task 2 + 3 załata DependencyContainer i testy. Acceptance criteria grep'owe MUSZĄ wszystkie przejść.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migracja HomeVM + HomeInjection no-op + naprawa DependencyContainer shim</name>

  <read_first>
    - `DeluluDetox/Sources/Features/Home/HomeViewModel.swift` (10 linii — pełen plik)
    - `DeluluDetox/Sources/Features/Home/HomeView.swift` (63 linii — pełen plik; #Preview `HomeView(model: HomeViewModel())`)
    - `DeluluDetox/Sources/App/DependencyContainer.swift` (po Wave 2 — z transitional makeObserve/makeRefresh factory)
    - `.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md` D-15 (HomeVM pusty w 01.1)
  </read_first>

  <files>
    DeluluDetox/Sources/Features/Home/ViewModel/HomeViewModel.swift (NEW — move bez zmian treści)
    DeluluDetox/Sources/Features/Home/View/HomeView.swift (NEW — move bez zmian treści)
    DeluluDetox/Sources/Features/Home/Injection/HomeInjection.swift (NEW — no-op)
    DeluluDetox/Sources/Features/Home/HomeViewModel.swift (DELETE)
    DeluluDetox/Sources/Features/Home/HomeView.swift (DELETE)
    DeluluDetox/Sources/App/DependencyContainer.swift (MODIFIED — factory OnboardingVM/DenialVM teraz tworzą VM przez ()) 
  </files>

  <action>
    1. Utwórz katalogi:
       - `mkdir -p DeluluDetox/Sources/Features/Home/ViewModel`
       - `mkdir -p DeluluDetox/Sources/Features/Home/View`
       - `mkdir -p DeluluDetox/Sources/Features/Home/Injection`

    2. **Stwórz `DeluluDetox/Sources/Features/Home/ViewModel/HomeViewModel.swift`** — KOPIA bez zmian treści (D-15):

    ```swift
    import Observation

    @Observable
    final class HomeViewModel {
        // Phase 1: empty state only. No data, no fetching.
        // Phase 2 will add: blocked app selections, FamilyActivityPicker trigger
        func chooseAppsTapped() {
            // No-op in Phase 1 -- wired to FamilyActivityPicker in Phase 2
        }
    }
    ```

    3. **Stwórz `DeluluDetox/Sources/Features/Home/View/HomeView.swift`** — KOPIA bez zmian treści (HomeVM nie zmienia się, #Preview nadal używa `HomeViewModel()`).

    ```swift
    import SwiftUI

    struct HomeView: View {
        @Bindable var model: HomeViewModel

        var body: some View {
            VStack(spacing: 0) {
                Spacer()

                VStack(spacing: 0) {
                    Image(systemName: "apps.iphone")
                        .font(.system(size: 56))
                        .symbolRenderingMode(.hierarchical)
                        .foregroundStyle(Theme.tertiaryText)
                        .accessibilityLabel("Phone with apps")

                    Spacer().frame(height: 16)

                    Text("No Apps Blocked Yet")
                        .font(.title2)
                        .bold()
                        .foregroundStyle(Theme.primaryText)
                        .multilineTextAlignment(.center)

                    Spacer().frame(height: 8)

                    Text("Pick the apps that steal your time. We\u{2019}ll do the rest.")
                        .font(.body)
                        .foregroundStyle(Theme.secondaryText)
                        .multilineTextAlignment(.center)
                        .padding(.horizontal, 24)

                    Spacer().frame(height: 32)

                    Button {
                        model.chooseAppsTapped()
                    } label: {
                        Text("Choose Apps to Block")
                            .font(.body)
                            .bold()
                            .foregroundStyle(.white)
                            .frame(maxWidth: .infinity)
                            .frame(height: 50)
                    }
                    .buttonStyle(.borderedProminent)
                    .tint(Theme.accent)
                    .clipShape(RoundedRectangle(cornerRadius: 12))
                    .padding(.horizontal, 24)
                }

                Spacer()
            }
            .background(Theme.background)
            .navigationTitle("DeluluDetox")
            .navigationBarTitleDisplayMode(.large)
        }
    }

    #Preview {
        NavigationStack {
            HomeView(model: HomeViewModel())
        }
    }
    ```

    4. **Stwórz `DeluluDetox/Sources/Features/Home/Injection/HomeInjection.swift`** — **puste/no-op** (D-15):

    ```swift
    // Features/Home/Injection/HomeInjection.swift
    //
    // No-op: HomeViewModel has no UseCase dependencies in Phase 01.1 (D-15).
    // Phase 2 will add the first UseCase (e.g., PickBlockedAppsUseCase) and corresponding registration.
    // Plik istnieje dla architektonicznej symetrii.

    enum HomeInjection {
        static func register(in container: DIContainer) {
            // No-op by design in Phase 01.1.
        }
    }
    ```

    5. **Zmodyfikuj `DeluluDetox/Sources/App/DependencyContainer.swift`** — `makeOnboardingViewModel` i `makeDenialViewModel` teraz produkują VM przez bezargumentowy init. `onAuthorized` dalej propagowany (wire po init). Pełna docelowa treść:

    ```swift
    import Observation

    // TRANSITIONAL — replaced by DIContainer + distributed <Feature>Injection in 01.1-04.
    // After Wave 3 (this plan): VMs use @LazyInjected over DIContainer.shared but app's
    // AppRootViewModel still resolves them via DependencyContainer for Phase 1 Screen enum wiring.
    // DELETED in 01.1-04.

    struct DependencyContainer {
        let screenTimeAuthRepository: ScreenTimeAuthRepository

        init(screenTimeAuthRepository: ScreenTimeAuthRepository = ScreenTimeAuthRepositoryImpl()) {
            self.screenTimeAuthRepository = screenTimeAuthRepository
        }

        func makeRequestScreenTimeAuthUseCase() -> RequestScreenTimeAuthUseCase {
            RequestScreenTimeAuthUseCaseImpl(repository: screenTimeAuthRepository)
        }

        func makeObserveScreenTimeAuthStatusUseCase() -> ObserveScreenTimeAuthStatusUseCase {
            ObserveScreenTimeAuthStatusUseCaseImpl(repository: screenTimeAuthRepository)
        }

        func makeRefreshScreenTimeAuthStatusUseCase() -> RefreshScreenTimeAuthStatusUseCase {
            RefreshScreenTimeAuthStatusUseCaseImpl(repository: screenTimeAuthRepository)
        }

        func makeAppRootViewModel() -> AppRootViewModel {
            AppRootViewModel(container: self)
        }

        // After Wave 3: OnboardingVM/DenialVM use @LazyInjected, init() is arg-less.
        // This factory still exists to keep AppRootViewModel's Screen enum wiring compilable.
        // onAuthorized is assigned POST-init (the VM exposes it as `var`).
        @MainActor
        func makeOnboardingViewModel(onAuthorized: (() -> Void)? = nil) -> OnboardingViewModel {
            let vm = OnboardingViewModel()
            vm.onAuthorized = onAuthorized
            return vm
        }

        @MainActor
        func makeDenialViewModel(onAuthorized: (() -> Void)? = nil) -> DenialViewModel {
            let vm = DenialViewModel()
            vm.onAuthorized = onAuthorized
            return vm
        }

        func makeHomeViewModel() -> HomeViewModel {
            HomeViewModel()
        }
    }
    ```

    **Uwaga concurrency:** `OnboardingViewModel` po Wave 3 Task 1 jest `@MainActor`. Factory methods zwracające je muszą być `@MainActor` albo przyjmować konstrukcję na MainActor. `DependencyContainer` jest `struct` — sam w sobie nie jest isolated, więc adnotujemy factory methods `@MainActor`. To wymusi że `AppRootViewModel.init` (które jest MainActor przez @Observable) wywołuje je synchronicznie na MainActor — OK.

    **Problem wiszący:** Ponieważ `@LazyInjected` rezolwuje z `DIContainer.shared` przy pierwszym dostępie, a DIContainer.shared jest **pusty** w Wave 3 (OnboardingInjection.register nie wołane z App.init()) → w runtime `grantAccessTapped()` spowoduje fatalError.
    - **Rozwiązanie w Wave 3:** Dodaj do `DeluluDetoxApp.init()` wywołanie `OnboardingInjection.register(in: .shared)` **tymczasowo**. To przesuwa bootstrap o jeden plan wcześniej niż D-20 sugeruje — ale jest konieczne żeby app nadal działał po Wave 3. W Wave 4 dołożymy pozostałe (`HomeInjection`, `DenialInjection`, `RootInjection`).

    Zmień `DeluluDetox/Sources/App/DeluluDetoxApp.swift` na:

    ```swift
    import SwiftUI

    @main
    struct DeluluDetoxApp: App {
        init() {
            // TRANSITIONAL bootstrap: Onboarding first (feature-owner of ScreenTimeAuth*, D-17).
            // Home/Denial/Root Injection added in 01.1-04 when DependencyContainer is deleted
            // and AppRootView owns child VM lifecycle via @State.
            OnboardingInjection.register(in: .shared)
        }

        private let container = DependencyContainer()

        var body: some Scene {
            WindowGroup {
                AppRootView(model: container.makeAppRootViewModel())
            }
        }
    }
    ```

    **Dodaj do `DIContainer.swift`** (w Core/DependencyInjection/) static helper jeśli `.shared` nie działa jako sugar w `register(in: .shared)` — sprawdź w buildzie. Jeśli kompilator nie rozumie `.shared`, zmień na `OnboardingInjection.register(in: DIContainer.shared)`.

    Patrz CONTEXT deferred bullet: *"Sugestia dla plannera: (3) refaktor feature-by-feature (Onboarding→Denial→Home→Root, ostatni trigguje Destination switch)"* — jesteśmy po Onboarding+Denial+Home, następny jest Root. Ten plan domyka 3 z 4 feature'ów.

    6. **Usuń stare pliki Home:**
       - `rm DeluluDetox/Sources/Features/Home/HomeViewModel.swift`
       - `rm DeluluDetox/Sources/Features/Home/HomeView.swift`

    7. `xcodegen generate`.

    8. Build przez XcodeBuildMCP `build_sim` scheme=DeluluDetox → **exit 0**.
       - Jeśli nadal fail, najczęściej:
         - `container.makeAppRootViewModel()` nie MainActor — `AppRootViewModel(container:)` musi pozostać dostępne synchronicznie. `AppRootViewModel` jest @Observable (wciąż nie @MainActor w Wave 3). Poprawka: dopiero Wave 4 dotyka AppRootViewModel.
         - Wywołanie `makeOnboardingViewModel` w `AppRootViewModel.init` przechodzi przez @MainActor factory — musi być wywołane z MainActor contextu. `AppRootViewModel.init` w Phase 1 nie jest @MainActor; jeśli Swift 6.2 zgłasza concurrency error, zanotuj i ROZWIĄŻ w Wave 4 (AppRootVM dostanie @MainActor). Jeśli błąd blokuje Wave 3 build → dodaj minimalny `@MainActor` na `init(container:)` tego AppRootVM jako spot-fix w tym planie (nie refaktor pełny).

       Jeśli spot-fix wystarczy → kontynuuj. Jeśli Swift 6.2 generuje lawinę błędów → STOP, zgłoś do usera że Wave 3/4 muszą być scalone atomicznie; patrz CONTEXT `<specifics>` #5 — AppRoot wzorzec jest sercem refaktoru. Oczekiwany wynik: spot-fix `@MainActor init(container:)` na AppRootViewModel wystarcza.
  </action>

  <verify>
    <automated>
      # Home files moved
      test -f DeluluDetox/Sources/Features/Home/ViewModel/HomeViewModel.swift
      test -f DeluluDetox/Sources/Features/Home/View/HomeView.swift
      test -f DeluluDetox/Sources/Features/Home/Injection/HomeInjection.swift
      test ! -f DeluluDetox/Sources/Features/Home/HomeViewModel.swift
      test ! -f DeluluDetox/Sources/Features/Home/HomeView.swift
      # HomeVM bez zmian funkcjonalnych — nadal empty (D-15)
      grep -q "Phase 1: empty state only" DeluluDetox/Sources/Features/Home/ViewModel/HomeViewModel.swift
      # HomeInjection no-op
      grep -q "No-op" DeluluDetox/Sources/Features/Home/Injection/HomeInjection.swift
      # HomeVM nie ma @LazyInjected (D-15)
      ! grep -q "@LazyInjected" DeluluDetox/Sources/Features/Home/ViewModel/HomeViewModel.swift
      # DependencyContainer shim tworzy VM przez ()
      grep -q "let vm = OnboardingViewModel()" DeluluDetox/Sources/App/DependencyContainer.swift
      grep -q "let vm = DenialViewModel()" DeluluDetox/Sources/App/DependencyContainer.swift
      grep -q "vm.onAuthorized = onAuthorized" DeluluDetox/Sources/App/DependencyContainer.swift
      # DeluluDetoxApp.init() bootstraps OnboardingInjection (transitional)
      grep -q "OnboardingInjection.register" DeluluDetox/Sources/App/DeluluDetoxApp.swift
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "@LazyInjected" DeluluDetox/Sources/Features/Home/` (recursive) → **0** (D-15)
    - `grep -c "enum HomeInjection" DeluluDetox/Sources/Features/Home/Injection/HomeInjection.swift` → **== 1**
    - `grep -c "OnboardingInjection.register" DeluluDetox/Sources/App/DeluluDetoxApp.swift` → **== 1** (transitional bootstrap)
    - `grep -c "makeOnboardingViewModel\|makeDenialViewModel" DeluluDetox/Sources/App/DependencyContainer.swift` → **≥ 2**
    - `grep -c "vm.onAuthorized = onAuthorized" DeluluDetox/Sources/App/DependencyContainer.swift` → **≥ 2** (callback wiring post-init)
    - `diff <(grep -v "^\s*$" DeluluDetox/Sources/Features/Home/ViewModel/HomeViewModel.swift) <(grep -v "^\s*$" <(echo 'import Observation'; echo '@Observable'; echo 'final class HomeViewModel {'; echo '    func chooseAppsTapped() {}'; echo '}'))` — nie ma być funkcjonalnych różnic (tylko komentarze Phase 1).
    - XcodeBuildMCP `build_sim` scheme=DeluluDetox → **exit 0**
    - Test run: AppRootViewModelTests są nietknięte przez Task 2 — muszą nadal kompilować się (używają `DependencyContainer` z przejściowym shim). OnboardingViewModelTests **NIE kompilują się** po Task 1 (zmiany inita VM) — Task 3 to fixuje.
  </acceptance_criteria>

  <done>
    Home feature przeniesiona do feature-first bez zmian funkcjonalnych. HomeInjection no-op istnieje. `DependencyContainer` ma factory methods które tworzą OnboardingVM/DenialVM przez `()` + propagują onAuthorized post-init. `DeluluDetoxApp.init()` tymczasowo bootstrapuje OnboardingInjection. Build zielony. Testy mogą być jeszcze czerwone (Task 3).
  </done>
</task>

<task type="auto">
  <name>Task 3: Naprawa OnboardingViewModelTests do DIContainer setup (Phase 1 testy zielone przed Wave 5)</name>

  <read_first>
    - `DeluluDetoxTests/OnboardingViewModelTests.swift` (50 linii — pełen plik, po Phase 1)
    - `DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift` (20 linii — pełen plik)
    - `.planning/phases/01.1-architecture-foundation/01.1-PATTERNS.md` §2.8 (DIContainer.reset() w setUp pattern)
    - `.claude/guides/dependency-injection/GUIDE.md` sekcja Testing (`DIContainer.shared.reset()` + register mocki)
    - `.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md` D-29
  </read_first>

  <files>
    DeluluDetoxTests/OnboardingViewModelTests.swift (MODIFIED — setUp z DIContainer.reset + register mocków)
    DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift (MODIFIED — dodanie statusPublisher + refreshStatus stub)
  </files>

  <action>
    1. **Zaktualizuj `DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift`** — dodaj obsługę nowych członków protokołu (statusPublisher, refreshStatus). Minimalna zmiana:

    ```swift
    import Combine
    import FamilyControls
    @testable import DeluluDetox

    final class MockScreenTimeAuthRepository: ScreenTimeAuthRepository, @unchecked Sendable {
        var stubbedStatus: AuthorizationStatus = .notDetermined {
            didSet { statusSubject.send(stubbedStatus) }
        }
        var requestAuthorizationError: Error?
        var requestAuthorizationCallCount = 0
        var refreshStatusCallCount = 0

        private let statusSubject: CurrentValueSubject<AuthorizationStatus, Never>

        var statusPublisher: AnyPublisher<AuthorizationStatus, Never> {
            statusSubject.eraseToAnyPublisher()
        }

        var authorizationStatus: AuthorizationStatus {
            stubbedStatus
        }

        init() {
            self.statusSubject = CurrentValueSubject(.notDetermined)
        }

        func requestAuthorization() async throws {
            requestAuthorizationCallCount += 1
            if let error = requestAuthorizationError {
                throw error
            }
            stubbedStatus = .approved
        }

        func refreshStatus() {
            refreshStatusCallCount += 1
            statusSubject.send(stubbedStatus)
        }
    }
    ```

    Klucz: `stubbedStatus` didSet propaguje do subject (żeby testy Wave 5 które obserwują publisher widziały zmiany), `refreshStatus()` spełnia protokół (i zeruje/inkrementuje counter). Wave 5 (`01.1-05` testy) rozszerzy to o dedykowane mocki dla UC.

    2. **Zaktualizuj `DeluluDetoxTests/OnboardingViewModelTests.swift`** — dodaj setUp z DIContainer mockingiem. Docelowa treść:

    ```swift
    import XCTest
    @testable import DeluluDetox

    @MainActor
    final class OnboardingViewModelTests: XCTestCase {
        var mockRepo: MockScreenTimeAuthRepository!

        override func setUp() {
            super.setUp()
            DIContainer.shared.reset()
            mockRepo = MockScreenTimeAuthRepository()
            DIContainer.shared.register(ScreenTimeAuthRepository.self, scope: .application) { [mockRepo] _ in
                mockRepo!
            }
            DIContainer.shared.register(RequestScreenTimeAuthUseCase.self, scope: .unique) { c in
                RequestScreenTimeAuthUseCaseImpl(repository: c.resolve())
            }
        }

        func testGrantAccessSuccess() async {
            var authorizedCalled = false
            let vm = OnboardingViewModel()
            vm.onAuthorized = { authorizedCalled = true }

            await vm.grantAccessTapped()

            XCTAssertTrue(authorizedCalled)
            XCTAssertNil(vm.error)
            XCTAssertFalse(vm.isRequesting)
        }

        func testGrantAccessFailure() async {
            mockRepo.requestAuthorizationError = NSError(
                domain: "FamilyControls", code: 2,
                userInfo: [NSLocalizedDescriptionKey: "Invalid account type"]
            )
            var authorizedCalled = false
            let vm = OnboardingViewModel()
            vm.onAuthorized = { authorizedCalled = true }

            await vm.grantAccessTapped()

            XCTAssertFalse(authorizedCalled)
            XCTAssertNotNil(vm.error)
            XCTAssertFalse(vm.isRequesting)
        }

        func testIsRequestingDuringRequest() async {
            let vm = OnboardingViewModel()

            // isRequesting starts false
            XCTAssertFalse(vm.isRequesting)
        }
    }
    ```

    Zmiany vs Phase 1:
    - setUp resetuje DIContainer + rejestruje mock Repo + real UC (który używa mock Repo).
    - `OnboardingViewModel()` wywoływane przez bezargumentowy init; `@LazyInjected` rezolwuje na pierwszy dostęp.
    - `onAuthorized` nadal testowany (pozostawiony jako transitional w Wave 3; Wave 4 Wave 5 zmieni testy żeby obserwowały Combine publisher).

    3. **NIE zmieniaj** `DeluluDetoxTests/AppRootViewModelTests.swift` — nadal używa `DependencyContainer(screenTimeAuthRepository:)` (który żyje do Wave 4). AppRootVM nie zmienił się w tym planie. Testy są zielone po Task 2 fixie `DependencyContainer` shimu. Pełny rewrite Wave 5.

    4. Build + test:
       - XcodeBuildMCP `build_sim` scheme=DeluluDetox → exit 0
       - XcodeBuildMCP `test_sim` scheme=DeluluDetox → **wszystkie testy zielone**:
         - AppRootViewModelTests (4/4) — nadal używa DependencyContainer
         - OnboardingViewModelTests (3/3) — używa DIContainer.reset + register w setUp
         - DeluluDetoxTests placeholder (1/1)
         - **łącznie 8/8 passed**
  </action>

  <verify>
    <automated>
      # Mock ma nowe pola
      grep -q "statusPublisher: AnyPublisher<AuthorizationStatus, Never>" DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift
      grep -q "func refreshStatus" DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift
      grep -q "CurrentValueSubject" DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift
      # OnboardingViewModelTests używa DIContainer.shared.reset() + register
      grep -q "DIContainer.shared.reset()" DeluluDetoxTests/OnboardingViewModelTests.swift
      grep -q "DIContainer.shared.register" DeluluDetoxTests/OnboardingViewModelTests.swift
      # init() bezargumentowy w testach
      grep -q "OnboardingViewModel()" DeluluDetoxTests/OnboardingViewModelTests.swift
      # Stare init z requestAuth: znikło
      ! grep -q "OnboardingViewModel(requestAuth:" DeluluDetoxTests/OnboardingViewModelTests.swift
      # AppRootViewModelTests nadal używa DependencyContainer (do Wave 5)
      grep -q "DependencyContainer(screenTimeAuthRepository:" DeluluDetoxTests/AppRootViewModelTests.swift
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "DIContainer.shared.reset()" DeluluDetoxTests/OnboardingViewModelTests.swift` → **≥ 1** (D-29)
    - `grep -c "DIContainer.shared.register" DeluluDetoxTests/OnboardingViewModelTests.swift` → **≥ 2** (Repo + Request UC)
    - `grep -c "OnboardingViewModel(requestAuth:" DeluluDetoxTests/OnboardingViewModelTests.swift` → **0** (stary init signature znikł)
    - `grep -c "OnboardingViewModel()" DeluluDetoxTests/OnboardingViewModelTests.swift` → **≥ 3** (3 testy, każdy tworzy VM przez ())
    - `grep -c "statusPublisher\|refreshStatus\|CurrentValueSubject" DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift` → **≥ 3** (3 nowe członkowie protokołu)
    - `grep -c "DependencyContainer(screenTimeAuthRepository:" DeluluDetoxTests/AppRootViewModelTests.swift` → **≥ 1** (AppRootVMTests nietknięte w Wave 3)
    - XcodeBuildMCP `test_sim` scheme=DeluluDetox → **8/8 passed** (zero regresji)
  </acceptance_criteria>

  <done>
    OnboardingViewModelTests działa z DIContainer.reset+register w setUp. AppRootViewModelTests zachowuje Phase 1 kontrakt (`DependencyContainer(screenTimeAuthRepository:)`). MockScreenTimeAuthRepository rozszerzony o statusPublisher/refreshStatus. Build + wszystkie 8 testów zielone. App nadal ma Phase 1 funkcjonalność: cold-launch → Onboarding/Denial/Home flow przez `DependencyContainer` + przejściowy OnboardingInjection bootstrap.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Brak zmian w trust boundaries. ScreenTime authorization prompt — identyczny kod (przeniesiony do feature-first, VM używa @LazyInjected zamiast init injection). Nowa powierzchnia: `DIContainer.shared` jako runtime lookup — ale kontener żyje wewnątrz procesu app, no cross-process IPC.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-08 | Denial of Service | `@LazyInjected` rezolwuje z pustego DIContainer w runtime | mitigate | Task 2 wymusza `OnboardingInjection.register(in: .shared)` w `DeluluDetoxApp.init()` jako transitional bootstrap. Bez tego fatalError przy pierwszym `grantAccessTapped`. Acceptance criteria wymaga `grep OnboardingInjection.register` w DeluluDetoxApp.swift. |
| T-01.1-09 | Tampering | Callback `onAuthorized` publiczny `var` — dowolny caller może podmienić | accept | TRANSITIONAL field, znika w Wave 4. Jedyni callerzy: `DependencyContainer.makeOnboardingViewModel` i `AppRootViewModel.init/checkAuthorization` — kod in-module, zaufany. Publiczność konieczna, żeby callback można było wire'ować post-init (VM init() bezargumentowy). |
| T-01.1-10 | Information Disclosure | `@LazyInjected` w VM — dostęp do `requestAuth` na pierwszym access | accept | `private var requestAuth` — nie eksponowany na zewnątrz VM. `wrappedValue` tylko dla internal use VM. Żaden test/view nie czyta go. |
| T-01.1-11 | Spoofing | Mock w teście rejestruje `Self` jako Repository — przypadkowe użycie mocka w prod build | mitigate | Rejestracja w `DIContainer.shared` w teście — ale `DeluluDetoxTests` jest osobnym schematem/targetem. `@testable import DeluluDetox` — mock nie wycieka do app target. setUp robi `DIContainer.shared.reset()` — izolacja między testami. |

**Core value protection (D-13):** Nie dotyka w tym planie (refreshStatus działa w repo, ale konsument — AppRootVM — nie jest podpięty do refreshStatus dopóki Wave 4 nie doda `scenePhase → refreshStatus()`). Po tym planie user cofający Screen Time w Settings nadal zobaczy stary ekran do restartu app — **zgodnie z Phase 1 zachowaniem** (nie regresja; dziura w core value jest zamykana w Wave 4).

Extensions baseline (D-30): ten plan NIE dotyka `Extensions/**`. Regression check przesunięty do Wave 6 (`01.1-07`).
</threat_model>

<verification>
1. Feature-first layout:
   - `Features/Onboarding/{ViewModel,View,Repository,UseCase,Injection}/` istnieje
   - `Features/Denial/{ViewModel,View,Injection}/` istnieje
   - `Features/Home/{ViewModel,View,Injection}/` istnieje
   - Stare `Features/<Feature>/<Feature>ViewModel.swift` i `...View.swift` znikły
2. VM kontrakty:
   - OnboardingVM + DenialVM mają `@ObservationIgnored @LazyInjected private var requestAuth` + `init()` bezargumentowy
   - Oba zachowują `var onAuthorized: (() -> Void)?` (TRANSITIONAL)
   - HomeVM nietknięty funkcjonalnie
3. Injection files:
   - OnboardingInjection.swift (z Wave 2) — 4 rejestracje
   - DenialInjection.swift — no-op
   - HomeInjection.swift — no-op
   - RootInjection.swift — NIE istnieje jeszcze (Wave 4)
4. DeluluDetoxApp bootstrap: `OnboardingInjection.register(in: .shared)` w `init()` (transitional Wave 3)
5. DependencyContainer: factory tworzą VM przez `()` + wire onAuthorized post-init
6. Testy: OnboardingViewModelTests używa DIContainer.reset+register w setUp; AppRootViewModelTests nietknięte (używa DependencyContainer)
7. Build + testy: **8/8 passed**
8. Rules:
   - VM → tylko UC (grep potwierdza brak importów Repository w VM)
   - Repo ↛ Repo (ScreenTimeAuthRepositoryImpl jest jedyne repo; brak cross-ref)
</verification>

<success_criteria>
- 8 plików feature-first (2×ViewModel, 2×View dla Onboarding/Denial; 1×ViewModel, 1×View, 1×Injection dla Home; 1×Injection dla Denial).
- 6 starych plików VM/View usuniętych.
- OnboardingVM + DenialVM używają `@LazyInjected` — VM→Repository rule zachowana.
- `@ObservationIgnored` poprzedza `@LazyInjected` w każdej deklaracji (D-10).
- DenialInjection + HomeInjection istnieją jako no-op.
- DeluluDetoxApp transitional bootstrapuje OnboardingInjection.
- DependencyContainer shim nadal żyje, przekazuje `onAuthorized` post-init.
- MockScreenTimeAuthRepository rozszerzony o Combine członków.
- OnboardingViewModelTests używa DIContainer.reset+register; AppRootViewModelTests nietknięte.
- Build + 8/8 testów zielonych.
- Decyzje D-03, D-10, D-15, D-16 (częściowo — onAuthorized transitional) spełnione.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-architecture-foundation/01.1-03-SUMMARY.md` zawierający:
- Tabelę: plik NEW/DELETED/MODIFIED + krótki opis
- Wynik wszystkich grep-acceptance kryteriów (pass/fail)
- Wynik XcodeBuildMCP build + test (8/8)
- Notatkę: "TRANSITIONAL state — 3 of 4 features migrated to feature-first + @LazyInjected. AppRootVM + DeluluDetoxApp bootstrap finalization happens in 01.1-04. onAuthorized callback pozostaje na OnboardingVM/DenialVM do 01.1-04."
- Lista decyzji spełnionych: D-03, D-10, D-15, D-16 (częściowo — pełne usunięcie onAuthorized w Wave 4)
- Oznaczenie że D-01, D-02, D-04, D-07..D-14 (AppRoot refactor) NADAL OCZEKUJĄ na Wave 4
</output>
</content>
</invoke>