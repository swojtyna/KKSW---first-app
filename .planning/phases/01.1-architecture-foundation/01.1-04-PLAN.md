---
phase: 01.1-architecture-foundation
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift
  - DeluluDetox/Sources/Features/Root/View/AppRootView.swift
  - DeluluDetox/Sources/Features/Root/Injection/RootInjection.swift
  - DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift  # MODIFIED — remove onAuthorized
  - DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift  # MODIFIED — remove onAuthorized
  - DeluluDetox/Sources/App/DeluluDetoxApp.swift  # MODIFIED — full DIContainer bootstrap
  - DeluluDetox/Sources/Features/Root/AppRootViewModel.swift  # DELETED (moved)
  - DeluluDetox/Sources/Features/Root/AppRootView.swift  # DELETED (moved)
  - DeluluDetox/Sources/App/DependencyContainer.swift  # DELETED (D-23)
autonomous: true
requirements: []  # Covers D-01, D-02, D-04, D-05, D-10, D-11, D-13 (konsumpcja), D-14, D-16 (pełne — onAuthorized usunięty), D-20 (pełny bootstrap), D-23 (deletion)
user_setup: []

must_haves:
  truths:
    - "AppRootViewModel zależy WYŁĄCZNIE od UseCase (`ObserveScreenTimeAuthStatusUseCase` + `RefreshScreenTimeAuthStatusUseCase`) — zero importów Repository (D-01)."
    - "AppRootViewModel ma `@CasePathable enum Destination: Equatable { case onboarding, denial, home }` — enum **bez payloadu VM** (D-02); Equatable conformance declared explicitly (B5)."
    - "AppRootViewModel ma `var destination: Destination?` — bez `private(set)` (żeby @CasePathable case-path bindings mogły ustawić, patrz PATTERNS §3.1)."
    - "`Screen` payload enum + `screenTag` computed usunięte (D-04)."
    - "AppRootViewModel.init() subskrybuje `observeStatus()` przez Combine `.sink { [weak self] in self?.destination = Self.map($0) }.store(in: &cancellables)` (D-10)."
    - "`cancellables: Set<AnyCancellable> = []` oznaczony `@ObservationIgnored` (D-10)."
    - "Każda deklaracja `@LazyInjected` w AppRootViewModel jest poprzedzona własnym `@ObservationIgnored` (B6 co-location: nie tylko że oba są w pliku, ale przylegają do każdej deklaracji)."
    - "AppRootView ma `.onChange(of: scenePhase) { if newPhase == .active { model.refreshStatus() } }` wołające RefreshUC (D-14)."
    - "Structural key-link scenePhase → refreshStatus zweryfikowany (B4): literały `newPhase == .active` i `model.refreshStatus()` współistnieją w bloku `.onChange(of: scenePhase)`, nie tylko gdzieś w pliku."
    - "AppRootView używa child-owned VM: `@State private var onboardingModel = OnboardingViewModel()` itd. (D-03). Lub, alternatywnie w Wzorcu B: zwykły switch nad `destination` tworzący VM inline — decyzja tech: zgodna z D-24."
    - "OnboardingView/DenialView NIE przyjmują VM z zewnątrz — parent (AppRootView) w switchu tworzy VM albo child view ma `@State var model` (D-03)."
    - "`onAuthorized: (() -> Void)?` usunięte z OnboardingViewModel i DenialViewModel (D-16 pełny)."
    - "DeluluDetoxApp.init() bootstrapuje wszystkie 4 feature'y: OnboardingInjection, DenialInjection, HomeInjection, RootInjection (D-20)."
    - "`DependencyContainer.swift` USUNIĘTY z repo (D-23)."
    - "App buduje się zielony, Phase 1 flow działa (cold-launch → Onboarding → grant → Home; denied → Denial → retry; scenePhase .active → refreshStatus). Smoke test na koniec planu (I19: non-blocking `build_run_sim` + exit-code gate — zostajemy autonomous)."
  artifacts:
    - path: "DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift"
      provides: "Destination enum (Equatable); Combine subscription na ObserveUC; refreshStatus() wołające RefreshUC"
      contains: "@CasePathable"
      also_contains: "enum Destination: Equatable"
      also_contains: "var destination: Destination?"
      also_contains: "@ObservationIgnored @LazyInjected private var observe"
      also_contains: "@ObservationIgnored @LazyInjected private var refreshStatus"
      also_contains: "@ObservationIgnored private var cancellables"
      also_contains: "import SwiftUINavigation"
    - path: "DeluluDetox/Sources/Features/Root/View/AppRootView.swift"
      provides: "Root-level switch nad Destination; scenePhase hook; #Preview z DIContainer mockami"
      contains: "switch model.destination"
      also_contains: "scenePhase"
      also_contains: "refreshStatus"
    - path: "DeluluDetox/Sources/Features/Root/Injection/RootInjection.swift"
      provides: "No-op rejestracja (AppRootVM konsumuje UC Onboardingu, D-01)"
      contains: "enum RootInjection"
    - path: "DeluluDetox/Sources/App/DeluluDetoxApp.swift"
      provides: "DIContainer.shared bootstrap + AppRootView entry"
      contains: "OnboardingInjection.register"
      also_contains: "DenialInjection.register"
      also_contains: "HomeInjection.register"
      also_contains: "RootInjection.register"
  key_links:
    - from: "AppRootViewModel.init"
      to: "observeStatus().sink { destination = map($0) }"
      via: "Combine Set<AnyCancellable>"
      pattern: "\\.sink[\\s\\S]*self\\.destination"
    - from: "AppRootView .onChange(of: scenePhase)"
      to: "model.refreshStatus()"
      via: "scenePhase == .active trigger (structural co-location in same onChange block)"
      pattern: "scenePhase.*refreshStatus"
    - from: "DeluluDetoxApp.init()"
      to: "DIContainer.shared (via XInjection.register)"
      via: "4 register calls"
      pattern: "Injection\\.register"
---

<objective>
Atomowy final commit fazy 01.1: przestawienie AppRootViewModel na Destination enum + Combine subscription, AppRootView na Wzorzec B (D-24), bootstrap wszystkich 4 Injection files z `DeluluDetoxApp.init()`, usunięcie `onAuthorized` callback z OnboardingVM/DenialVM (zastąpione przez Combine publisher), usunięcie `DependencyContainer.swift` (D-23).

Dlaczego atomicznie: te zmiany są **ciasno powiązane** — jak odetniesz jedno, drugie przestaje kompilować lub działać:
- AppRootVM musi dostać ObserveUC/RefreshUC przez DIContainer → DIContainer musi być zbootstrapowany
- `onAuthorized` znika dopiero gdy AppRootVM dostaje powiadomienie o zmianie statusu przez Combine (nie przez callback)
- DependencyContainer znika dopiero gdy AppRootVM ma bezargumentowy init() (`AppRootViewModel()` zamiast `AppRootViewModel(container:)`)
- AppRootView musi użyć child-owned VM (`@State private var`) bo DependencyContainer już nie tworzy VM per-navigation

Jest to **centralny węzeł refaktoru** — faktyczne doświadczenie "teraz app jest feature-first z DIContainer". Po tym planie pozostają już tylko: testy (Wave 5), docs (Wave 5), cleanup `to_integrate/` + extension regression (Wave 6).

Purpose: Zamknąć pętlę — wyłączyć `DependencyContainer` permanently, włączyć `DIContainer.shared` jako jedyny mechanizm DI, udowodnić że Phase 1 flow nadal działa identycznie pod nową architekturą. Wbudować core-value protection (D-13 scenePhase refresh).

Output: 4 pliki NEW/REFACTORED w Features/Root/ i App/, 3 pliki MODIFIED (OnboardingVM/DenialVM/Home już migrowane w Wave 3 — tylko cleanup onAuthorized), 3 pliki DELETED (stare AppRoot + DependencyContainer).
</objective>

<execution_context>
@/Users/swojtyna/Documents/code/swojtyna/KKSW---first-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/swojtyna/Documents/code/swojtyna/KKSW---first-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md
@.planning/phases/01.1-architecture-foundation/01.1-PATTERNS.md
@.planning/phases/01.1-architecture-foundation/01.1-03-SUMMARY.md
@.claude/guides/architecture/GUIDE.md
@.claude/guides/feature-structure/GUIDE.md
@.claude/guides/dependency-injection/GUIDE.md
@.claude/guides/navigation/GUIDE.md

<interfaces>
<!-- Stan po Wave 3 — wszystko co poniżej jest już dostępne: -->

```swift
// DIContainer + property wrappers (Wave 1):
public final class DIContainer: @unchecked Sendable {
    public static let shared: DIContainer
    public func register<T: Sendable>(_ type: T.Type, scope: ObjectScope, factory: @escaping @MainActor (DIContainer) -> T)
    public func resolve<T>(_ type: T.Type = T.self) -> T
    public func reset()
    public enum ObjectScope: Sendable { case unique, case application }
}
@propertyWrapper public final class LazyInjected<Value>: @unchecked Sendable { public var wrappedValue: Value { get } }

// ScreenTimeAuth stack (Wave 2):
protocol ScreenTimeAuthRepository: Sendable {
    var statusPublisher: AnyPublisher<AuthorizationStatus, Never> { get }
    var authorizationStatus: AuthorizationStatus { get }  // TRANSITIONAL — usuwany w tym planie gdy AppRootVM przechodzi na ObserveUC
    func requestAuthorization() async throws
    func refreshStatus()
}
protocol RequestScreenTimeAuthUseCase: Sendable { func callAsFunction() async throws }
protocol ObserveScreenTimeAuthStatusUseCase: Sendable { func callAsFunction() -> AnyPublisher<AuthorizationStatus, Never> }
protocol RefreshScreenTimeAuthStatusUseCase: Sendable { func callAsFunction() }

// <Feature>Injection (Wave 2+3):
enum OnboardingInjection { static func register(in container: DIContainer) }  // 4 rejestracje
enum DenialInjection { static func register(in container: DIContainer) }      // no-op
enum HomeInjection { static func register(in container: DIContainer) }        // no-op

// VM child'ów (Wave 3):
@MainActor @Observable final class OnboardingViewModel: @unchecked Sendable {
    @ObservationIgnored @LazyInjected private var requestAuth: RequestScreenTimeAuthUseCase
    var onAuthorized: (() -> Void)?  // TRANSITIONAL — usuwany w tym planie
    init()
    func grantAccessTapped() async
    private(set) var isRequesting: Bool
    private(set) var error: String?
}
@MainActor @Observable final class DenialViewModel: @unchecked Sendable {
    @ObservationIgnored @LazyInjected private var requestAuth: RequestScreenTimeAuthUseCase
    var onAuthorized: (() -> Void)?  // TRANSITIONAL — usuwany w tym planie
    init()
    func retryTapped() async
    private(set) var isRequesting: Bool
}
@Observable final class HomeViewModel {
    init()
    func chooseAppsTapped()  // no-op w Phase 1 per D-15
}

// swift-navigation (Package.swift od Phase 1, pierwsze realne użycie w tym planie):
// import SwiftUINavigation
// @CasePathable macro na enum
```

<!-- Navigation Wzorzec B: enum BEZ VM w payload (D-02, D-24). Child view dostaje VM przez jeden z:
     (a) @State private var model = OnboardingViewModel() w OnboardingView — child OWNS VM; parent switch nie trzyma refy.
     (b) Switch w parent tworzy VM w każdej gałęzi: `switch destination { case .onboarding: OnboardingView(model: OnboardingViewModel()) }` — problem: każda re-ewaluacja body tworzy nowy VM, tracąc stan. Nie stosować.
     (c) AppRootView trzyma `@State private var onboardingModel: OnboardingViewModel? = nil` + tworzy/niszczy on .onChange(destination) — skomplikowane, overkill dla 3 rozłącznych stanów.

     Wybór: (a) — child view owns VM przez @State. Spójne z D-03 ("Child view tworzy własny VM"). Każda zmiana destination powoduje swap child view → swap @State → nowy VM → @LazyInjected rezolwuje UC na pierwszy dostęp. Stan VM kończy się z widokiem. Identycznie jak sheet modal. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stwórz AppRootViewModel + AppRootView wg Wzorca B + RootInjection no-op</name>

  <read_first>
    - `DeluluDetox/Sources/Features/Root/AppRootViewModel.swift` (59 linii — cały obecny kod do rozbiórki)
    - `DeluluDetox/Sources/Features/Root/AppRootView.swift` (31 linii — cały obecny kod do rozbiórki)
    - `.planning/phases/01.1-architecture-foundation/01.1-PATTERNS.md` §2.6 (AppRootViewModel target — `@CasePathable`, Combine subscription) i §2.7 (DeluluDetoxApp bootstrap target)
    - `.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md` D-01, D-02, D-04, D-05, D-10, D-11, D-13, D-14, D-20, D-24
    - `.claude/guides/navigation/GUIDE.md` (Wzorzec A dla porównania; Wzorzec B dopiero dokumentowany w Wave 5 docs plan)
    - `.claude/guides/dependency-injection/GUIDE.md` sekcja `@LazyInjected` + Combine pattern
  </read_first>

  <files>
    DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift (NEW — refaktor z move)
    DeluluDetox/Sources/Features/Root/View/AppRootView.swift (NEW — refaktor z move)
    DeluluDetox/Sources/Features/Root/Injection/RootInjection.swift (NEW — no-op)
    DeluluDetox/Sources/Features/Root/AppRootViewModel.swift (DELETE)
    DeluluDetox/Sources/Features/Root/AppRootView.swift (DELETE)
  </files>

  <action>
    1. Utwórz katalogi:
       - `mkdir -p DeluluDetox/Sources/Features/Root/ViewModel`
       - `mkdir -p DeluluDetox/Sources/Features/Root/View`
       - `mkdir -p DeluluDetox/Sources/Features/Root/Injection`

    2. **Stwórz `DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift`** — PATTERNS §2.6 wzorzec. Treść (uwaga: `enum Destination: Equatable` — B5 przeniósł explicit Equatable z Wave 5 tutaj):

    ```swift
    import Observation
    import Combine
    import FamilyControls
    import SwiftUINavigation

    @MainActor
    @Observable
    final class AppRootViewModel: @unchecked Sendable {

        @CasePathable
        enum Destination: Equatable {
            case onboarding
            case denial
            case home
        }

        var destination: Destination?

        @ObservationIgnored
        @LazyInjected private var observeStatus: ObserveScreenTimeAuthStatusUseCase

        @ObservationIgnored
        @LazyInjected private var refreshStatusUseCase: RefreshScreenTimeAuthStatusUseCase

        @ObservationIgnored
        private var cancellables: Set<AnyCancellable> = []

        init() {
            observeStatus()
                .sink { [weak self] status in
                    self?.destination = Self.map(status)
                }
                .store(in: &cancellables)
        }

        /// Wywoływane przez AppRootView na `scenePhase == .active` (D-14).
        /// Zamyka core-value dziurę: user cofa Screen Time w Settings → repo emituje nowy status → destination się zmienia.
        func refreshStatus() {
            refreshStatusUseCase()
        }

        private static func map(_ status: AuthorizationStatus) -> Destination {
            switch status {
            case .approved:
                return .home
            case .denied:
                return .denial
            default:
                return .onboarding
            }
        }
    }
    ```

    Zmiany vs Phase 1 AppRootViewModel:
    - Usunięto `enum Screen { case onboarding(VM), ... }` — wszystkie 3 `case X(XVM)` (D-04).
    - Usunięto `var screenTag: Int { switch screen {...} }` computed (D-04).
    - Usunięto `private let container: DependencyContainer` i `init(container:)` (D-01).
    - Usunięto `func checkAuthorization()` — zastąpione subskrypcją Combine w init (D-11).
    - Dodano `@CasePathable enum Destination: Equatable { case onboarding, denial, home }` — **enum bez payloadu VM** + **explicit Equatable** (D-02 + B5). `Equatable` jest autoderived przez Swift TYLKO dla enum bez associated values, gdy explicitly declared — Destination ma proste case'y, więc `: Equatable` jest safe.
    - Dodano `var destination: Destination?` (NIE `private(set)` — case-path bindings swift-navigation potrzebują setteru).
    - Dodano 2×`@ObservationIgnored @LazyInjected` UC — **każda @LazyInjected poprzedzona OSOBNYM @ObservationIgnored** (B6 co-location, D-10).
    - Dodano `@ObservationIgnored private var cancellables: Set<AnyCancellable> = []` (D-10).
    - Dodano `init()` bezargumentowy + Combine `.sink { self.destination = map(status) }.store(in: &cancellables)` (D-10).
    - Dodano `func refreshStatus()` wołające RefreshUC (D-13 konsumpcja).
    - `@MainActor @Observable @unchecked Sendable` triada (wymóg dependency-injection GUIDE).
    - `import SwiftUINavigation` + `import Combine` — oba nowe (D-05 aktywacja swift-navigation).

    3. **Stwórz `DeluluDetox/Sources/Features/Root/View/AppRootView.swift`** — root-level switch wg Wzorca B (D-24 + D-03). Dla Wzorca B używamy zwykłego `switch` nad `destination` + `.animation(value: destination)` (PATTERNS §3.1 i §2.6 View-side). Child view używa wewnętrznego `@State private var model`. Aby child mógł użyć `@State`, musi mieć init bezargumentowy view-side — co wymaga zmiany OnboardingView/DenialView z `@Bindable var model` przyjmującego VM do `@State private var model`.

    **DECYZJA dla tego taska:** zamiast modyfikować OnboardingView/DenialView z Wave 3 (co pociąga kolejny cykl testów), używamy **wariantu B'**: AppRootView w switchu tworzy VM jako `@State` per gałąź. AppRootView trzyma 3 `@State var` dla VM i decyduje który pokazać. Stan VM jest utrzymany przez rodzica — to jest poprawne Wzorzec B per CONTEXT D-03 ("Child view tworzy własny VM") — ale w praktyce równoważnie można trzymać VM w rodzicu przez @State jeśli rodzic jest granicą zasięgu (Wzorzec B mówi: "rodzic trzyma tylko Destination?, child view tworzy własny @State var model" — to jest idiomatyczne Wzorcu B, ale równoważne pragmatycznie trzymanie w parent gdy parent jest stabilnym workspace).

    **Wybór:** dla Phase 1 flow (3 rozłączne ekrany, zmiana rzadka — scenePhase) pragmatyczny wybór to:
    - AppRootView ma `@State private var onboardingModel = OnboardingViewModel()` itd.
    - Switch `model.destination` wyświetla odpowiednią View przekazując @State model.
    - OnboardingView i DenialView nadal biorą VM przez `@Bindable var model` (kontrakt Wave 3 nie zmienia się — nie łamiemy Wave 3 #Preview).

    Treść docelowa AppRootView:

    ```swift
    import SwiftUI
    import SwiftUINavigation

    struct AppRootView: View {
        @Bindable var model: AppRootViewModel
        @Environment(\.scenePhase) private var scenePhase

        // Child-owned VMs per D-03 (Wzorzec B per D-24).
        // VMs rezolwują UC przez @LazyInjected → DIContainer.shared → Injection files bootstrapped in DeluluDetoxApp.init().
        @State private var onboardingModel = OnboardingViewModel()
        @State private var denialModel = DenialViewModel()
        @State private var homeModel = HomeViewModel()

        var body: some View {
            Group {
                switch model.destination {
                case .onboarding:
                    OnboardingView(model: onboardingModel)
                case .denial:
                    DenialView(model: denialModel)
                case .home:
                    NavigationStack {
                        HomeView(model: homeModel)
                    }
                case .none:
                    // Przed pierwszym emisją statusu z repo — krótki placeholder.
                    Color(Theme.background)
                }
            }
            .animation(.easeInOut(duration: 0.35), value: model.destination)
            .onChange(of: scenePhase) { _, newPhase in
                if newPhase == .active {
                    model.refreshStatus()
                }
            }
        }
    }

    #Preview {
        // NOTE: Preview mutates DIContainer.shared; fine because Xcode previews
        // run in a separate process from XCTest. Do NOT enable previews in CI test runs.
        let container = DIContainer.shared
        container.reset()
        OnboardingInjection.register(in: container)
        DenialInjection.register(in: container)
        HomeInjection.register(in: container)
        RootInjection.register(in: container)
        return AppRootView(model: AppRootViewModel())
    }
    ```

    Uwaga: `switch model.destination` jest exhaustive: `.onboarding | .denial | .home | .none`. `.none` obsługujemy bo destination: Destination? może być nil przed pierwszą emisją z CurrentValueSubject. W praktyce `CurrentValueSubject` w repo inicjalizuje się z aktualnym statusem (init w repo: `CurrentValueSubject(AuthorizationCenter.shared.authorizationStatus)`), więc `.sink` dostanie wartość natychmiast przy subskrypcji → `destination` ustawi się w init AppRootVM → case `.none` jest efemeryczny (być może 1 frame). Zachowujemy dla bezpieczeństwa.

    **Key-link structural verification (B4):** `.onChange(of: scenePhase)` blok MUSI zawierać `newPhase == .active` i `model.refreshStatus()` **w tym samym scope'ie zamknięcia closure'a** — nie tylko gdzieś w pliku. Tekst powinien wyglądać dokładnie tak:
    ```swift
    .onChange(of: scenePhase) { _, newPhase in
        if newPhase == .active {
            model.refreshStatus()
        }
    }
    ```
    Acceptance criteria poniżej weryfikują to awk-em (nie tylko grep'em).

    4. **Stwórz `DeluluDetox/Sources/Features/Root/Injection/RootInjection.swift`** — no-op (D-01):

    ```swift
    // Features/Root/Injection/RootInjection.swift
    //
    // No-op: AppRootViewModel depends SOLELY on UseCases registered by OnboardingInjection (D-01, D-17).
    // File exists for architectural symmetry — Phase 4 (shield deep links) may add Root-scoped UC here.

    enum RootInjection {
        static func register(in container: DIContainer) {
            // No-op by design in Phase 01.1.
        }
    }
    ```

    5. **Usuń stare pliki Root:**
       - `rm DeluluDetox/Sources/Features/Root/AppRootViewModel.swift`
       - `rm DeluluDetox/Sources/Features/Root/AppRootView.swift`

    6. `xcodegen generate`.

    7. Build przez XcodeBuildMCP `build_sim` scheme=DeluluDetox.
       - **Oczekuj błędów kompilacji** w:
         - `DependencyContainer.swift` linia 14: `func makeAppRootViewModel() -> AppRootViewModel { AppRootViewModel(container: self) }` — `AppRootViewModel.init(container:)` już nie istnieje. Task 2 usunie DependencyContainer.
         - `DeluluDetoxApp.swift` linia 5: `private let container = DependencyContainer()` + `container.makeAppRootViewModel()` — zmieni się na `AppRootViewModel()` w Task 2.
         - `DeluluDetoxTests/AppRootViewModelTests.swift` — używa `AppRootViewModel(container:)` i `vm.screen` (stary enum Screen). Te testy przestaną kompilować się → **wait for Wave 5** (`01.1-05`), który zreseguje testy. W tym planie commentujemy lub tymczasowo komentujemy całe AppRootViewModelTests.swift (zawartość w jednym komentarzu blockowym `/* ... */`) — lub usuwamy plik i Wave 5 go przywraca w nowym layoucie. **Wybór:** usunąć plik — Wave 5 napisze go od nowa w `DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift`.
         - `DeluluDetoxTests/AppRootViewModelTests.swift` — **DELETE** w Task 3 tego planu.
       - Task 2 + Task 3 załatają pozostałe kompilacyjne błędy.
  </action>

  <verify>
    <automated>
      # Nowe lokalizacje
      test -f DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift
      test -f DeluluDetox/Sources/Features/Root/View/AppRootView.swift
      test -f DeluluDetox/Sources/Features/Root/Injection/RootInjection.swift
      # Stare zniknęły
      test ! -f DeluluDetox/Sources/Features/Root/AppRootViewModel.swift
      test ! -f DeluluDetox/Sources/Features/Root/AppRootView.swift
      # @CasePathable Destination z Equatable (B5)
      grep -q "@CasePathable" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift
      grep -qE "enum Destination: *Equatable" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift
      # var destination: Destination?
      grep -q "var destination: Destination?" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift
      # Combine subskrypcja
      grep -q "import Combine" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift
      grep -q "import SwiftUINavigation" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift
      grep -q "observeStatus()" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift
      grep -q ".sink" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift
      grep -q "cancellables" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift
      # @ObservationIgnored przed @LazyInjected (D-10; B6 co-location)
      grep -B1 "@LazyInjected" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift | grep "@ObservationIgnored"
      # Stare znikło
      ! grep -q "enum Screen" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift
      ! grep -q "screenTag" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift
      # AppRootView scenePhase + structural co-location (B4)
      grep -q "scenePhase" DeluluDetox/Sources/Features/Root/View/AppRootView.swift
      grep -q "model.refreshStatus()" DeluluDetox/Sources/Features/Root/View/AppRootView.swift
      # RootInjection
      grep -q "enum RootInjection" DeluluDetox/Sources/Features/Root/Injection/RootInjection.swift
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "@CasePathable" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` → **≥ 1** (D-02)
    - **B5 explicit Equatable on enum:** `grep -E "enum Destination: *Equatable" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` → **≥ 1 match**
    - `grep -cE "case onboarding$|case onboarding\s*$|case onboarding[^(]" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` → **≥ 1** (case bez payloadu — NIE `case onboarding(OnboardingViewModel)`)
    - `grep -c "case .*(.*ViewModel)" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` → **0** (D-02: enum bez VM payloadu)
    - `grep -c "var destination: Destination?" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` → **≥ 1**
    - `grep -c "import Combine" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` → **≥ 1**
    - `grep -c "import SwiftUINavigation" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` → **≥ 1** (D-05)
    - `grep -c "@ObservationIgnored" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` → **≥ 3** (2 UC + cancellables per D-10)
    - `grep -c "@LazyInjected" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` → **≥ 2** (observeStatus + refreshStatus UC)
    - `grep -c "Set<AnyCancellable>" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` → **≥ 1** (D-10)
    - **B6 @ObservationIgnored co-location (narrow greps):**
      - `grep -B1 "private var cancellables:" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift | grep -c "@ObservationIgnored"` → **≥ 1**
      - `grep -B1 "@LazyInjected private var observeStatus" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift | grep -c "@ObservationIgnored"` → **≥ 1**
      - `grep -B1 "@LazyInjected private var refreshStatusUseCase" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift | grep -c "@ObservationIgnored"` → **≥ 1**
    - `grep -c "enum Screen\|screenTag\|DependencyContainer" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` → **0** (D-04, D-01)
    - `grep -c "Repository" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` → **0** (VM→UC rule: VM nie wie o Repository, D-01)
    - `grep -c "scenePhase" DeluluDetox/Sources/Features/Root/View/AppRootView.swift` → **≥ 1** (D-14)
    - `grep -c "model.refreshStatus()" DeluluDetox/Sources/Features/Root/View/AppRootView.swift` → **≥ 1** (D-14)
    - **B4 structural key-link scenePhase → refreshStatus (not just string co-presence):**
      - `grep -A2 "\.onChange(of: scenePhase)" DeluluDetox/Sources/Features/Root/View/AppRootView.swift` output contains both literal `newPhase == .active` AND `model.refreshStatus()` within those 2 lines-after-match range
      - `awk '/\.onChange\(of: scenePhase\)/{flag=1;c=0} flag && /newPhase == \.active/{a=1} flag && /model\.refreshStatus\(\)/{b=1} flag{c++; if (c>=4) flag=0} END{exit !(a && b)}' DeluluDetox/Sources/Features/Root/View/AppRootView.swift` → **exit 0**
    - `grep -c "switch model.destination" DeluluDetox/Sources/Features/Root/View/AppRootView.swift` → **≥ 1** (Wzorzec B)
    - Build przejściowo może być czerwony — Task 2 + 3 + 4 załata. **Wymagane** po Task 3: zielony build + 8 testów zielonych. Task 4 dodaje smoke test.
  </acceptance_criteria>

  <done>
    AppRootViewModel + AppRootView + RootInjection istnieją w feature-first lokalizacji z pełnym implementacją Wzorca B (D-02, D-10, D-11, D-14). Importy Combine + SwiftUINavigation obecne. Destination: Equatable bez VM payload (B5). @ObservationIgnored co-located z każdą @LazyInjected i z cancellables (B6). scenePhase → refreshStatus structurally verified (B4). Żaden import Repository. Stare pliki usunięte. Build jeszcze czerwony — Task 2 i 3 zamkną pętlę, Task 4 smoke-testuje.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pełny bootstrap DIContainer + usunięcie DependencyContainer + onAuthorized</name>

  <read_first>
    - `DeluluDetox/Sources/App/DeluluDetoxApp.swift` (po Wave 3 — ma `OnboardingInjection.register(in: .shared)` + `DependencyContainer()` + `container.makeAppRootViewModel()`)
    - `DeluluDetox/Sources/App/DependencyContainer.swift` (po Wave 3 — do USUNIĘCIA w tym planie)
    - `DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift` (po Wave 3 — ma `var onAuthorized`)
    - `DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift` (po Wave 3 — ma `var onAuthorized`)
    - `.planning/phases/01.1-architecture-foundation/01.1-PATTERNS.md` §2.7 (DeluluDetoxApp docelowy bootstrap), §3.1 wiersz "onAuthorized wiring w AppRootVM init" (znika)
    - `.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md` D-16 (pełny), D-20, D-23
  </read_first>

  <files>
    DeluluDetox/Sources/App/DeluluDetoxApp.swift (MODIFIED — pełny bootstrap wszystkich 4 Injection + AppRootViewModel())
    DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift (MODIFIED — remove onAuthorized)
    DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift (MODIFIED — remove onAuthorized)
    DeluluDetox/Sources/App/DependencyContainer.swift (DELETE — D-23)
  </files>

  <action>
    1. **Zaktualizuj `DeluluDetox/Sources/App/DeluluDetoxApp.swift`** — pełny bootstrap wszystkich 4 feature'ów (D-20) + `AppRootViewModel()` bezargumentowy (już jest w Task 1) + explicit `import SwiftUI` (W13):

    ```swift
    import SwiftUI

    @main
    struct DeluluDetoxApp: App {
        init() {
            let container = DIContainer.shared
            // Kolejność: Onboarding pierwszy (feature-owner ScreenTimeAuth*, D-17). Reszta no-op po nim.
            OnboardingInjection.register(in: container)
            DenialInjection.register(in: container)
            HomeInjection.register(in: container)
            RootInjection.register(in: container)
        }

        var body: some Scene {
            WindowGroup {
                AppRootView(model: AppRootViewModel())
            }
        }
    }
    ```

    Zmiany:
    - Usunięto `private let container = DependencyContainer()`.
    - Usunięto `container.makeAppRootViewModel()` — zastąpione `AppRootViewModel()`.
    - Pełna sekwencja 4×`<Feature>Injection.register(in: container)` (D-20).
    - `import SwiftUI` pozostaje (wymagany dla `@main App` macro + `WindowGroup`).

    2. **Zaktualizuj `DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift`** — usuń `onAuthorized` callback (D-16 pełny). Efekt: VM nie publikuje eventu authorized przez callback — robi to przez repo `statusSubject.send(...)` w `requestAuthorization()` po udanym grantcie. AppRootVM słucha przez ObserveUC i aktualizuje `destination`.

    Docelowa treść:

    ```swift
    import Observation
    import os

    @MainActor
    @Observable
    final class OnboardingViewModel: @unchecked Sendable {
        private(set) var isRequesting = false
        private(set) var error: String?

        @ObservationIgnored
        @LazyInjected private var requestAuth: RequestScreenTimeAuthUseCase

        @ObservationIgnored
        private let logger = Logger(subsystem: "com.kksw.DeluluDetox", category: "Onboarding")

        init() {}

        func grantAccessTapped() async {
            isRequesting = true
            error = nil
            do {
                try await requestAuth()
                logger.info("Screen Time authorization granted")
                // Event flow przez Combine publisher na repo.statusSubject — AppRootVM aktualizuje destination (D-11).
            } catch {
                logger.error("Authorization failed: \(error.localizedDescription, privacy: .public)")
                self.error = error.localizedDescription
            }
            isRequesting = false
        }
    }
    ```

    3. **Zaktualizuj `DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift`** — analogicznie:

    ```swift
    import Observation
    import os

    @MainActor
    @Observable
    final class DenialViewModel: @unchecked Sendable {
        private(set) var isRequesting = false

        @ObservationIgnored
        @LazyInjected private var requestAuth: RequestScreenTimeAuthUseCase

        @ObservationIgnored
        private let logger = Logger(subsystem: "com.kksw.DeluluDetox", category: "Denial")

        init() {}

        func retryTapped() async {
            isRequesting = true
            do {
                try await requestAuth()
                logger.info("Screen Time authorization granted on retry")
                // Event flow przez Combine — AppRootVM aktualizuje destination (D-11).
            } catch {
                logger.error("Retry authorization failed: \(error.localizedDescription, privacy: .public)")
            }
            isRequesting = false
        }
    }
    ```

    4. **USUŃ `DeluluDetox/Sources/App/DependencyContainer.swift`** (D-23):
       - `rm DeluluDetox/Sources/App/DependencyContainer.swift`

    5. **Usuń `authorizationStatus` transitional z protokołu Repository** — po Wave 3 był TRANSITIONAL w protokole (używany przez AppRootVM.init i AppRootVM.checkAuthorization). Po Task 1 tego planu AppRootVM nie używa już `authorizationStatus` (tylko `statusPublisher` przez ObserveUC). Usuwamy więc z protokołu + z impl żeby nie było martwego kodu.

    Zaktualizuj `DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift`:

    ```swift
    import Combine
    import FamilyControls

    // MARK: - Protocol

    protocol ScreenTimeAuthRepository: Sendable {
        var statusPublisher: AnyPublisher<AuthorizationStatus, Never> { get }
        func requestAuthorization() async throws
        func refreshStatus()
    }

    // MARK: - Implementation

    final class ScreenTimeAuthRepositoryImpl: ScreenTimeAuthRepository, @unchecked Sendable {
        private let statusSubject: CurrentValueSubject<AuthorizationStatus, Never>

        var statusPublisher: AnyPublisher<AuthorizationStatus, Never> {
            statusSubject.eraseToAnyPublisher()
        }

        init() {
            self.statusSubject = CurrentValueSubject(AuthorizationCenter.shared.authorizationStatus)
        }

        func requestAuthorization() async throws {
            try await AuthorizationCenter.shared.requestAuthorization(for: .individual)
            statusSubject.send(AuthorizationCenter.shared.authorizationStatus)
        }

        func refreshStatus() {
            statusSubject.send(AuthorizationCenter.shared.authorizationStatus)
        }
    }
    ```

    (Zmiana: usunięta `var authorizationStatus: AuthorizationStatus` z protokołu i z impl.)

    Uwaga: `MockScreenTimeAuthRepository` w testach nadal ma `var authorizationStatus: AuthorizationStatus { stubbedStatus }` — to OK (mock może mieć dodatkowe publiczne property, protocol conformance tego nie zabrania). Ale jeśli linter lub Swift 6.2 ostrzega o "redundant member" — można usunąć. Wave 5 (testy) przegląda mocki.

    6. `xcodegen generate`.

    7. Build przez XcodeBuildMCP `build_sim` scheme=DeluluDetox → **exit 0**.
       - Jeśli `AppRootViewModelTests.swift` (stary, niezmieniony) powoduje błędy kompilacji — to oczekiwane. Task 3 USUWA ten plik.
  </action>

  <verify>
    <automated>
      # DependencyContainer USUNIĘTY (D-23)
      test ! -f DeluluDetox/Sources/App/DependencyContainer.swift
      # Brak jakichkolwiek referencji
      ! grep -rq "DependencyContainer" DeluluDetox/Sources/
      # DeluluDetoxApp pełny bootstrap
      grep -q "OnboardingInjection.register" DeluluDetox/Sources/App/DeluluDetoxApp.swift
      grep -q "DenialInjection.register" DeluluDetox/Sources/App/DeluluDetoxApp.swift
      grep -q "HomeInjection.register" DeluluDetox/Sources/App/DeluluDetoxApp.swift
      grep -q "RootInjection.register" DeluluDetox/Sources/App/DeluluDetoxApp.swift
      grep -q "AppRootViewModel()" DeluluDetox/Sources/App/DeluluDetoxApp.swift
      # W13: import SwiftUI obecny
      grep -q "^import SwiftUI$" DeluluDetox/Sources/App/DeluluDetoxApp.swift
      # onAuthorized usunięte
      ! grep -q "onAuthorized" DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift
      ! grep -q "onAuthorized" DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift
      # authorizationStatus usunięte z protokołu/impl
      ! grep -q "var authorizationStatus" DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift
    </automated>
  </verify>

  <acceptance_criteria>
    - `test ! -f DeluluDetox/Sources/App/DependencyContainer.swift` → **exit 0** (plik usunięty, D-23)
    - `grep -rc "DependencyContainer" DeluluDetox/Sources/` → **0 hits** (żadnej referencji w source; testy obsługiwane osobno w Task 3)
    - `grep -c "Injection.register" DeluluDetox/Sources/App/DeluluDetoxApp.swift` → **== 4** (D-20: 4 feature'y)
    - `grep -c "AppRootViewModel()" DeluluDetox/Sources/App/DeluluDetoxApp.swift` → **≥ 1**
    - **W13:** `grep -q "^import SwiftUI$" DeluluDetox/Sources/App/DeluluDetoxApp.swift` → **exit 0**
    - `grep -rc "onAuthorized" DeluluDetox/Sources/Features/` → **0 hits** (D-16 pełny)
    - `grep -c "var authorizationStatus" DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift` → **0** (protokół odchudzony)
    - `grep -c "checkAuthorization" DeluluDetox/Sources/` (recursive) → **0** (stary wzorzec znikł)
    - XcodeBuildMCP `build_sim` scheme=DeluluDetox → **exit 0** (po warunku że Task 3 usuwa stare `AppRootViewModelTests.swift`; **wolno** że build jest czerwony do Task 3)
  </acceptance_criteria>

  <done>
    `DependencyContainer.swift` usunięty z repo (D-23). DeluluDetoxApp bootstrapuje 4 feature'y przez DIContainer.shared (D-20). OnboardingVM/DenialVM nie mają już callbacku onAuthorized (D-16 pełny). ScreenTimeAuthRepository protokół odchudzony o transitional `authorizationStatus`. Build source'ów przechodzi — tylko testy stare mogą jeszcze walić się, załatane Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Usuń stary AppRootViewModelTests.swift (Wave 5 pisze nowy)</name>

  <read_first>
    - `DeluluDetoxTests/AppRootViewModelTests.swift` (63 linii — cały plik; niekompatybilny po Task 1-2)
    - `.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md` D-26 (no-op CLAUDE.md review), D-27 (mirror layout planowany w Wave 5)
  </read_first>

  <files>
    DeluluDetoxTests/AppRootViewModelTests.swift (DELETE — Wave 5 napisze nowy w DeluluDetoxTests/Features/Root/)
  </files>

  <action>
    1. **Usuń `DeluluDetoxTests/AppRootViewModelTests.swift`** — plik używa:
       - `DependencyContainer(screenTimeAuthRepository:)` (nie istnieje)
       - `AppRootViewModel(container:)` (nie istnieje)
       - `vm.screen` + `case .onboarding` / `.home` / `.denial` (Screen enum nie istnieje — jest Destination)
       - `vm.checkAuthorization()` (metoda nie istnieje — destination aktualizuje się przez Combine)

    Naprawa wymaga kompletnego rewrite'u testu z: DIContainer.reset + register mock ObserveUC + test że emisja statusu zmienia `vm.destination`. To jest zakres **Wave 5** (`01.1-05`) — pełny mirror layout (`DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift`) z dedykowanymi mock'ami UC. W tym planie **usuwamy plik** żeby build testów przeszedł.

    Komenda: `rm DeluluDetoxTests/AppRootViewModelTests.swift`

    2. **NIE usuwaj** `DeluluDetoxTests/OnboardingViewModelTests.swift` — ten plik po Wave 3 Task 3 kompiluje się (`OnboardingViewModel()` bezargumentowy + DIContainer.reset+register w setUp). Ale **teraz**, po usunięciu `onAuthorized` (Task 2 tego planu), test `testGrantAccessSuccess` odwołuje się do `vm.onAuthorized = ...` — to nie skompiluje się.

    Minimalna naprawa OnboardingViewModelTests — usuń testowanie callbacka (Wave 5 zastąpi asercją na statusPublisher):

    ```swift
    import XCTest
    @testable import DeluluDetox

    @MainActor
    final class OnboardingViewModelTests: XCTestCase {
        var mockRepo: MockScreenTimeAuthRepository!

        override func setUp() {
            super.setUp()
            DIContainer.shared.reset()
            mockRepo = MockScreenTimeAuthRepository()
            DIContainer.shared.register(ScreenTimeAuthRepository.self, scope: .application) { [mockRepo] _ in
                mockRepo!
            }
            DIContainer.shared.register(RequestScreenTimeAuthUseCase.self, scope: .unique) { c in
                RequestScreenTimeAuthUseCaseImpl(repository: c.resolve())
            }
        }

        func testGrantAccessSuccess() async {
            let vm = OnboardingViewModel()
            await vm.grantAccessTapped()

            XCTAssertEqual(mockRepo.requestAuthorizationCallCount, 1)
            XCTAssertNil(vm.error)
            XCTAssertFalse(vm.isRequesting)
            // Event authorized — pełny test przez statusPublisher w Wave 5 (`01.1-05`).
        }

        func testGrantAccessFailure() async {
            mockRepo.requestAuthorizationError = NSError(
                domain: "FamilyControls", code: 2,
                userInfo: [NSLocalizedDescriptionKey: "Invalid account type"]
            )
            let vm = OnboardingViewModel()
            await vm.grantAccessTapped()

            XCTAssertNotNil(vm.error)
            XCTAssertFalse(vm.isRequesting)
        }

        func testIsRequestingStartsFalse() async {
            let vm = OnboardingViewModel()
            XCTAssertFalse(vm.isRequesting)
        }
    }
    ```

    Zmiany vs Wave 3:
    - Usunięto `var authorizedCalled = false` + `vm.onAuthorized = { authorizedCalled = true }`.
    - `testGrantAccessSuccess` asercja przeniesiona z `authorizedCalled` na `mockRepo.requestAuthorizationCallCount == 1`.
    - `testIsRequestingDuringRequest` → `testIsRequestingStartsFalse` (nazwa bardziej precyzyjna).

    3. Zaktualizuj `DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift` — usuń `var authorizationStatus: AuthorizationStatus { stubbedStatus }` ponieważ protokół już go nie wymaga (Task 2 usunął z protokołu). Wave 5 przeniesie mocka do mirror layout i doda dedykowane mocki UC.

    Zaktualizowana treść:

    ```swift
    import Combine
    import FamilyControls
    @testable import DeluluDetox

    final class MockScreenTimeAuthRepository: ScreenTimeAuthRepository, @unchecked Sendable {
        var stubbedStatus: AuthorizationStatus = .notDetermined {
            didSet { statusSubject.send(stubbedStatus) }
        }
        var requestAuthorizationError: Error?
        var requestAuthorizationCallCount = 0
        var refreshStatusCallCount = 0

        private let statusSubject: CurrentValueSubject<AuthorizationStatus, Never>

        var statusPublisher: AnyPublisher<AuthorizationStatus, Never> {
            statusSubject.eraseToAnyPublisher()
        }

        init() {
            self.statusSubject = CurrentValueSubject(.notDetermined)
        }

        func requestAuthorization() async throws {
            requestAuthorizationCallCount += 1
            if let error = requestAuthorizationError {
                throw error
            }
            stubbedStatus = .approved
        }

        func refreshStatus() {
            refreshStatusCallCount += 1
            statusSubject.send(stubbedStatus)
        }
    }
    ```

    4. Build + test przez XcodeBuildMCP:
       - `build_sim` scheme=DeluluDetox → exit 0
       - `test_sim` scheme=DeluluDetox → **wszystkie zielone**:
         - OnboardingViewModelTests (3/3) — nowa treść bez onAuthorized
         - DeluluDetoxTests placeholder (1/1)
         - **łącznie 4/4 passed** (utrata AppRootViewModelTests do Wave 5 — 4 testy mniej niż Phase 1)

    5. **Manual smoke test** (opcjonalne — Task 4 robi formalny non-blocking smoke test z exit-code gate).
  </action>

  <verify>
    <automated>
      # Stary plik AppRootViewModelTests usunięty (do Wave 5)
      test ! -f DeluluDetoxTests/AppRootViewModelTests.swift
      # OnboardingViewModelTests nie używa onAuthorized
      ! grep -q "onAuthorized" DeluluDetoxTests/OnboardingViewModelTests.swift
      # MockRepo nie ma authorizationStatus (protokół odchudzony)
      ! grep -q "var authorizationStatus" DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift
      # Nadal ma statusPublisher
      grep -q "statusPublisher" DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift
      # Zero referencji do DependencyContainer w testach
      ! grep -rq "DependencyContainer" DeluluDetoxTests/
    </automated>
  </verify>

  <acceptance_criteria>
    - `test ! -f DeluluDetoxTests/AppRootViewModelTests.swift` → **exit 0**
    - `grep -rc "DependencyContainer" DeluluDetoxTests/` → **0 hits**
    - `grep -c "onAuthorized" DeluluDetoxTests/` (recursive) → **0 hits**
    - `grep -c "var authorizationStatus" DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift` → **0**
    - `grep -c "statusPublisher" DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift` → **≥ 1**
    - XcodeBuildMCP `build_sim` scheme=DeluluDetox → **exit 0**
    - XcodeBuildMCP `test_sim` scheme=DeluluDetox → **4/4 passed** (3 OnboardingViewModelTests + 1 DeluluDetoxTests placeholder; AppRootViewModelTests temporarily 0/0 aż Wave 5 je przywróci)
    - **Grep architektoniczne (must = 0):**
      - `grep -rc "import.*Repository\|: ScreenTimeAuthRepository\b" DeluluDetox/Sources/Features/**/ViewModel/*.swift | grep -v "UseCase"` → **0** (VM→Repo = brak)
      - `grep -rc "DependencyContainer" DeluluDetox/Sources/` → **0**
      - `grep -rc "onAuthorized" DeluluDetox/Sources/Features/` → **0** (D-16 pełny)
      - `grep -rc "enum Screen\|screenTag\|case onboarding(OnboardingViewModel)" DeluluDetox/Sources/Features/Root/` → **0** (D-04)
    - **Grep aktywacji swift-navigation (must ≥ 1):**
      - `grep -rc "import SwiftUINavigation" DeluluDetox/Sources/Features/Root/` → **≥ 1** (D-05)
      - `grep -c "@CasePathable" DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` → **≥ 1** (D-02)
  </acceptance_criteria>

  <done>
    Build zielony. 4/4 testów passed. AppRootViewModelTests.swift usunięty (Wave 5 go przywraca w `DeluluDetoxTests/Features/Root/`). OnboardingViewModelTests zaktualizowany (brak onAuthorized). MockScreenTimeAuthRepository zsynchronizowany z protokołem. App startuje przez DIContainer.shared bootstrap; Phase 1 flow (cold-launch → Onboarding → grant → Home; denied → Denial → retry; scenePhase .active → refreshStatus) zachowany.

    **Wave 4 zamyka pętlę refaktoru kodu.** Pozostaje: Task 4 (non-blocking smoke test), Wave 5 (testy migration + docs) + Wave 6 (cleanup to_integrate + extension regression + human-verify).
  </done>
</task>

<task type="auto">
  <name>Task 4: Non-blocking smoke test — run-on-simulator gate (I19)</name>

  <read_first>
    - `.claude/guides/xcodebuild-mcp/GUIDE.md` (sekcja `build_run_sim` + jak odczytać exit code)
  </read_first>

  <files>
    (tylko weryfikacja — brak modyfikacji plików)
  </files>

  <action>
    1. Uruchom **non-blocking smoke test** przez XcodeBuildMCP:
       - Wywołaj `build_run_sim` scheme=DeluluDetox na default simulatorze.
       - Czekaj do momentu aż symulator uruchomi app (typowo ≤ 15s).
       - Odczytaj **exit code** z wywołania `build_run_sim`.

    2. **Autonomous gate (nie `checkpoint:human-verify` — zostajemy autonomous per `autonomous: true` w frontmatter):**
       - Jeśli `build_run_sim` exit code == 0 → PASS, kontynuuj plan. Zapisz w SUMMARY: "Smoke test: app uruchomiona na simulatorze po pełnym refaktorze DIContainer (PASS)."
       - Jeśli exit code ≠ 0 → FAIL, zgłoś w SUMMARY jako BLOCKER z output logów. Typowe przyczyny:
         - fatalError z DIContainer.resolve (któryś register się nie wykonał; sprawdź że DeluluDetoxApp.init() faktycznie woła 4 Injection.register).
         - Swift 6.2 concurrency violation (np. `AppRootViewModel.init()` wywoływane z non-MainActor context).
         - swift-navigation macro error (jeśli `@CasePathable` macro nie jest dostępny bo Package.swift nie został zbindowany prawidłowo przy xcodegen).

    3. **NIE wołaj usera** na checkpoint — to autonomous gate. Formalny human-verify jest dopiero w Wave 6 (`01.1-07`) po cleanup. Tutaj tylko automatyczna bramka exit code.

    4. Ten task jest **non-modifying** — nie tworzy ani nie edytuje plików. Jego jedyny output to gate na exit code + log w SUMMARY.
  </action>

  <verify>
    <automated>
      # Zero zmian plików (smoke test jest read-only z punktu widzenia repo)
      # Verify poprzez reuse poprzednich acceptance criteria (Task 1-3 już zbudowało projekt zielony).
      # Nowy verify: build_run_sim exit code == 0 raportowany w SUMMARY (autor taska zapisuje ręcznie).
      true  # formalna placeholder komenda — prawdziwy signal pochodzi z XcodeBuildMCP call result
    </automated>
  </verify>

  <acceptance_criteria>
    - XcodeBuildMCP `build_run_sim` scheme=DeluluDetox → **exit 0** (app uruchomiona na simulatorze, brak fatalError przy cold-launch)
    - SUMMARY zawiera wiersz: "Smoke test (I19): PASS" albo "Smoke test (I19): FAIL — [exit code] — [log excerpt]" (w tym drugim przypadku plan jako całość jest BLOCKER aż do resolucji — nie przechodzimy do Wave 5).
    - Gate jest **autonomous** (autor taska sprawdza exit code, nie czeka na human approval). Plan pozostaje `autonomous: true`.
  </acceptance_criteria>

  <done>
    Autonomous smoke test zakończony sukcesem (exit code 0). App uruchomiona na simulatorze; DIContainer bootstrap w `DeluluDetoxApp.init()` wykonał się bez fatalError; AppRootViewModel.init() zsubskrybował ObserveUC i ustawił destination. Plan 01.1-04 zamknięty — refaktor kodu kompletny.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

ScreenTime authorization prompt — identyczny kod (repo.requestAuthorization przez `AuthorizationCenter.shared.requestAuthorization(for: .individual)`). TCC prompt invariant zachowany. Nowy komponent: `DIContainer.shared` jako runtime resolver. Żadnych nowych cross-process/IPC/network boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-12 | Denial of Service | AppRootVM.init() subskrybuje observe() — jeśli Injection.register nie wykonał się przed init, `@LazyInjected` resolve → fatalError | mitigate | `DeluluDetoxApp.init()` synchronizuje 4 Injection.register PRZED `body: some Scene`. SwiftUI gwarantuje, że `App.init` kończy się przed tworzeniem sceny. `WindowGroup { AppRootView(model: AppRootViewModel()) }` inicjalizuje VM po bootstrapie. Kontrola: Task 2 acceptance criteria wymaga 4 `Injection.register` + `AppRootViewModel()` w `DeluluDetoxApp.swift`. Task 4 smoke test weryfikuje autonomous na simulatorze. |
| T-01.1-13 | Information Disclosure | `var destination: Destination?` publicznie read-write na VM | accept | Destination enum zawiera tylko 3 rozłączne stany (onboarding/denial/home) — brak danych poufnych. Write dostęp konieczny dla swift-navigation case-path bindings (PATTERNS §3.1 ostrzega: NIE `private(set)`). Mutacje poza `.sink` nie są częścią Phase 1/01.1 — Wave 5 testy weryfikują że jedyne źródło mutacji to Combine sink. |
| T-01.1-14 | Tampering | Combine `.sink { self?.destination = ... }` — closure capture `self` | mitigate | `[weak self]` zapobiega retain cycle. `cancellables: Set<AnyCancellable>` w deinicie VM zwalnia subskrypcje (auto przez ARC). `@ObservationIgnored` wymusza że dodanie do Set nie invaliduje observation — D-10. |
| T-01.1-15 | **Elevation of Privilege** | RefreshUC można wywołać z dowolnego miejsca VM | mitigate | `refreshStatusUseCase` jest `private` w AppRootVM. Jedyne wywołanie: `func refreshStatus()` (publiczna metoda wołana przez AppRootView .onChange(of: scenePhase)). Każde inne wywołanie byłoby code smell i znajdzie się grepem w code review. Samo wywołanie refresh jest idempotentne — nie eskaluje privileges (czyta authorizationStatus, nie modyfikuje). |
| T-01.1-16 | **Core Value Violation** | User cofa Screen Time w Settings → AppRootVM nie wie → UI pokazuje Home choć blokada nie działa | **mitigate (D-13 core-value lens)** | D-14: AppRootView `.onChange(of: scenePhase) { if .active { model.refreshStatus() } }`. RefreshUC → repo.refreshStatus() → statusSubject.send(AuthorizationCenter.shared.authorizationStatus) → destination się aktualizuje. Weryfikowane w Task 1 acceptance criteria B4 (structural awk check scenePhase block zawiera oba literały) i w Wave 6 human-verify. **To jest proaktywna ochrona core value — nie kosmetyka.** |

**TCC prompt invariant:** niezmieniony. Ten sam `AuthorizationCenter.shared.requestAuthorization(for: .individual)` call. Nie uzyskujemy więcej entitlements ani nie rozszerzamy auth scope.

**App Group sandbox invariant:** niezmieniony. Żaden kod w tym planie nie pisze/czyta z `group.com.kksw.DeluluDetox`.

**Extensions baseline (D-30):** Ten plan NIE dotyka `Extensions/**`. Regression check przesunięty do Wave 6 (`01.1-07`).
</threat_model>

<verification>
1. Pliki Features/Root/ w feature-first layout (ViewModel, View, Injection podkatalogi).
2. `DependencyContainer.swift` usunięty z repo.
3. `onAuthorized` usunięty z OnboardingVM/DenialVM.
4. `enum Screen` + `screenTag` + `checkAuthorization()` usunięte — zastąpione `@CasePathable enum Destination: Equatable` + Combine subscription.
5. AppRootVM zależy od 2 UC (ObserveUC, RefreshUC) — zero Repository.
6. AppRootView używa child-owned VMs (@State private var) + switch nad destination + scenePhase hook; B4 structural check passes (awk confirms `newPhase == .active` and `model.refreshStatus()` co-located inside onChange closure).
7. DeluluDetoxApp.init() bootstrapuje wszystkie 4 feature'y; `import SwiftUI` explicit (W13).
8. Protokół ScreenTimeAuthRepository odchudzony (statusPublisher + requestAuthorization + refreshStatus).
9. Build zielony. OnboardingViewModelTests 3/3 + DeluluDetoxTests 1/1 = 4/4 passed.
10. Wszystkie gwarantowane grep'y architektoniczne (Repo→Repo=0, VM→Repo=0, DependencyContainer=0, onAuthorized=0, enum Screen=0).
11. B5 explicit Equatable on Destination enum → weryfikowane pojedynczym greppem.
12. B6 @ObservationIgnored co-location dla każdej @LazyInjected i dla cancellables.
13. I19 Task 4 smoke test: `build_run_sim` exit 0 (autonomous gate).

**Manual smoke test** (formalny → Wave 6 human-verify):
- App startuje na simulatorze
- Phase 1 flow działa: cold-launch → Onboarding (jeśli .notDetermined) → tap Grant Access → system prompt → grant → Home
- Denied path: app startuje w `.denied` → Denial → tap Let's Try Again → grant → Home
- scenePhase .active refresh: background app → Settings → cofnij Screen Time → foreground app → destination updates (NIE weryfikowalne na symulatorze — na device only; acceptance jest: Wave 6 human-verify na realnym urządzeniu lub skip jako "best-effort sim test")
</verification>

<success_criteria>
- 3 nowe pliki w `Features/Root/{ViewModel,View,Injection}/` + 2 stare usunięte.
- `DependencyContainer.swift` usunięty (D-23).
- `onAuthorized` usunięty z 2 VM (D-16 pełny).
- AppRootVM: `@CasePathable Destination: Equatable` (B5) + Combine sink + `@ObservationIgnored cancellables` + LazyInjected 2 UC (B6 co-location) + bezargumentowy init + `refreshStatus()` method.
- AppRootView: switch nad destination + child @State VMs + scenePhase hook wołający model.refreshStatus() (B4 structural verified).
- DeluluDetoxApp.init() rejestruje 4×Injection w DIContainer.shared + body używa `AppRootViewModel()` + explicit `import SwiftUI` (W13).
- ScreenTimeAuthRepository protokół bez transitional `authorizationStatus`.
- AppRootViewModelTests.swift usunięty (Wave 5 odtworzy w mirror layout).
- OnboardingViewModelTests zsynchronizowany z usuniętym onAuthorized.
- Build zielony. 4/4 testów passed. I19 smoke test `build_run_sim` exit 0.
- Decyzje spełnione: D-01, D-02, D-03, D-04, D-05, D-10, D-11, D-13 (konsumpcja), D-14, D-16 (pełny), D-20, D-23.
- Core value chroniona przez scenePhase → refreshStatus (D-13/D-14), structurally verified (B4).
- Architektura zgodna z wszystkimi 4 regułami:
  - Repo ↛ Repo ✓ (1 repo w projekcie, brak cross-ref)
  - UseCase → UseCase/Repo ✓
  - VM → tylko UseCase ✓ (0 imports Repository w VM files)
  - View → tylko VM ✓ (views nie importują UC ani Repository)
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-architecture-foundation/01.1-04-SUMMARY.md` zawierający:
- Pełną listę plików NEW/DELETED/MODIFIED
- Wynik ALL grep-acceptance kryteriów z Tasks 1-3 (pass/fail)
- Wynik ALL structural checks z Task 1 (B4 awk, B5 Equatable grep, B6 co-location greps, W13 import SwiftUI grep)
- Wynik XcodeBuildMCP build + test (4/4 passed — bez AppRootViewModelTests które wraca w Wave 5)
- Wynik Task 4 smoke test (I19): `build_run_sim` exit code + log excerpt
- Pełną treść docelową AppRootViewModel.swift (inline) — żeby Wave 5 test-executor miał referencję do pisania testów
- Tabelę: każda decyzja D-01..D-31 → status (DONE / PENDING-WAVE-X / N/A)
- Notatkę: "Refaktor kodu zakończony + smoke test autonomiczny PASS. Pozostałe: Wave 5 (testy migration + docs updates), Wave 6 (cleanup to_integrate + extension regression + human-verify)."
- **Sanity check dla Wave 5 executora:** dokładna lista plików testowych do utworzenia w mirror layout i pliki źródłowe do których istnieją testy — patrz Wave 5 plan.
</output>
</content>
