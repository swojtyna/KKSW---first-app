# KKSW---first-app

iOS app, Swift + SwiftUI. Greenfield — Xcode project not scaffolded yet.

## Repo layout

- `CLAUDE.md` — main entry point for Claude Code (project rules, guides index)
- `.claude/guides/` — step-by-step how-to docs (XcodeGen, XcodeBuildMCP, authoring new guides)
- `.claude/research/` — curated product & API research (Screen Time API / Opal-clone, AI on-device, rynek). Index: `.claude/research/README.md`
- `.claude/settings.json` — project-scoped Claude Code config and plugin enablement

Build/run/test commands will land here once the Xcode project is generated.
