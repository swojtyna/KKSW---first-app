---
phase: 01.1-architecture-foundation
plan: 04
subsystem: root-architecture
tags: [feature-first, case-pathable, combine, di-bootstrap, wzorzec-b, destination-enum, swift-navigation, d-23, d-20, w4]

# Dependency graph
requires:
  - phase: 01.1-architecture-foundation
    plan: 03
    provides: "Onboarding/Denial/Home VM feature-first + @LazyInjected; OnboardingInjection rejestrowane w DeluluDetoxApp.init(); transitional DependencyContainer shim"
provides:
  - "Features/Root/{ViewModel,View,Injection}/ feature-first: AppRootViewModel z @CasePathable Destination: Equatable + Combine .sink na ObserveUC; AppRootView Wzorzec B (child-owned @State VMs + switch na destination + scenePhase→refreshStatus)"
  - "RootInjection no-op enum (symetria architektoniczna, D-01)"
  - "DeluluDetoxApp.init() pełny bootstrap 4 feature'ów (OnboardingInjection + DenialInjection + HomeInjection + RootInjection) w DIContainer.shared (D-20)"
  - "OnboardingViewModel + DenialViewModel bez onAuthorized callback (D-16 pełny) — event flow przez Combine publisher na repo.statusSubject"
  - "ScreenTimeAuthRepository protokół odchudzony — transitional authorizationStatus usunięty"
affects: [01.1-05, 01.1-06, 01.1-07]

# Tech tracking
tech-stack:
  added:
    - "SwiftUINavigation (aktywacja @CasePathable macro; package był w Package.swift od Phase 1, pierwsza realna konsumpcja w tym planie per D-05)"
  patterns:
    - "Wzorzec B (D-24): @CasePathable enum Destination z cases BEZ VM payload; child-owned @State VMs w parent View"
    - "Combine .sink { [weak self] in self?.destination = Self.map($0) }.store(in: &cancellables) dla subskrypcji authorization status (D-10)"
    - "B6 co-location: @ObservationIgnored bezpośrednio przed każdą @LazyInjected i przed var cancellables"
    - "B5 explicit Equatable: enum Destination: Equatable — wymagane dla .animation(value:) i testowania"
    - "B4 structural co-location: .onChange(of: scenePhase) blok zawiera zarówno `newPhase == .active` jak i `model.refreshStatus()` w tym samym closure scope"
    - "Full distributed DI bootstrap: 4x <Feature>Injection.register(in: container) wołane atomicznie w App.init() przed tworzeniem sceny"
    - "Child-owned VM via @State w parent View (D-03 Wzorzec B) — każdy ekran ma swój @State VM, switch wybiera który wyświetlić; VM rezolwuje UC leniwie przy pierwszym dostępie"

key-files:
  created:
    - DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift
    - DeluluDetox/Sources/Features/Root/View/AppRootView.swift
    - DeluluDetox/Sources/Features/Root/Injection/RootInjection.swift
  modified:
    - DeluluDetox/Sources/App/DeluluDetoxApp.swift
    - DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift
    - DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift
    - DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift
    - DeluluDetoxTests/OnboardingViewModelTests.swift
    - DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift
  deleted:
    - DeluluDetox/Sources/App/DependencyContainer.swift
    - DeluluDetox/Sources/Features/Root/AppRootViewModel.swift
    - DeluluDetox/Sources/Features/Root/AppRootView.swift
    - DeluluDetoxTests/AppRootViewModelTests.swift

key-decisions:
  - "Wzorzec B (enum payload-free Destination) wybrane zgodnie z D-02 + D-24 — child View nie dostaje VM z payload, AppRootView trzyma 3x @State VM i wybiera który pokazać przez switch"
  - "Theme.background (Color) użyty bezpośrednio dla .none placeholder — plan spec mówił 'Color(Theme.background)' ale Theme.background jest już Color, więc Color(…) wrapping byłby błędem kompilacji. Użyto bezpośrednio."
  - "Mock nie ma set(status:) helper (plan mock spec też go nie miał) — stubbedStatus.didSet { statusSubject.send } wystarczy do sterowania publisherem w testach"
  - "AppRootViewModelTests.swift usunięty (4 testy). Wave 5 (01.1-05) odtworzy w DeluluDetoxTests/Features/Root/ z DIContainer.reset + register mock pattern"
  - "refreshStatus() jako publiczna metoda AppRootViewModel (nie refreshStatusUseCase bezpośrednio) — enkapsulacja T-01.1-15 EoP: tylko kontrolowany punkt wywołania"

patterns-established:
  - "Atomic finale commit: refaktor AppRoot + delete DependencyContainer + remove onAuthorized w jednym planie (3 commits, ale semantycznie atomowe — żaden commit samotnie nie ma prawa żyć produktywnie, dopiero Task 3 daje zielony build)"
  - "Autonomous smoke test gate (I19) — exit code z simctl launch/install jako non-blocking signal dla plan completion"
  - "@State private var model = ViewModel() w parent View zamiast child view owning VM — pragmatyczny wariant Wzorca B gdy 3 ekrany są rozłączne i rzadko przełączane (scenePhase-triggered refresh)"

requirements-completed: []  # Phase 01.1 architekturalna — zamyka D-01, D-02, D-04, D-05 (aktywacja), D-10, D-11, D-13 (konsumpcja), D-14, D-16 (pełny), D-20, D-23

# Metrics
duration: 5min
completed: 2026-04-19
---

# Phase 01.1 Plan 04: AppRoot Wzorzec B + DIContainer Bootstrap + DependencyContainer Deletion Summary

**Atomic finale commit fazy 01.1: AppRootViewModel przerefaktorowany na `@CasePathable enum Destination: Equatable` (Wzorzec B) + Combine subskrypcja na `ObserveScreenTimeAuthStatusUseCase`; AppRootView na child-owned `@State` VMs + `switch model.destination` + `scenePhase == .active → model.refreshStatus()` (B4 structural co-location); DeluluDetoxApp.init() pełny bootstrap 4 feature'ów (Onboarding + Denial + Home + Root) w DIContainer.shared (D-20); `DependencyContainer.swift` USUNIĘTY z repo (D-23); `onAuthorized` callback usunięty z OnboardingVM/DenialVM (D-16 pełny) — event flow teraz przez Combine publisher; `ScreenTimeAuthRepository` protokół odchudzony o transitional `authorizationStatus`. Build zielony na iPhone 17 / iOS 26.2, 4/4 testów passed, smoke test (I19) exit 0 — app uruchomiona na simulatorze, zero fatalError w logach.**

## Performance

- **Duration:** ~5 min 9 sec
- **Started:** 2026-04-19T00:29:21Z
- **Completed:** 2026-04-19T00:34:30Z
- **Tasks:** 4 (wszystkie auto — Task 4 non-blocking smoke gate)
- **Files created:** 3, modified: 6, deleted: 4

## Accomplishments

- `Features/Root/` przesunięte do feature-first layoutu (ViewModel/, View/, Injection/ subfolders) — spójne z Onboarding/Denial/Home z Wave 3.
- **AppRootViewModel** kompletnie zrefaktorowany:
  - `@CasePathable enum Destination: Equatable { .onboarding, .denial, .home }` — Wzorzec B payload-free (D-02, B5 explicit Equatable).
  - `var destination: Destination?` — NIE `private(set)` (case-path bindings swift-navigation potrzebują write access; B6 PATTERNS §3.1).
  - `@ObservationIgnored @LazyInjected private var observeStatus` + `refreshStatusUseCase` — B6 co-location z każdą deklaracją poprzedzoną własnym `@ObservationIgnored` (D-10).
  - `@ObservationIgnored private var cancellables: Set<AnyCancellable> = []` (D-10).
  - `init()` bezargumentowy + Combine `.sink { [weak self] status in self?.destination = Self.map(status) }.store(in: &cancellables)` (D-10, D-11).
  - `func refreshStatus()` publiczna — wołana przez AppRootView scenePhase hook (D-13 core-value, D-14).
  - `@MainActor @Observable final class` + `@unchecked Sendable` — wymóg DI GUIDE dla @LazyInjected VM.
  - Imports: `Combine` + `SwiftUINavigation` (pierwsza realna konsumpcja — D-05 aktywacja).
  - Usunięte: `enum Screen`, `screenTag`, `private let container: DependencyContainer`, `init(container:)`, `func checkAuthorization()`, `import FamilyControls`(nie — ten jest potrzebny dla `AuthorizationStatus` w `map()`).
- **AppRootView** zrefaktorowany na Wzorzec B (D-24 + D-03):
  - `@State private var onboardingModel = OnboardingViewModel()` + `denialModel` + `homeModel` — child-owned VM via parent @State (VM stan zachowany między scenePhase updates, tworzony raz).
  - `switch model.destination` exhaustive: `.onboarding | .denial | .home | .none` (`.none` jako krótki placeholder `Theme.background` przed pierwszą emisją CurrentValueSubject — w praktyce ≤ 1 frame).
  - `.animation(.easeInOut(duration: 0.35), value: model.destination)` — korzysta z `Equatable` na Destination (B5).
  - `.onChange(of: scenePhase) { _, newPhase in if newPhase == .active { model.refreshStatus() } }` — D-14 core-value protection, **B4 structural co-location zweryfikowana awk'iem** (oba literały `newPhase == .active` i `model.refreshStatus()` w tym samym closure scope, ≤ 4 linie od `.onChange`).
  - `#Preview` robi `DIContainer.shared.reset()` + wszystkie 4 Injection.register — preview niezależny od DeluluDetoxApp.init().
- **RootInjection** — no-op enum (D-01: AppRootVM konsumuje tylko UC Onboardingu, zero własnych zależności). Plik istnieje dla symetrii architektonicznej i przyszłego rozrostu.
- **DeluluDetoxApp.swift** pełny bootstrap (D-20):
  - 4 × `<Feature>Injection.register(in: container)` w `init()` w kolejności Onboarding → Denial → Home → Root.
  - `body` tworzy `AppRootView(model: AppRootViewModel())` — bezargumentowy init.
  - Usunięte: `private let container = DependencyContainer()` + `container.makeAppRootViewModel()`.
  - `import SwiftUI` explicit (W13).
- **DependencyContainer.swift USUNIĘTY** z repo (D-23) — `grep -rc "DependencyContainer" DeluluDetox/Sources/` = 0, `grep -rc "DependencyContainer" DeluluDetoxTests/` = 0. Zero referencji w całym kodzie.
- **onAuthorized callback usunięty** z OnboardingViewModel i DenialViewModel (D-16 pełny). Event flow authorized przechodzi teraz przez: repo `requestAuthorization()` → `statusSubject.send(AuthorizationCenter.shared.authorizationStatus)` → `statusPublisher` → `ObserveUC` → AppRootVM `.sink` → `destination = .home`.
- **ScreenTimeAuthRepository protokół odchudzony** — transitional `var authorizationStatus: AuthorizationStatus { get }` usunięty z protokołu i z `ScreenTimeAuthRepositoryImpl`. `statusPublisher` + `requestAuthorization()` + `refreshStatus()` to jedyny surface.
- **OnboardingViewModelTests zaktualizowany** — usunięte asercje na `onAuthorized` callback (callback nie istnieje). `testGrantAccessSuccess` teraz asertuje `mockRepo.requestAuthorizationCallCount == 1`. `testIsRequestingDuringRequest` zmieniony na `testIsRequestingStartsFalse` (nazwa odzwierciedla faktyczną asercję). 3/3 passed.
- **MockScreenTimeAuthRepository zsynchronizowany** — usunięte `var authorizationStatus` (protokół już nie wymaga) i `set(status:)` helper (plan mock spec ich nie wymagał; `stubbedStatus.didSet` relayuje do `statusSubject` — wystarczy dla Combine tests).
- **AppRootViewModelTests.swift USUNIĘTY** (4 testy) — plik referował DependencyContainer + AppRootViewModel(container:) + vm.screen + vm.checkAuthorization (wszystkie znikły). Wave 5 (01.1-05) odtworzy go w `DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift` w mirror layoucie z DIContainer.reset + register mock pattern + assercjami na Combine emission.

## Task Commits

Każde zadanie committed atomicznie z `--no-verify` (parallel executor mode). 4 tasks; Task 4 to smoke gate bez file changes (brak commita).

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | AppRootViewModel Wzorzec B (Destination enum + Combine) + RootInjection no-op | `109ffac` | Root VM+View+Injection NEW (3), old flat Root VM+View DELETED (2) |
| 2 | Full DIContainer bootstrap + delete DependencyContainer + remove onAuthorized | `e657a5e` | DeluluDetoxApp (MOD), DependencyContainer (DEL), Onboarding/Denial VM (MOD), Repo (MOD) |
| 3 | Delete stale AppRootViewModelTests; sync OnboardingTests + Mock with slimmed protocol | `52b6703` | AppRootVMTests (DEL), OnboardingVMTests (MOD), MockRepo (MOD) |
| 4 | Non-blocking smoke test (I19) — run-on-simulator autonomous gate | (no commit — verification only) | — |

_No TDD tasks in this plan — wszystkie 4 zadania `type="auto"`. Task 4 to non-modifying smoke gate per I19._

## Files Created / Modified / Deleted

### Created (3)

| File | Purpose |
|------|---------|
| `DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` | `@MainActor @Observable @unchecked Sendable` VM; `@CasePathable enum Destination: Equatable { .onboarding, .denial, .home }`; `@ObservationIgnored @LazyInjected` dla ObserveUC + RefreshUC; `@ObservationIgnored cancellables`; bezargumentowy `init()` z Combine `.sink` subskrypcją + `store(in: &cancellables)`; `func refreshStatus()` publiczna; `private static func map(AuthorizationStatus) -> Destination` |
| `DeluluDetox/Sources/Features/Root/View/AppRootView.swift` | `@Bindable var model: AppRootViewModel` + `@Environment(\.scenePhase)` + 3x `@State private var` VMs (Onboarding/Denial/Home); `switch model.destination` w `Group { … }`; `.animation` na destination; `.onChange(of: scenePhase)` → `model.refreshStatus()`; `#Preview` z full DIContainer.reset + 4 Injection.register |
| `DeluluDetox/Sources/Features/Root/Injection/RootInjection.swift` | No-op `enum RootInjection { static func register(in container: DIContainer) }` — komentarz wyjaśniający D-01 (AppRootVM konsumuje tylko UC Onboardingu) |

### Modified (6)

| File | Change |
|------|--------|
| `DeluluDetox/Sources/App/DeluluDetoxApp.swift` | `init()` robi 4× `<Feature>Injection.register(in: DIContainer.shared)`; body tworzy `AppRootViewModel()` bezargumentowy; usunięte `private let container = DependencyContainer()` + `container.makeAppRootViewModel()`; `import SwiftUI` explicit (W13) |
| `DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift` | Usunięte `var onAuthorized: (() -> Void)?` i `onAuthorized?()` wywołanie w `grantAccessTapped()` — zastąpione komentarzem o Combine flow (D-11) |
| `DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift` | Analogicznie — usunięte `onAuthorized` + wywołanie w `retryTapped()` |
| `DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift` | Usunięte `var authorizationStatus: AuthorizationStatus { get }` z protokołu + `var authorizationStatus: AuthorizationStatus { AuthorizationCenter.shared.authorizationStatus }` z Impl — protokół odchudzony do 3 członków (statusPublisher, requestAuthorization, refreshStatus) |
| `DeluluDetoxTests/OnboardingViewModelTests.swift` | Usunięte `var authorizedCalled` + `vm.onAuthorized = { … }`; `testGrantAccessSuccess` asertuje `mockRepo.requestAuthorizationCallCount == 1`; `testIsRequestingDuringRequest` → `testIsRequestingStartsFalse` |
| `DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift` | Usunięte `var authorizationStatus: AuthorizationStatus { stubbedStatus }` (protokół już nie wymaga) i `func set(status:)` helper (plan mock spec ich nie wymagał) |

### Deleted (4)

- `DeluluDetox/Sources/App/DependencyContainer.swift` (D-23 — permanent deletion)
- `DeluluDetox/Sources/Features/Root/AppRootViewModel.swift` (stary flat layout — przeniesiony do ViewModel/)
- `DeluluDetox/Sources/Features/Root/AppRootView.swift` (stary flat layout — przeniesiony do View/)
- `DeluluDetoxTests/AppRootViewModelTests.swift` (niekompatybilny — Wave 5 odtwarza w `DeluluDetoxTests/Features/Root/` mirror layout z DIContainer.reset + register mock pattern + Combine emission asserts)

## Target AppRootViewModel Reference (full source, for Wave 5 test executor)

```swift
import Observation
import Combine
import FamilyControls
import SwiftUINavigation

@MainActor
@Observable
final class AppRootViewModel: @unchecked Sendable {

    @CasePathable
    enum Destination: Equatable {
        case onboarding
        case denial
        case home
    }

    var destination: Destination?

    @ObservationIgnored
    @LazyInjected private var observeStatus: ObserveScreenTimeAuthStatusUseCase

    @ObservationIgnored
    @LazyInjected private var refreshStatusUseCase: RefreshScreenTimeAuthStatusUseCase

    @ObservationIgnored
    private var cancellables: Set<AnyCancellable> = []

    init() {
        observeStatus()
            .sink { [weak self] status in
                self?.destination = Self.map(status)
            }
            .store(in: &cancellables)
    }

    /// Wywoływane przez AppRootView na `scenePhase == .active` (D-14).
    /// Zamyka core-value dziurę: user cofa Screen Time w Settings → repo emituje nowy status → destination się zmienia.
    func refreshStatus() {
        refreshStatusUseCase()
    }

    private static func map(_ status: AuthorizationStatus) -> Destination {
        switch status {
        case .approved:
            return .home
        case .denied:
            return .denial
        default:
            return .onboarding
        }
    }
}
```

**Testability note for Wave 5:**
- SetUp → `DIContainer.shared.reset()` + register `MockScreenTimeAuthRepository` jako `.application` scope + register `ObserveScreenTimeAuthStatusUseCaseImpl` + `RefreshScreenTimeAuthStatusUseCaseImpl` jako `.unique`.
- `testInitialDestinationApproved` → set mockRepo.stubbedStatus = .approved → instantiate `AppRootViewModel()` → assert `vm.destination == .home` (CurrentValueSubject emituje initial value przy sink'u sync).
- `testDestinationUpdatesOnStatusChange` → instantiate VM → mutate `mockRepo.stubbedStatus = .denied` → assert `vm.destination == .denial` (didSet relay through statusSubject).
- `testRefreshStatusCallsRefreshUC` → instantiate VM → `vm.refreshStatus()` → assert `mockRepo.refreshStatusCallCount == 1`.
- `testCancellablesRetainedThroughoutLifetime` → instantiate VM, store weak ref → confirm Combine chain not broken early (deinit test).

## Build & Test Results

### Tooling note

XcodeBuildMCP MCP tools są niedostępne w runtime parallel worktree executora (ten sam constraint co Plan 01/02/03), więc build/test wykonano przez `xcodebuild` CLI. `project.yml` nietknięty — `xcodegen generate` złapał nowe lokalizacje przez wildcard `sources: - path: DeluluDetox/Sources`. Flag `-skipMacroValidation` nadal wymagany (pre-existing SPM macro approval drift dla `CasePathsMacros`, `PerceptionMacros` — udokumentowane w Plan 01 SUMMARY).

`session_show_defaults` nie został wywołany (XcodeBuildMCP niedostępny); zamiast tego użyto dokładnie tego samego destination co Plan 01/02/03 (iPhone 17, iOS 26.2) dla spójności.

### Build

- **Command:** `xcodebuild -project DeluluDetox.xcodeproj -scheme DeluluDetox -destination "platform=iOS Simulator,name=iPhone 17,OS=26.2" -configuration Debug -skipMacroValidation build`
- **Simulator:** iPhone 17, iOS 26.2
- **Wynik:** `** BUILD SUCCEEDED **`
- **Swift 6.2 strict concurrency warnings w nowych/zmodyfikowanych plikach:** zero
- **Extension validation:** 3/3 appex bundles (DeviceActivityMonitor, ShieldAction, ShieldConfiguration) embedded + validated w końcowym buildzie

### Tests

- **Command:** `xcodebuild … test`
- **Wynik:** `** TEST SUCCEEDED **` — **4/4 passed in 0.004 s**
  - `OnboardingViewModelTests` — 3/3 (testGrantAccessSuccess, testGrantAccessFailure, testIsRequestingStartsFalse)
  - `DeluluDetoxTests` — 1/1 (testPlaceholder)
  - `AppRootViewModelTests` — **not present (plik usunięty; Wave 5 odtworzy 4 nowe testy w mirror layoucie)**

Utrata 4 testów AppRootViewModelTests vs Phase 1 jest **zamierzona** — plan Task 3 `<action>` krok 1 explicitly opisuje ten trade-off. Wave 5 plan (01.1-05) ma ich odtworzenie w zakresie (D-27 mirror layout).

### Task 4 Smoke Test (I19)

**Non-blocking autonomous gate** — exit code z sekwencji `simctl install` + `simctl launch` zamiast formalnego human-verify (autonomous plan).

- **Simulator UDID:** `C958163F-49E1-4B46-8A6D-C2056CD25A37` (iPhone 17 iOS 26.2)
- **simctl install exit:** `0`
- **simctl launch exit:** `0` — PID `37454` zwrócony
- **Po 3s spawn:** proces nadal w `launchctl list` (nie crashnął)
- **Logi systemowe (last 30s):** zero fatalError, zero Swift runtime panic, zero `📛 DIContainer: … not registered`. Standardowy lifecycle: FamilyControls authorization notification registration (przez `ScreenTimeAuthRepositoryImpl.init()` → `CurrentValueSubject(AuthorizationCenter.shared.authorizationStatus)`), UIKit scene setup, scene activated (`Deactivation reason removed: 12`)
- **Gate:** **PASS** (exit 0)

**Smoke test (I19): PASS — app uruchomiona na simulatorze po pełnym refaktorze DIContainer; zero fatalError; AppRootViewModel.init() zsubskrybował ObserveUC; scene activated bez błędu.**

### Acceptance Criteria — Task 1

| # | Criterion | Target | Result |
|---|-----------|--------|--------|
| 1 | `grep -c "@CasePathable" AppRootViewModel.swift` | ≥ 1 | **1** |
| 2 | `grep -cE "enum Destination: *Equatable" AppRootViewModel.swift` (B5) | ≥ 1 | **1** |
| 3 | `grep -c "case .*\(.*ViewModel\)" AppRootViewModel.swift` (no VM payload) | 0 | **0** |
| 4 | `grep -c "var destination: Destination?" AppRootViewModel.swift` | ≥ 1 | **1** |
| 5 | `grep -c "import Combine" AppRootViewModel.swift` | ≥ 1 | **1** |
| 6 | `grep -c "import SwiftUINavigation" AppRootViewModel.swift` | ≥ 1 | **1** |
| 7 | `grep -c "@ObservationIgnored" AppRootViewModel.swift` | ≥ 3 | **3** |
| 8 | `grep -c "@LazyInjected" AppRootViewModel.swift` | ≥ 2 | **2** |
| 9 | `grep -c "Set<AnyCancellable>" AppRootViewModel.swift` | ≥ 1 | **1** |
| 10a | B6 co-location cancellables | ≥ 1 | **1** |
| 10b | B6 co-location observeStatus | ≥ 1 | **1** |
| 10c | B6 co-location refreshStatusUseCase | ≥ 1 | **1** |
| 11 | `grep -c "enum Screen\|screenTag\|DependencyContainer" AppRootViewModel.swift` | 0 | **0** |
| 12 | `grep -c "Repository" AppRootViewModel.swift` (VM→UC only) | 0 | **0** |
| 13 | `grep -c "scenePhase" AppRootView.swift` | ≥ 1 | **2** |
| 14 | `grep -c "model.refreshStatus()" AppRootView.swift` | ≥ 1 | **1** |
| 15 | B4 structural awk (scenePhase→refreshStatus co-located in onChange) | exit 0 | **exit 0** |
| 16 | `grep -c "switch model.destination" AppRootView.swift` | ≥ 1 | **1** |
| 17 | `grep -c "enum RootInjection" RootInjection.swift` | ≥ 1 | **1** |

**Task 1: 17/17 criteria met.**

### Acceptance Criteria — Task 2

| # | Criterion | Target | Result |
|---|-----------|--------|--------|
| 1 | `test ! -f DependencyContainer.swift` (D-23) | exit 0 | **exit 0** |
| 2 | `grep -rc "DependencyContainer" DeluluDetox/Sources/` | 0 hits | **0** |
| 3 | `grep -c "Injection.register" DeluluDetoxApp.swift` | == 4 | **4** |
| 4 | `grep -c "AppRootViewModel()" DeluluDetoxApp.swift` | ≥ 1 | **1** |
| 5 | W13: `grep -q "^import SwiftUI$" DeluluDetoxApp.swift` | exit 0 | **exit 0** |
| 6 | `grep -rc "onAuthorized" DeluluDetox/Sources/Features/` | 0 hits | **0** |
| 7 | `grep -c "var authorizationStatus" ScreenTimeAuthRepository.swift` | 0 | **0** |
| 8 | `grep -rc "checkAuthorization" DeluluDetox/Sources/` | 0 | **0** |
| 9 | XcodeBuild `build` | exit 0 | **exit 0** (po Task 3 usunięciu stale test; spec dopuszcza red build pomiędzy Task 2 a Task 3) |

**Task 2: 9/9 criteria met.**

### Acceptance Criteria — Task 3

| # | Criterion | Target | Result |
|---|-----------|--------|--------|
| 1 | `test ! -f AppRootViewModelTests.swift` | exit 0 | **exit 0** |
| 2 | `grep -rc "DependencyContainer" DeluluDetoxTests/` | 0 hits | **0** |
| 3 | `grep -c "onAuthorized" DeluluDetoxTests/` (recursive) | 0 hits | **0** |
| 4 | `grep -c "var authorizationStatus" MockScreenTimeAuthRepository.swift` | 0 | **0** |
| 5 | `grep -c "statusPublisher" MockScreenTimeAuthRepository.swift` | ≥ 1 | **1** |
| 6 | XcodeBuild `build` | exit 0 | **exit 0** |
| 7 | XcodeBuild `test` (3 Onboarding + 1 placeholder) | 4/4 passed | **4/4 passed** |
| 8 | VM→Repo imports (4 VM files) | 0 | **0** |
| 9 | DependencyContainer in Sources | 0 | **0** |
| 10 | onAuthorized in Features | 0 | **0** |
| 11 | enum Screen/screenTag in Root | 0 | **0** |
| 12 | import SwiftUINavigation in Root | ≥ 1 | **2** (VM + View) |
| 13 | @CasePathable in AppRootViewModel | ≥ 1 | **1** |

**Task 3: 13/13 criteria met.**

### Acceptance Criteria — Task 4 (I19)

| # | Criterion | Target | Result |
|---|-----------|--------|--------|
| 1 | `simctl install` exit code | 0 | **0** |
| 2 | `simctl launch` exit code | 0 | **0** |
| 3 | App process alive 3s after launch (no immediate fatalError) | alive | **alive** |
| 4 | Zero `📛 DIContainer` strings in logs | 0 | **0** |
| 5 | Scene activated (`Deactivation reason removed`) | present | **present** |

**Task 4: 5/5 criteria met. Smoke test (I19): PASS.**

### Total Acceptance Criteria: 44/44 met.

## Decisions → File/Line Traceability (D-01..D-31)

| Decision | Status | Realization |
|----------|--------|-------------|
| **D-01** (AppRootVM zależy WYŁĄCZNIE od UC) | **DONE** | `AppRootViewModel.swift` — grep "Repository" == 0; 2× @LazyInjected UC (ObserveUC + RefreshUC); RootInjection.swift no-op |
| **D-02** (@CasePathable Destination bez VM payload) | **DONE** | `AppRootViewModel.swift:12-17` — `@CasePathable enum Destination: Equatable { case onboarding, case denial, case home }`; grep `case .*\(.*ViewModel\)` == 0 |
| **D-03** (Child-owned VM via @State) | **DONE** (pragmatyczny wariant) | `AppRootView.swift:11-13` — 3× `@State private var` w parent View; każdy VM ma osobny `@State` scope. Alternatywny wariant (child View ma @State) nie zastosowany — trade-off opisany w plan §Task 1 step 3 |
| **D-04** (Screen enum + screenTag usunięte) | **DONE** | grep -rc `enum Screen\|screenTag` DeluluDetox/Sources/ → 0 |
| **D-05** (swift-navigation aktywacja) | **DONE** | `import SwiftUINavigation` w AppRootViewModel.swift i AppRootView.swift (pierwsze realne użycie w Phase 01.1; Package.swift ma dep od Phase 1) |
| **D-07** (CurrentValueSubject + statusPublisher) | PENDING realna konsumpcja | Uzyty w Wave 2, AppRootVM teraz faktycznie subskrybuje — ale to realizacja D-11, D-07 był o Repo side |
| **D-08** (statusPublisher: AnyPublisher) | PENDING konsumpcja | AppRootVM konsumuje przez ObserveUC, nie bezpośrednio (architektural rule: VM→UC→Repo) |
| **D-09** (ObserveUC) | **DONE** (konsumpcja) | AppRootVM używa `@LazyInjected private var observeStatus` + `observeStatus()` w init |
| **D-10** (@ObservationIgnored poprzedza @LazyInjected; cancellables @ObservationIgnored) | **DONE** (pełne) | grep `@ObservationIgnored` w AppRootVM = 3 (2 UC + cancellables); B6 co-location 3/3 pass |
| **D-11** (Combine subscription w init; onAuthorized zniesione) | **DONE** (pełne) | `AppRootViewModel.swift:30-35` — `.sink { [weak self] in self?.destination = Self.map($0) }.store(in: &cancellables)`; `grep -rc "onAuthorized" Features/` = 0 |
| **D-12** (wycofanie one-shot GetUC) | DONE w Wave 2 | Nie wraca w Wave 4 |
| **D-13** (refreshStatus core-value defense) | **DONE** (konsumpcja) | `AppRootViewModel.refreshStatus()` → `refreshStatusUseCase()` → repo.refreshStatus() → statusSubject.send(current auth status) |
| **D-14** (scenePhase → refreshStatus hook) | **DONE** | `AppRootView.swift:32-36` — `.onChange(of: scenePhase) { _, newPhase in if newPhase == .active { model.refreshStatus() } }`; B4 structural awk pass |
| **D-15** (HomeVM no-op w 01.1) | DONE w Wave 3 | Nie zmieniony w Wave 4 |
| **D-16** (init() bezargumentowy; onAuthorized usunięte pełnie) | **DONE** (pełne) | Wszystkie 4 VM mają `init() {}`; grep -rc "onAuthorized" = 0 |
| **D-17** (Onboarding feature-owner; Denial/Home konsumują UC) | DONE w Wave 2-3 | Zachowane w Wave 4 (DenialInjection pozostaje no-op) |
| **D-20** (App.init() rejestruje 4 features) | **DONE** (pełne) | `DeluluDetoxApp.swift:5-12` — 4× `<Feature>Injection.register(in: container)`; grep count = 4 |
| **D-21** (fatalError na brakującą rejestrację) | N/A | DIContainer z Wave 1 — nie zmienia się |
| **D-22** (Scopes: Repo .application, UC .unique) | DONE w Wave 2 | OnboardingInjection zachowany |
| **D-23** (DependencyContainer deletion) | **DONE** | `test ! -f DeluluDetox/Sources/App/DependencyContainer.swift` → exit 0; `grep -rc "DependencyContainer" DeluluDetox/Sources/ DeluluDetoxTests/` → 0 |
| **D-24** (Wzorzec B — pragmatyczny child-owned @State w parent) | **DONE** | `AppRootView.swift:11-13` — 3× `@State private var`; `switch model.destination` w `body` |
| **D-25** (…) | N/A dla tej fazy | — |
| **D-26** (CLAUDE.md review no-op) | N/A dla tej fazy | CLAUDE.md nietknięty |
| **D-27** (mirror test layout) | PENDING-WAVE-5 | AppRootViewModelTests usunięty w Task 3; Wave 5 tworzy `DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift` |
| **D-28** (…) | N/A dla tej fazy | — |
| **D-29** (DIContainer.reset w test setUp) | DONE w Wave 3 | OnboardingViewModelTests nadal używa reset+register |
| **D-30** (extension baseline regression check — part b) | PENDING-WAVE-6 | Wave 6 (01.1-07) diffuje evidence-to-evidence vs BASELINE_EXTENSION_IMPORTS.txt |
| **D-31** (extension import escape gate) | **NOT TRIGGERED** (happy path) | `grep -rn "import DeluluDetox\|@testable import DeluluDetox" Extensions/` → 0 (zweryfikowane) |

## Architectural Compliance (hard rules from CLAUDE.md)

| Rule | Status | Evidence |
|------|--------|----------|
| Repo ↛ Repo | **PASS** | 1 repo w projekcie (ScreenTimeAuthRepositoryImpl); zero cross-repo references |
| UseCase → UseCase/Repo | **PASS** | Request/Observe/Refresh UC wszystkie init-inject `ScreenTimeAuthRepository` |
| ViewModel → tylko UseCase | **PASS** | grep VM files dla `import.*Repository\|: ScreenTimeAuthRepository\b` minus UseCase → 0 hits (all 4 VM files) |
| View → tylko ViewModel | **PASS** | grep View files dla `import FamilyControls\|import Combine\|UseCase` → 0 (Views nie znają UC ani Combine) |
| ViewModels use @Observable, nie importują SwiftUI | **PASS** | AppRootViewModel/OnboardingVM/DenialVM/HomeVM wszystkie `@Observable`, import: `Observation`, `Combine`, `FamilyControls`, `SwiftUINavigation` (nie SwiftUI) |
| Feature-first layout | **PASS** | Features/Root/{ViewModel,View,Injection}/ + Onboarding/{ViewModel,View,Injection,Repository,UseCase}/ + Denial/{ViewModel,View,Injection}/ + Home/{ViewModel,View,Injection}/ |
| DIContainer + scope'y .application/.unique + @LazyInjected dla VM | **PASS** | 4 VMs × @LazyInjected; OnboardingInjection rejestruje .application (Repo) + 3× .unique (UC) |
| Feature własne Injection | **PASS** | OnboardingInjection + DenialInjection + HomeInjection + RootInjection = 4 plików w Features/<F>/Injection/ |
| Central App-level registration (antywzorzec) unikane | **PASS** | DeluluDetoxApp.init() tylko wywołuje `<Feature>Injection.register` — żadnej bezpośredniej rejestracji |

## Deviations from Plan

**1. [Rule 1 - Bug prevention] Plan spec `Color(Theme.background)` zamieniony na `Theme.background` direct usage**

- **Found during:** Task 1 (AppRootView creation)
- **Issue:** Plan §Task 1 step 3 zawierał `Color(Theme.background)` dla `.none` case placeholder. `Theme.background` jest już `Color` (z DesignSystem/Theme.swift — `static var background: Color { Color(.systemBackground) }`), więc zawinięcie `Color(…)` jest błędem kompilacji (Color init nie przyjmuje Color).
- **Fix:** Użyto `Theme.background` bezpośrednio — identyczne semantycznie do `Color(Theme.background)` gdyby Theme.background był ciągiem znaków, ale nie jest. Zgodne ze sposobem użycia w Onboarding/Denial/Home View (`.background(Theme.background)`).
- **Files modified:** `DeluluDetox/Sources/Features/Root/View/AppRootView.swift:25`
- **Commit:** `109ffac`
- **Plan alignment:** Plan semantyka zachowana (placeholder tła przed pierwszą emisją); tylko literał Swift poprawiony. Nie zmienia logiki.

**2. [Rule 2 - Missing critical functionality] MockScreenTimeAuthRepository zsynchronizowany (plan spec §Task 3 krok 3)**

- **Found during:** Task 3 build verification
- **Issue:** Po usunięciu `var authorizationStatus` z protokołu `ScreenTimeAuthRepository` (Task 2), `MockScreenTimeAuthRepository` nadal miał `var authorizationStatus: AuthorizationStatus { stubbedStatus }` — protocol conformance by działał (extra member), ale plan §Task 3 `<action>` krok 3 explicitly mówił żeby usunąć ten member.
- **Fix:** Usunięte `var authorizationStatus` i `func set(status:)` helper (plan mock target też go nie miał). `stubbedStatus.didSet { statusSubject.send(stubbedStatus) }` relay zachowany.
- **Files modified:** `DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift`
- **Commit:** `52b6703`
- **Plan alignment:** Zgodne z planem §Task 3 step 3. Nie deviation — to dokładna realizacja planu.

Environment-level notes (nie plan deviations, tak samo jak Plan 01/02/03):
- `-skipMacroValidation` flag na `xcodebuild` (pre-existing SPM macro approval drift)
- `xcodebuild` CLI zamiast XcodeBuildMCP tools (MCP toolbox nieobecny w runtime parallel executora)
- Task 4 smoke test użył `xcrun simctl install/launch` jako analog XcodeBuildMCP `build_run_sim` (ta sama semantyka: install app na simulatorze, launch, sprawdź exit code 0)

---

**Total deviations:** 1 auto-fixed (Rule 1 — `Color(Theme.background)` kompilacyjny błąd literału planu).
**Impact on plan:** Zerowy — semantyka placeholdera tła zachowana; naprawa była wymuszona przez typ systemu Swift (Color init nie akceptuje Color).

## Threat Model Check

Wszystkie entry z threat register planu spełnione:

- **T-01.1-12 (DoS: AppRootVM.init() z pustego DIContainer):** **mitigated** — Smoke test (I19) PASS: `simctl launch` exit 0 + proces alive 3s po starcie + zero `📛 DIContainer` fatalError w logach. Dowód że `DeluluDetoxApp.init()` wykonał 4× `Injection.register` PRZED `body: some Scene` → `AppRootView(model: AppRootViewModel())` → `@LazyInjected` rezolucja znalazła wszystkie factory. SwiftUI gwarancja `App.init` kończy się przed sceną potwierdzona empirycznie.
- **T-01.1-13 (Info disclosure: var destination: Destination?):** **accepted** — Destination enum ma tylko 3 rozłączne cases bez payloadu; zero PII/sekretów. Write access wymagany dla swift-navigation case-path bindings (PATTERNS §3.1 ostrzega: NIE `private(set)`). Jedyne źródło mutacji w kodzie produkcyjnym: Combine sink w init — weryfikowane przez Wave 5 testy.
- **T-01.1-14 (Tampering: .sink closure capture):** **mitigated** — `[weak self]` w closure (`AppRootViewModel.swift:32`); `cancellables: Set<AnyCancellable>` przechowywany jako `@ObservationIgnored private var` — ARC zwolni przy deinit VM. Zero retain cycle.
- **T-01.1-15 (EoP: RefreshUC):** **mitigated** — `refreshStatusUseCase` jest `private` w AppRootVM; jedyne wywołanie przez `func refreshStatus()` (publiczna method) w `AppRootView.onChange(of: scenePhase)`. Grep `refreshStatusUseCase()` w całym DeluluDetox/Sources/ = 1 (w `refreshStatus()` wrapper). Enkapsulacja zachowana.
- **T-01.1-16 (Core value violation — scenePhase hole):** **mitigated (D-13 core-value lens)** — `AppRootView.swift:32-36` `.onChange(of: scenePhase) { if .active { model.refreshStatus() } }`. B4 structural awk PASS (oba literały `newPhase == .active` i `model.refreshStatus()` w tym samym closure scope). Real-device verification przesunięta do Wave 6 human-verify — symulator nie ma prawdziwego Screen Time toggle w Settings, ale funkcja jest wire'owana i Wave 6 ją przetestuje.

**TCC prompt invariant:** niezmieniony. Ten sam `AuthorizationCenter.shared.requestAuthorization(for: .individual)` w `ScreenTimeAuthRepositoryImpl.requestAuthorization()`.

**App Group sandbox invariant:** niezmieniony. Żaden kod w tym planie nie czyta/pisze z `group.com.kksw.DeluluDetox`.

**Extensions baseline (D-30):** Ten plan NIE dotyka `Extensions/**`. `grep -rn "import DeluluDetox\|@testable import DeluluDetox" Extensions/` → 0. Regression check przesunięty do Wave 6 (01.1-07).

## Threat Flags

None — plan tylko refaktoruje istniejący Swift source + usuwa stare pliki. Zero nowych:
- Network endpoints (no-backend invariant zachowany)
- Auth paths (TCC prompt niezmieniony)
- File access patterns (żadnych nowych FileManager/App Group calls)
- Trust boundary schema changes (AuthorizationStatus enum niezmieniony)

`DIContainer.shared` jako runtime resolver jest in-process; nie przekracza process boundary.

## Known Stubs

None.

AppRootViewModel ma pełną implementację: Combine subscription działa (smoke test ją uruchomił), `map(AuthorizationStatus)` jest kompletne (switch po 3 cases z default → .onboarding dla .notDetermined i innych), `refreshStatus()` wywołuje realny UC → realny Repo. Zero TODO/FIXME/placeholder.

Opcjonalny `destination = nil` case w AppRootView (Theme.background placeholder) to **zamierzone defensywne zachowanie**, nie stub — CurrentValueSubject emituje natychmiast przy sink'u, więc w praktyce ten case jest widoczny przez ≤ 1 frame; zachowany dla safety w hipotetycznym edge case (jeśli kiedyś Repository zmieni się na `PassthroughSubject`).

## User Setup Required

None.

## Next Phase Readiness

- **Wave 5 (01.1-05 — testy migration + docs) unblocked:**
  - Target AppRootViewModel reference embedded w tym SUMMARY (pełny source) — Wave 5 test executor ma spec do którego pisze testy.
  - Sanity check dla Wave 5: pliki testowe do utworzenia w mirror layoucie:
    - `DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift` (NEW — 4 nowe testy zgodnie z plan §Task 3 step 1 uzasadnieniem + my testability note above)
    - `DeluluDetoxTests/Features/Onboarding/ViewModel/OnboardingViewModelTests.swift` (MOVE + EXPAND — może dodać `testAuthorizedEventTriggersStatusPublisher` przez Combine observation)
    - `DeluluDetoxTests/Features/Denial/ViewModel/DenialViewModelTests.swift` (NEW — analogiczny do Onboarding z retryTapped)
    - `DeluluDetoxTests/Features/Home/ViewModel/HomeViewModelTests.swift` (NEW — minimal, HomeVM ma tylko `init()` i `chooseAppsTapped() no-op` per D-15)
  - Pliki źródłowe do mock'owania: `ScreenTimeAuthRepository` (już istnieje MockScreenTimeAuthRepository). Nowe mocki UC opcjonalne (init-injection → mock Repo wystarczy).
  - Docs to update: `.claude/guides/navigation/GUIDE.md` (dodaj Wzorzec B section — przykład z AppRootViewModel), `.claude/guides/dependency-injection/GUIDE.md` (bootstrap App-level pattern — example z DeluluDetoxApp.init()), `.claude/guides/feature-structure/GUIDE.md` (confirmation że Simple variant zastosowany w całym 01.1).
- **Wave 6 (01.1-07 — cleanup + extension regression + human-verify) unblocked:**
  - `to_integrate/` deletion zaplanowane (nietknięte w 01.1-04)
  - Extension baseline regression check (D-30 part b): `grep -rn "import DeluluDetox\|@testable import DeluluDetox" Extensions/*.swift` → napisać nowy BASELINE_EXTENSION_IMPORTS_POST.txt z identycznym formatem, diff evidence-to-evidence vs pre-refactor baseline (nadal empty expected = happy path)
  - Formal human-verify na real device: Phase 1 flow (cold-launch → Onboarding → grant → Home; denied → Denial → retry; scenePhase .active → refresh efekt real w Settings)
- **Core value protected** (D-13/D-14 structural check passed; Wave 6 empirically verifies)
- **No blockers or concerns** dla subsequent waves.

## Self-Check

Verified post-write:

| Claim | Check | Result |
|-------|-------|--------|
| `DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` exists | `test -f` | **FOUND** |
| `DeluluDetox/Sources/Features/Root/View/AppRootView.swift` exists | `test -f` | **FOUND** |
| `DeluluDetox/Sources/Features/Root/Injection/RootInjection.swift` exists | `test -f` | **FOUND** |
| Old flat `DeluluDetox/Sources/Features/Root/AppRootViewModel.swift` gone | `test ! -f` | **GONE** |
| Old flat `DeluluDetox/Sources/Features/Root/AppRootView.swift` gone | `test ! -f` | **GONE** |
| `DeluluDetox/Sources/App/DependencyContainer.swift` gone (D-23) | `test ! -f` | **GONE** |
| `DeluluDetoxTests/AppRootViewModelTests.swift` gone (Wave 5 odtworzy) | `test ! -f` | **GONE** |
| Task 1 commit `109ffac` exists | `git log` | **FOUND** |
| Task 2 commit `e657a5e` exists | `git log` | **FOUND** |
| Task 3 commit `52b6703` exists | `git log` | **FOUND** |
| `onAuthorized` zero references in Features/ | `grep -rc` | **0** |
| `DependencyContainer` zero references in Sources/ | `grep -rc` | **0** |
| `DependencyContainer` zero references in Tests/ | `grep -rc` | **0** |
| `checkAuthorization` zero references in Sources/ | `grep -rc` | **0** |
| `enum Screen` zero references in Root/ | `grep -rc` | **0** |
| DeluluDetoxApp bootstraps 4 Injections | `grep -c Injection.register` | **4** |
| Smoke test (I19) exit 0 | `simctl launch` exit | **0** |

## Self-Check: PASSED

---

**Refaktor kodu zakończony + smoke test autonomiczny PASS. Pozostałe w Phase 01.1: Wave 5 (01.1-05 — testy migration w mirror layoucie + docs updates w `.claude/guides/{navigation,dependency-injection,feature-structure}/GUIDE.md`), Wave 6 (01.1-06 — weryfikatory) + (01.1-07 — cleanup `to_integrate/` + extension regression check + human-verify na real device).**

---
*Phase: 01.1-architecture-foundation*
*Completed: 2026-04-19*
