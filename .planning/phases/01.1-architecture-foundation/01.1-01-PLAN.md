---
phase: 01.1-architecture-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DeluluDetox/Sources/Core/DependencyInjection/DIContainer.swift
  - DeluluDetox/Sources/Core/DependencyInjection/PropertyWrappers/Injected.swift
  - DeluluDetox/Sources/Core/DependencyInjection/PropertyWrappers/LazyInjected.swift
  - .planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt
autonomous: true
requirements: []  # Phase 01.1 is architectural — no user-facing REQ-IDs. Covers D-18, D-19, D-30 (part a).
user_setup: []

must_haves:
  truths:
    - "Pliki DI (DIContainer + 2 property wrappers) żyją w `DeluluDetox/Sources/Core/DependencyInjection/` i kompilują się w ramach targetu DeluluDetox pod Swift 6.2 strict concurrency."
    - "Extension baseline (grep + 3 buildy) zapisany przed jakąkolwiek zmianą feature-first — istnieje punkt odniesienia do regression checka w Wave 6."
    - "App nadal buduje się i testy nadal przechodzą — ten plan nie zmienia żadnej funkcjonalności."
  artifacts:
    - path: "DeluluDetox/Sources/Core/DependencyInjection/DIContainer.swift"
      provides: "Singleton DI container z rejestracją/resolve/reset, wymuszony MainActor przez MainActor.assumeIsolated, fail-fast fatalError"
      contains: "public final class DIContainer"
    - path: "DeluluDetox/Sources/Core/DependencyInjection/PropertyWrappers/Injected.swift"
      provides: "Eager property wrapper nad DIContainer.shared"
      contains: "public struct Injected"
    - path: "DeluluDetox/Sources/Core/DependencyInjection/PropertyWrappers/LazyInjected.swift"
      provides: "Lazy property wrapper z cache + NSLock nad DIContainer.shared"
      contains: "public final class LazyInjected"
    - path: ".planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt"
      provides: "Snapshot importów z main targetu do 3 extension targetów przed refaktorem (D-30 part a)"
  key_links:
    - from: "DIContainer factory registration"
      to: "`@MainActor` (ViewModels in Wave 3+)"
      via: "MainActor.assumeIsolated wrapping w DIContainer.register(_:scope:factory:)"
      pattern: "MainActor\\.assumeIsolated"
    - from: "LazyInjected.wrappedValue"
      to: "DIContainer.shared"
      via: "resolve(Value.self) przy pierwszym dostępie"
      pattern: "DIContainer\\.shared\\.resolve"
---

<objective>
Przenieść gotowy kod DI z `to_integrate/DependencyInjection/` do docelowej lokalizacji `DeluluDetox/Sources/Core/DependencyInjection/` (kopia 1:1 — zero zmian treści) i zweryfikować, że kompiluje się pod Swift 6.2 strict concurrency w tym samym targecie co istniejący kod Phase 1. Dodatkowo: zapisać baseline importów między extension targetami a main targetem (D-30 part a), żeby Wave 6 mógł wykryć regresję.

Purpose: Przygotować fundament DI bez dotykania żadnego feature'a. App nadal używa `DependencyContainer` — w tym planie tylko dokładamy równoległe pliki. Kolejne plany (Wave 2+) dopiero zaczną rejestrować i konsumować przez `DIContainer`. Rozłącza ryzyko "DI integruje się" od "feature'y się migrują".

Output:
- `DeluluDetox/Sources/Core/DependencyInjection/DIContainer.swift`
- `DeluluDetox/Sources/Core/DependencyInjection/PropertyWrappers/Injected.swift`
- `DeluluDetox/Sources/Core/DependencyInjection/PropertyWrappers/LazyInjected.swift`
- `.planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt`
</objective>

<execution_context>
@/Users/swojtyna/Documents/code/swojtyna/KKSW---first-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/swojtyna/Documents/code/swojtyna/KKSW---first-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md
@.planning/phases/01.1-architecture-foundation/01.1-PATTERNS.md
@CLAUDE.md
@.claude/guides/dependency-injection/GUIDE.md
@.claude/guides/xcodegen/GUIDE.md
@.claude/guides/xcodebuild-mcp/GUIDE.md

<interfaces>
<!-- Źródłowe pliki do skopiowania 1:1 (po przeniesieniu usuwamy ze starej lokalizacji) -->

From to_integrate/DependencyInjection/DIContainer.swift:
```swift
import Foundation

public final class DIContainer: @unchecked Sendable {
    public static let shared = DIContainer()

    public enum ObjectScope: Sendable {
        case unique
        case application
    }

    public func register<T: Sendable>(
        _ type: T.Type,
        scope: ObjectScope,
        factory: @escaping @MainActor (DIContainer) -> T
    )

    public func resolve<T>(_ type: T.Type = T.self) -> T  // fatalError jeśli brak rejestracji
    public func reset()
}
```

From to_integrate/DependencyInjection/PropertyWrappers/Injected.swift:
```swift
@propertyWrapper
public struct Injected<Value> {
    public let wrappedValue: Value
    public init()  // resolve immediately from DIContainer.shared
}
```

From to_integrate/DependencyInjection/PropertyWrappers/LazyInjected.swift:
```swift
@propertyWrapper
public final class LazyInjected<Value>: @unchecked Sendable {
    public var wrappedValue: Value { /* lazy resolve + cache */ }
    public init()
}
```

<!-- XcodeGen: project.yml target `DeluluDetox` ma `sources: - path: DeluluDetox/Sources` (wildcard katalogowy) — nowe pliki pod `Sources/Core/` są łapane automatycznie. Nie modyfikujemy project.yml. -->
<!-- Pliki w `to_integrate/` NIE są w project.yml sources, więc istnieją w repo ale NIE są kompilowane — bezpiecznie zostawić do Wave 6 cleanup. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Przenieść DI core do `DeluluDetox/Sources/Core/DependencyInjection/` i zweryfikować build</name>

  <read_first>
    - `to_integrate/DependencyInjection/DIContainer.swift` (pełen plik — kopia 1:1)
    - `to_integrate/DependencyInjection/PropertyWrappers/Injected.swift` (pełen plik — kopia 1:1)
    - `to_integrate/DependencyInjection/PropertyWrappers/LazyInjected.swift` (pełen plik — kopia 1:1)
    - `project.yml` linie 23-58 (target DeluluDetox, `sources: - path: DeluluDetox/Sources`)
    - `.claude/guides/dependency-injection/GUIDE.md` (konwencja lokalizacji — `Core/DependencyInjection/`)
    - `.claude/guides/xcodebuild-mcp/GUIDE.md` (jak budować przez XcodeBuildMCP)
  </read_first>

  <files>
    DeluluDetox/Sources/Core/DependencyInjection/DIContainer.swift (NEW)
    DeluluDetox/Sources/Core/DependencyInjection/PropertyWrappers/Injected.swift (NEW)
    DeluluDetox/Sources/Core/DependencyInjection/PropertyWrappers/LazyInjected.swift (NEW)
  </files>

  <action>
    1. Utwórz katalogi:
       - `mkdir -p DeluluDetox/Sources/Core/DependencyInjection/PropertyWrappers`
    2. Skopiuj 3 pliki DI **1:1 treścią** (bez żadnych zmian — to jest wymaganie D-18, analog "exact" w PATTERNS.md §1):
       - `to_integrate/DependencyInjection/DIContainer.swift` → `DeluluDetox/Sources/Core/DependencyInjection/DIContainer.swift`
       - `to_integrate/DependencyInjection/PropertyWrappers/Injected.swift` → `DeluluDetox/Sources/Core/DependencyInjection/PropertyWrappers/Injected.swift`
       - `to_integrate/DependencyInjection/PropertyWrappers/LazyInjected.swift` → `DeluluDetox/Sources/Core/DependencyInjection/PropertyWrappers/LazyInjected.swift`
    3. **NIE usuwaj** plików źródłowych z `to_integrate/` — cały katalog `to_integrate/` znika dopiero w Wave 6 cleanup (D-23).
    4. **NIE dotykaj** `project.yml` — `sources: - path: DeluluDetox/Sources` jest wildcardem katalogowym, nowy `Core/` zostanie złapany automatycznie. (project.yml linie 27-28 w obecnym stanie repo.)
    5. Uruchom `xcodegen generate` — zweryfikuj że regeneruje `.xcodeproj` bez błędów.
    6. Zweryfikuj build przez XcodeBuildMCP:
       - Wywołaj `session_show_defaults` żeby potwierdzić project/scheme/simulator.
       - Wywołaj `build_sim` dla schematu `DeluluDetox` na default simulatorze.
       - Build MUSI przejść zielony. Jeśli Swift 6.2 strict concurrency zgłosi warningi w `DIContainer.swift` (np. `MainActor.assumeIsolated`), zanotuj je w SUMMARY ale NIE modyfikuj treści DI — plik jest już z założenia przygotowany pod Swift 6 (`@unchecked Sendable` na klasie + manualny `NSRecursiveLock` + `MainActor.assumeIsolated` w factory). Jeśli pojawi się **błąd** kompilacji (nie warning), zatrzymaj się i zgłoś — treść z `to_integrate/` miała się budować pod Swift 6.2 1:1.
    7. **NIE rejestruj niczego** w `DIContainer.shared` w tym planie — rejestracja zaczyna się w Wave 2 (`OnboardingInjection`).
    8. Uruchom istniejące testy przez XcodeBuildMCP `test_sim` dla schematu `DeluluDetox` — wszystkie (AppRootViewModelTests + OnboardingViewModelTests) MUSZĄ przejść zielone. Ten plan nie zmienia zachowania, tylko dokłada równoległe pliki.
  </action>

  <verify>
    <automated>
      # Pliki istnieją
      test -f DeluluDetox/Sources/Core/DependencyInjection/DIContainer.swift
      test -f DeluluDetox/Sources/Core/DependencyInjection/PropertyWrappers/Injected.swift
      test -f DeluluDetox/Sources/Core/DependencyInjection/PropertyWrappers/LazyInjected.swift
      # to_integrate/ zachowany (usunięcie dopiero Wave 6)
      test -d to_integrate
      # Kluczowe sygnatury są w nowym miejscu
      grep -q "public final class DIContainer" DeluluDetox/Sources/Core/DependencyInjection/DIContainer.swift
      grep -q "MainActor.assumeIsolated" DeluluDetox/Sources/Core/DependencyInjection/DIContainer.swift
      grep -q "public struct Injected" DeluluDetox/Sources/Core/DependencyInjection/PropertyWrappers/Injected.swift
      grep -q "public final class LazyInjected" DeluluDetox/Sources/Core/DependencyInjection/PropertyWrappers/LazyInjected.swift
      # Build + testy via XcodeBuildMCP
      # (wykonane w kroku 6-8 akcji; raport zaliczenia w SUMMARY)
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "public final class DIContainer" DeluluDetox/Sources/Core/DependencyInjection/DIContainer.swift` → **≥ 1**
    - `grep -c "MainActor.assumeIsolated" DeluluDetox/Sources/Core/DependencyInjection/DIContainer.swift` → **≥ 1**
    - `grep -c "fatalError" DeluluDetox/Sources/Core/DependencyInjection/DIContainer.swift` → **≥ 1** (fail-fast D-21)
    - `grep -c "func reset" DeluluDetox/Sources/Core/DependencyInjection/DIContainer.swift` → **≥ 1** (test support D-29)
    - `diff to_integrate/DependencyInjection/DIContainer.swift DeluluDetox/Sources/Core/DependencyInjection/DIContainer.swift` → **pusty** (kopia 1:1)
    - `diff to_integrate/DependencyInjection/PropertyWrappers/Injected.swift DeluluDetox/Sources/Core/DependencyInjection/PropertyWrappers/Injected.swift` → **pusty**
    - `diff to_integrate/DependencyInjection/PropertyWrappers/LazyInjected.swift DeluluDetox/Sources/Core/DependencyInjection/PropertyWrappers/LazyInjected.swift` → **pusty**
    - XcodeBuildMCP `build_sim` scheme=DeluluDetox → **exit 0, zero compilation errors**
    - XcodeBuildMCP `test_sim` scheme=DeluluDetox → **wszystkie testy zielone** (AppRootViewModelTests, OnboardingViewModelTests, DeluluDetoxTests placeholder)
  </acceptance_criteria>

  <done>
    Trzy pliki DI w `DeluluDetox/Sources/Core/DependencyInjection/` są bit-for-bit identyczne z `to_integrate/`. Projekt buduje się zielony pod Swift 6.2. Istniejące testy Phase 1 nadal zielone. `to_integrate/` pozostaje nietknięty.
  </done>
</task>

<task type="auto">
  <name>Task 2: Zapisać extension baseline (D-30 part a)</name>

  <read_first>
    - `.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md` sekcja Extension Targets (D-30, D-31)
    - `.planning/phases/01.1-architecture-foundation/01.1-PATTERNS.md` §4.7 (Extensions baseline/regression)
    - `Extensions/DeviceActivityMonitorExtension/` — cały katalog (lista plików)
    - `Extensions/ShieldActionExtension/` — cały katalog
    - `Extensions/ShieldConfigurationExtension/` — cały katalog
    - `project.yml` linie 60-127 (3 extension targety — `sources: - path: Extensions/<Ext>`; **nie dotykamy** tych `sources:` w 01.1)
    - `.claude/guides/xcodebuild-mcp/GUIDE.md` (build per target przez XcodeBuildMCP)
  </read_first>

  <files>
    .planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt (NEW)
  </files>

  <action>
    1. **Baseline capture — single canonical format (no duality).** Utwórz baseline plik z fixed header + evidence block, gdzie evidence block MOŻE BYĆ pusty (oznacza "no hits" — expected happy-path). **Ten sam format będzie użyty w Wave 6 regression capture**, więc diff będzie porównywał evidence-to-evidence (nie cały plik).

       Krok 1a: Wykonaj grep z zawężonym wzorcem (Swift nie robi path imports — usuwamy `DeluluDetox/Sources` noise z Wave 1):
       ```bash
       EVIDENCE=$(grep -rnE "(^|[[:space:]])@?testable? *import[[:space:]]+DeluluDetox([[:space:]]|$)" Extensions/ --include="*.swift" 2>/dev/null || true)
       ```

       Krok 1b: Zapisz do pliku używając template (evidence może być pusty):
       ```bash
       ISO_DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
       cat > .planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt <<EOF
       # DeluluDetox extension → main-target import baseline
       # Captured: ${ISO_DATE}
       # Pattern: import DeluluDetox\b OR @testable import DeluluDetox\b in Extensions/**/*.swift
       # Empty evidence block means "no hits" (expected happy-path).
       ---EVIDENCE---
       ${EVIDENCE}
       ---END-EVIDENCE---
       EOF
       ```

       Uwaga: jeśli `EVIDENCE` jest pusty, evidence block ma po prostu pustą linię między markerami — to jest **poprawny happy-path stan**, NIE specjalny case. Nie dodawaj żadnej dodatkowej linii komentarza wewnątrz evidence block (żeby Wave 6 diff porównywał evidence-to-evidence symmetrycznie).

    2. Zbuduj KAŻDY z 3 extension targetów osobno przez XcodeBuildMCP i zapisz wynik w SUMMARY:
       - `build_sim` scheme=DeluluDetox (zbuduje też embedded extensions — to wystarczający signal; alternatywnie jeśli XcodeBuildMCP wspiera per-target build, zbuduj każdy z osobna)
       - Każdy z 3 extension targetów MUSI zbudować się zielony.
       - W SUMMARY zapisz: "DeviceActivityMonitorExtension: PASS", "ShieldActionExtension: PASS", "ShieldConfigurationExtension: PASS" wraz z datą/timestamp.

    3. **NIE modyfikuj** żadnego pliku w `Extensions/**`. Plan 01.1 eksplicytnie nie dotyka extensions (D-30). Jeśli baseline grep wykazałby import z main targetu (D-31 decyzyjny gate), zatrzymaj się i zgłoś do usera — to zmienia scope Wave 2+.

    4. **NIE modyfikuj** `project.yml` sekcji `DeviceActivityMonitorExtension`, `ShieldActionExtension`, `ShieldConfigurationExtension` — ich `sources: - path: Extensions/<Ext>` pozostaje nietknięty przez całą fazę 01.1.
  </action>

  <verify>
    <automated>
      # Plik baseline istnieje
      test -f .planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt
      # Plik ma oba markery (sanity check template format)
      grep -q "^---EVIDENCE---$" .planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt
      grep -q "^---END-EVIDENCE---$" .planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt
      # Extension sources nietknięte
      test -d Extensions/DeviceActivityMonitorExtension
      test -d Extensions/ShieldActionExtension
      test -d Extensions/ShieldConfigurationExtension
      # project.yml extension targety nietknięte (wystarczy sprawdzenie że nadal mają swoje sources)
      grep -q "Extensions/DeviceActivityMonitorExtension" project.yml
      grep -q "Extensions/ShieldActionExtension" project.yml
      grep -q "Extensions/ShieldConfigurationExtension" project.yml
    </automated>
  </verify>

  <acceptance_criteria>
    - `test -f .planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt` → **exit 0**
    - `grep -c "^---EVIDENCE---$" .planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt` → **== 1** (dokładnie jeden marker start)
    - `grep -c "^---END-EVIDENCE---$" .planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt` → **== 1** (dokładnie jeden marker end)
    - Evidence block ma ≥ 0 linii (może być pusty — to happy-path). Żadnej specjalnej wkładki typu "# No imports" wewnątrz markerów.
    - XcodeBuildMCP build 3 extension targetów w SUMMARY: **wszystkie PASS**
    - `git diff Extensions/` → **pusty** (zero zmian w katalogu extensions)
    - `git diff project.yml` → zawiera **tylko** linie związane z Core/ DI (jeśli w ogóle — powinien być pusty bo wildcard katalogowy), a NIE z sekcji extension targetów
  </acceptance_criteria>

  <done>
    Plik `BASELINE_EXTENSION_IMPORTS.txt` istnieje w planning/ w canonical format (header + `---EVIDENCE---` / `---END-EVIDENCE---` markery) jako referencja do regression checka w Wave 6 (`01.1-07`). Evidence block może być pusty (expected happy-path). 3 extension targety zbudowane zielone pre-refactor. Żaden plik w `Extensions/` ani żadna sekcja extension targetu w `project.yml` nie została zmodyfikowana. Jeśli baseline wykazał import z main targetu, fakt jest zgłoszony jako D-31 decyzyjny gate do usera PRZED kontynuacją Wave 2.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Brak nowych trust boundaries. Ten plan tylko przenosi istniejący kod w repo (Swift files z `to_integrate/` do `DeluluDetox/Sources/Core/`) i wykonuje read-only grep po `Extensions/`. Plik `BASELINE_EXTENSION_IMPORTS.txt` żyje w `.planning/` (dokumentacja, nie bundle).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-01 | Tampering | DIContainer.swift podczas kopiowania | accept | Krok 1 akcji wymaga kopii 1:1; `diff` w acceptance_criteria wymusza zero zmian treści. Ręczna modyfikacja wykryta diffem natychmiast. |
| T-01.1-02 | Information Disclosure | BASELINE_EXTENSION_IMPORTS.txt | accept | Plik zawiera nazwy plików Swift projektowych, brak sekretów, brak PII. Jest w `.planning/` git-trackowanym. |
| T-01.1-03 | Denial of Service | Swift 6.2 strict concurrency fail w DIContainer | mitigate | Kod już napisany z `@unchecked Sendable` + manualnym `NSRecursiveLock` + `MainActor.assumeIsolated` — wzorzec sprawdzony pod iOS 26/Swift 6.2. Task 1 krok 6 wymaga zielonego buildu przed przejściem dalej. |

Brak threatów związanych z auth/network/input boundary — plan nie dotyka żadnego z tych obszarów. Auth boundary Phase 1 (ScreenTime authorization prompt) pozostaje niezmieniony.
</threat_model>

<verification>
1. Struktura katalogów: `DeluluDetox/Sources/Core/DependencyInjection/{DIContainer.swift, PropertyWrappers/{Injected.swift, LazyInjected.swift}}` istnieje.
2. Build: `xcodegen generate && XcodeBuildMCP build_sim scheme=DeluluDetox` → exit 0.
3. Testy Phase 1: `XcodeBuildMCP test_sim scheme=DeluluDetox` → wszystkie zielone (AppRootViewModelTests 4/4, OnboardingViewModelTests 3/3, DeluluDetoxTests 1/1).
4. Baseline: `BASELINE_EXTENSION_IMPORTS.txt` istnieje w canonical format; oba markery `---EVIDENCE---` i `---END-EVIDENCE---` obecne; evidence block capture'ed via zawężony grep `@?testable? import DeluluDetox\b` (może być pusty).
5. Zero zmian w: `Extensions/**`, `project.yml` sekcje extension targetów, `DeluluDetox/Sources/Features/**`, `DeluluDetox/Sources/App/**`, `DeluluDetoxTests/**`.
6. Treść: `diff to_integrate/DependencyInjection/*.swift` vs `DeluluDetox/Sources/Core/DependencyInjection/` pusty.
</verification>

<success_criteria>
- 3 pliki DI w nowej lokalizacji, treścią identyczne z `to_integrate/` (verified by diff).
- Projekt buduje się zielony pod Swift 6.2 strict concurrency na default simulatorze.
- Wszystkie istniejące testy Phase 1 przechodzą (brak regresji — zmiana jest additive).
- Baseline extension'ów zapisany (D-30 part a) w canonical format do użycia w Wave 6 regression diff (01.1-07).
- `to_integrate/` nietknięty (cleanup w Wave 6).
- Żadne source'y feature'ów ani `DeluluDetoxApp.swift` ani `DependencyContainer.swift` nie zmodyfikowane w tym planie.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-architecture-foundation/01.1-01-SUMMARY.md` zawierający:
- Listę utworzonych plików + ich SHA (lub krótki fragment pierwszych 3 linijek dla weryfikacji integralności kopii)
- Wynik `diff to_integrate/DependencyInjection/*.swift` vs nowa lokalizacja (powinien być pusty — zapisz "identical" dla każdego pliku)
- Wynik buildu XcodeBuildMCP dla schematu DeluluDetox (simulator model + iOS version + pass/fail + ewentualne warnings Swift 6.2 bez zmian treści DI)
- Wynik testów: liczba zielonych vs łącznie (np. "8/8 passed")
- Wynik baseline extension grep — zawartość BASELINE_EXTENSION_IMPORTS.txt (pełna, z markerami) + wynik buildu 3 extension targetów
- Notatkę czy baseline wykrył D-31 gate (import z main targetu w Extensions/) — jeśli tak, oznaczyć jako BLOCKER dla Wave 2
</output>
</content>
