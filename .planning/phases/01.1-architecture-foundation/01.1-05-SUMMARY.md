---
phase: 01.1-architecture-foundation
plan: 05
subsystem: testing
tags: [mirror-layout, feature-first-tests, di-reset, combine-flakiness, task-yield, xctest, w12, w14, w16, d-27, d-28, d-29]

# Dependency graph
requires:
  - phase: 01.1-architecture-foundation
    plan: 04
    provides: "Finalny AppRootViewModel (@CasePathable Destination: Equatable + Combine sink na ObserveUC + refreshStatus); OnboardingVM/DenialVM bez onAuthorized; ScreenTimeAuthRepository slim (statusPublisher + requestAuthorization + refreshStatus); MockScreenTimeAuthRepository zsynchronizowany; stare AppRootViewModelTests usunięty"
provides:
  - "Mirror test layout: DeluluDetoxTests/Features/{Onboarding,Denial,Home,Root}/ (+ Mocks/ w feature-ownerze Onboarding) per D-27, D-28"
  - "4 mocki w feature-owner location (Onboarding/Mocks/): MockScreenTimeAuthRepository (z initialStatus), MockRequestScreenTimeAuthUseCase (stubbedError + callCount), MockObserveScreenTimeAuthStatusUseCase (publiczny CurrentValueSubject + callCount), MockRefreshScreenTimeAuthStatusUseCase (callCount)"
  - "AppRootViewModelTests (6 testów): initial .notDetermined/.approved routing; emisja .approved/.denied/.notDetermined → destination update przez Combine; refreshStatus() wywołuje RefreshUC; multiple emissions update destination. Każdy Combine-dependent test używa Task.yield() przed asercją (W12)."
  - "DenialViewModelTests (3 testy): retry success/failure + isRequesting start state — konsumuje RequestUC zarejestrowany per test (D-17)"
  - "HomeViewModelTests (1 test): chooseAppsTapped no-op per D-15"
  - "OnboardingViewModelTests (3 testy) — zmigrowany do mirror layoutu + przełączony na MockRequestScreenTimeAuthUseCase (direct UC mock, zero Repo zależności w VM tests)"
  - "Dokumentacja inline: W12 flakiness mitigation rationale + W16 IUO idiom safety komentarze"
affects: [01.1-06, 01.1-07]

# Tech tracking
tech-stack:
  added: []  # Brak nowych deps — wykorzystujemy XCTest + Combine już dostępne
  patterns:
    - "Mirror test layout (D-27): DeluluDetoxTests/Features/<Feature>/{Mocks,<VM>Tests.swift} — 1:1 mapowanie ze strukturą source"
    - "Shared mocks → feature-owner (D-28): ScreenTimeAuth* mocki żyją w Features/Onboarding/Mocks/ — ten sam wzorzec co source-side 'Features/Onboarding/Repository' (zero dualizmu reguł)"
    - "DIContainer.shared.reset() w setUp (D-29) — każdy test case używający @LazyInjected VM wymusza czysty stan między testami"
    - "VM test isolation via direct UC mock: testy VM NIE używają Repository mock + Impl UC — każda z 4 protokołów UC ma własny mock, VM tests rejestrują go bezpośrednio przez DIContainer (eliminuje stack Repo→Impl UC)"
    - "W12 flakiness mitigation: każdy test który robi subject.send(...) → assert vm.destination wstawia 'try await Task.yield()' przed asercją (Combine sink + Observation mutation propagation wymagają run-loop tick na MainActor)"
    - "W16 IUO idiom w XCTest: 'var mockX: MockX!' jest bezpieczny bo setUp runs before every test — dokumentowane komentarzem jednolinijkowym nad deklaracją"
    - "MockObserveScreenTimeAuthStatusUseCase eksponuje publiczny CurrentValueSubject — testy wywołują .send(...) żeby sterować emisjami (zamiast manipulować Repository.stubbedStatus → didSet → subject.send chain)"

key-files:
  created:
    - DeluluDetoxTests/Features/Onboarding/Mocks/MockScreenTimeAuthRepository.swift
    - DeluluDetoxTests/Features/Onboarding/Mocks/MockRequestScreenTimeAuthUseCase.swift
    - DeluluDetoxTests/Features/Onboarding/Mocks/MockObserveScreenTimeAuthStatusUseCase.swift
    - DeluluDetoxTests/Features/Onboarding/Mocks/MockRefreshScreenTimeAuthStatusUseCase.swift
    - DeluluDetoxTests/Features/Onboarding/OnboardingViewModelTests.swift
    - DeluluDetoxTests/Features/Denial/DenialViewModelTests.swift
    - DeluluDetoxTests/Features/Home/HomeViewModelTests.swift
    - DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift
  modified: []  # Plan NIE dotyka source-side plików (B5 — AppRootViewModel Equatable już w Wave 4)
  deleted:
    - DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift  # moved to Features/Onboarding/Mocks/ (git rename 84% similarity)
    - DeluluDetoxTests/Mocks/                                    # pusty katalog usunięty
    - DeluluDetoxTests/OnboardingViewModelTests.swift            # moved to Features/Onboarding/ (git rename 62% similarity, rewrite to use MockRequestUC)

key-decisions:
  - "W14 pre-flight (wildcard project.yml sources) — potwierdzone przed move'em: `grep -A5 '^  DeluluDetoxTests:' project.yml | grep -c '^      - path: DeluluDetoxTests$'` → 1. Plan spec użył `-A3` co jest za wąskie (format ma pustą linię między `sources:` a `- path:`), ale underlying condition met. Zmiana nie potrzebna w project.yml."
  - "Mirror mocks w Onboarding/Mocks/ (D-28 feature-owner) — **ten sam wzorzec co source-side 'Features/Onboarding/Repository/ScreenTimeAuthRepository.swift'**. Denial/Home konsumują mock przez DIContainer.register, analog jak produkcja konsumuje Repo przez UC (bez duplikacji; zero dualizmu reguł per user feedback §#2)."
  - "VM tests używają direct UC mocks — nie MockRepo + Impl UC. Powody: (a) izolacja — VM test nie zależy od poprawności Impl UC; (b) prostota — nie ma setup'u 3 warstw w każdym setUp(); (c) interface-based testing. MockRepo nadal istnieje jako `: ScreenTimeAuthRepository` pełne conformance — przyda się gdy testy integracyjne lub UC unit tests pojawią się w Wave 6+."
  - "MockObserveScreenTimeAuthStatusUseCase ma publiczny `let subject: CurrentValueSubject` — testy kontrolują emisje z zewnątrz (`mockObserve.subject.send(.approved)`). Alternatywa (setter na Publisher) byłaby skomplikowana dla AnyPublisher<…, Never>; `let subject` jest stabilny (let nie umożliwia podmiany, tylko send)."
  - "Task.yield() zamiast XCTestExpectation — 1 tick run-loop wystarcza dla Combine .sink propagation na MainActor, nie potrzeba asyncPredicate. Jeśli flaky → plan spec przewiduje sleep(.milliseconds(1)) fallback (nie wystąpiło w tym runie)."

patterns-established:
  - "Test mirror layout per D-27: DeluluDetoxTests/Features/<Feature>/{Mocks,<VM>Tests.swift} — kopia struktury source"
  - "Feature-owner owns shared test mocks (D-28): te same protokoły → te same mocki → ten sam właściciel feature-owner (Onboarding) co source-side Repository"
  - "DIContainer.shared.reset() w override setUp() jako pierwsza instrukcja po super.setUp() — bulletproof isolation (D-29)"
  - "W12 run-loop tick pattern: `mockObserve.subject.send(…); try await Task.yield(); XCTAssertEqual(vm.destination, …)` — stabilny, 0 tick sleep, 9× użyte w AppRootVMTests"
  - "W16 IUO idiom komentarz — 1 linia dokumentacji powyżej `var mockX: MockX!` uzasadniająca safety (standard XCTest; setUp runs before every test; tearDown nils)"

requirements-completed: []  # Plan requirements.json = [] — obsługuje D-27, D-28, D-29 (test-strukturalne; brak user-facing reqs)

# Metrics
duration: 4min
completed: 2026-04-19
---

# Phase 01.1 Plan 05: Test Mirror Layout + AppRoot Combine VMTests + Full Feature Coverage Summary

**Migracja testów do mirror layoutu (D-27) + 4 mocki feature-owner (D-28) + nowe AppRootViewModelTests/DenialViewModelTests/HomeViewModelTests (7 nowych testów) + zmigrowane OnboardingViewModelTests; 14/14 passed; AppRootVM Combine sink + destination update + refreshStatus UC call pokryte z W12 Task.yield() flakiness mitigation. Brak zmian source-side (B5 — Equatable już w Wave 4).**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-04-19T00:42:19Z
- **Completed:** 2026-04-19T00:45:59Z
- **Tasks:** 2 (wszystkie auto)
- **Files created:** 8 (4 mocki + 4 test files), modified: 0 (source untouched per B5), deleted: 2 (Mocks/MockScreenTimeAuthRepository.swift + flat OnboardingViewModelTests.swift — oba jako git renames)

## Accomplishments

- **Mirror layout dostarczony** — `DeluluDetoxTests/Features/{Onboarding,Denial,Home,Root}/` 1:1 z `DeluluDetox/Sources/Features/…`. Zero dualizmu reguł (user feedback §#2).
- **4 nowe mocki w feature-owner location** (Onboarding/Mocks/):
  - `MockScreenTimeAuthRepository` — przeniesiony + rozszerzony o `init(initialStatus:)` (elastyczność testowa)
  - `MockRequestScreenTimeAuthUseCase` — `stubbedError` + `callCount`
  - `MockObserveScreenTimeAuthStatusUseCase` — `let subject: CurrentValueSubject` publicznie (testy sterują emisjami) + `callCount`
  - `MockRefreshScreenTimeAuthStatusUseCase` — `callCount`
- **AppRootViewModelTests (6 testów)** — pełne pokrycie core-value flow:
  - `testInitialDestinationForNotDetermined` — CurrentValueSubject initial → .onboarding
  - `testInitialDestinationForApproved` — .approved → .home (initialStatus)
  - `testEmissionOfApprovedRoutesToHome` — .notDetermined → emit .approved → .home (Combine sink path)
  - `testEmissionOfDeniedRoutesToDenial` — emit .denied → .denial
  - `testRefreshStatusCallsUseCase` — vm.refreshStatus() → mockRefresh.callCount == 1 (D-13/D-14 core-value)
  - `testMultipleEmissionsUpdateDestination` — 3× emisja (approved → denied → notDetermined) → 3× destination update
- **DenialViewModelTests (3 testy)** — nowe w 01.1: testRetrySuccess (callCount=1), testRetryFailure (error path), testIsRequestingStartsFalse. Denial konsumuje RequestUC zarejestrowany per test (D-17) — DI distributed registration weryfikowane empirycznie.
- **HomeViewModelTests (1 test)** — trivial bo HomeVM jest pusty w Phase 1 (D-15). testChooseAppsTappedIsNoOpInPhase1 weryfikuje że wywołanie nie crashuje.
- **OnboardingViewModelTests (3 testy)** — zmigrowane do mirror + przestawione na MockRequestUC (zamiast MockRepo + Impl UC) — prostszy setup, VM test nie zależy od Impl UC poprawności.
- **W12 flakiness mitigation udokumentowana w kodzie** — komentarz na górze AppRootVMTests tłumaczy dlaczego `try await Task.yield()` jest konieczny przed asercjami post-`subject.send`.
- **W16 IUO idiom komentarz** — każdy test z `var mockX: MockX!` ma dokumentacyjny komentarz wyjaśniający safety (setUp runs before every test).
- **Stary flat layout w pełni usunięty:** `DeluluDetoxTests/Mocks/` (katalog znikł) + `DeluluDetoxTests/OnboardingViewModelTests.swift` (plik znikł).
- **Zero referencji do `DependencyContainer` / `onAuthorized` w całym DeluluDetoxTests/.**
- **Plan NIE dotknął `DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift`** (B5 — Equatable dodane w Wave 4; `git diff db67ef8 -- AppRootViewModel.swift` = empty).
- **Build + test zielony na iPhone 17 / iOS 26.2, 14/14 tests passed w 0.010s.**

## Task Commits

Każde zadanie committed atomicznie z `--no-verify` (parallel executor mode).

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Move MockScreenTimeAuthRepository + add 3 UC mocks (D-27, D-28) | `af93a01` | Mocks × 4 (3 NEW + 1 renamed), old Mocks/ deleted |
| 2 | Add mirror-layout VMTests + migrate Onboarding (D-27, D-29, W12) | `165dc1b` | VMTests × 4 (3 NEW + 1 renamed-and-rewritten), flat OnboardingViewModelTests deleted |

_No TDD tasks — wszystkie 2 zadania `type="auto"`. Plan NIE modyfikuje source-side — test-only migration._

## Files Created / Modified / Deleted

### Created (8)

| File | Purpose |
|------|---------|
| `DeluluDetoxTests/Features/Onboarding/Mocks/MockScreenTimeAuthRepository.swift` | Mock `: ScreenTimeAuthRepository` z `init(initialStatus:)`; `stubbedStatus.didSet { statusSubject.send(…) }`; `requestAuthorizationCallCount` + `refreshStatusCallCount` + `requestAuthorizationError` knobs |
| `DeluluDetoxTests/Features/Onboarding/Mocks/MockRequestScreenTimeAuthUseCase.swift` | Mock `: RequestScreenTimeAuthUseCase` z `stubbedError` + `callCount` |
| `DeluluDetoxTests/Features/Onboarding/Mocks/MockObserveScreenTimeAuthStatusUseCase.swift` | Mock `: ObserveScreenTimeAuthStatusUseCase` z publicznym `let subject: CurrentValueSubject` + `callCount` + `init(initialStatus:)` |
| `DeluluDetoxTests/Features/Onboarding/Mocks/MockRefreshScreenTimeAuthStatusUseCase.swift` | Mock `: RefreshScreenTimeAuthStatusUseCase` z `callCount` |
| `DeluluDetoxTests/Features/Onboarding/OnboardingViewModelTests.swift` | `@MainActor` XCTestCase × 3 testy (success/failure/startsFalse); DIContainer.reset + register MockRequestUC w setUp |
| `DeluluDetoxTests/Features/Denial/DenialViewModelTests.swift` | `@MainActor` XCTestCase × 3 testy (success/failure/startsFalse); konsumuje to samo MockRequestUC przez direct register (D-17) |
| `DeluluDetoxTests/Features/Home/HomeViewModelTests.swift` | XCTestCase × 1 test (no-op per D-15) — brak setUp bo HomeVM nie używa @LazyInjected |
| `DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift` | `@MainActor` XCTestCase × 6 testów; komentarz W12 na górze; setUp rejestruje MockObserveUC + MockRefreshUC; testy Combine emisji z Task.yield() |

### Deleted (2 files + 1 empty dir)

- `DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift` — zmapowany przez `git rename` (84% similarity) do `Features/Onboarding/Mocks/…`. Plik ten sam semantycznie (z dodanym `init(initialStatus:)`); logika niezmieniona.
- `DeluluDetoxTests/Mocks/` — pusty katalog po renamie, usunięty `rmdir`.
- `DeluluDetoxTests/OnboardingViewModelTests.swift` — zmapowany przez `git rename` (62% similarity) do `Features/Onboarding/OnboardingViewModelTests.swift`. Rewrite — używa teraz `MockRequestScreenTimeAuthUseCase` zamiast `MockScreenTimeAuthRepository` + `RequestScreenTimeAuthUseCaseImpl`.

### Source-side Modified (0 — B5)

**Plan NIE dotyka `DeluluDetox/Sources/**` ani `project.yml`.** AppRootViewModel.Destination ma już `: Equatable` od Wave 4 (Task 1 commit `109ffac`). `git diff db67ef8 -- DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` = empty (confirmed).

## Test Matrix

| Test Class | Test | Assertions | Result |
|------------|------|------------|--------|
| `AppRootViewModelTests` | testInitialDestinationForNotDetermined | 1 (`.onboarding`) | **PASSED** |
| `AppRootViewModelTests` | testInitialDestinationForApproved | 1 (`.home`) | **PASSED** |
| `AppRootViewModelTests` | testEmissionOfApprovedRoutesToHome | 2 (`.onboarding` initial, `.home` post-emit) | **PASSED** |
| `AppRootViewModelTests` | testEmissionOfDeniedRoutesToDenial | 1 (`.denial`) | **PASSED** |
| `AppRootViewModelTests` | testRefreshStatusCallsUseCase | 2 (callCount 0 → 1) | **PASSED** |
| `AppRootViewModelTests` | testMultipleEmissionsUpdateDestination | 3 (`.home`, `.denial`, `.onboarding`) | **PASSED** |
| `OnboardingViewModelTests` | testGrantAccessSuccess | 3 (callCount=1, error=nil, !isRequesting) | **PASSED** |
| `OnboardingViewModelTests` | testGrantAccessFailure | 4 (callCount=1, error=non-nil + string match, !isRequesting) | **PASSED** |
| `OnboardingViewModelTests` | testIsRequestingStartsFalse | 1 (!isRequesting) | **PASSED** |
| `DenialViewModelTests` | testRetrySuccess | 2 (callCount=1, !isRequesting) | **PASSED** |
| `DenialViewModelTests` | testRetryFailure | 2 (callCount=1, !isRequesting) | **PASSED** |
| `DenialViewModelTests` | testIsRequestingStartsFalse | 1 (!isRequesting) | **PASSED** |
| `HomeViewModelTests` | testChooseAppsTappedIsNoOpInPhase1 | 0 (just non-crash) | **PASSED** |
| `DeluluDetoxTests` | testPlaceholder | inherited | **PASSED** |

**Total: 14/14 passed in 0.010 seconds.** Assertion count: 23 explicit + 1 non-crash check = 24 assertions.

## Build & Test Results

### Tooling note

Ten sam constraint co W1-W4 — XcodeBuildMCP tools niedostępne w runtime parallel worktree executora, użyto `xcodebuild` CLI. `project.yml` nietknięty; `xcodegen generate` pochłonął nowe katalogi przez wildcard `sources: - path: DeluluDetoxTests`. Flag `-skipMacroValidation` wymagany (pre-existing SPM macro approval drift dla CasePathsMacros, PerceptionMacros).

### Build

- **Command:** `xcodebuild -project DeluluDetox.xcodeproj -scheme DeluluDetox -destination "platform=iOS Simulator,name=iPhone 17,OS=26.2" -configuration Debug -skipMacroValidation build`
- **Wynik:** `** BUILD SUCCEEDED **`
- **Swift 6.2 strict concurrency warnings w nowych plikach:** zero
- **Extension validation:** 3/3 appex bundles OK

### Tests

- **Command:** `xcodebuild … test`
- **Wynik:** `** TEST SUCCEEDED **` — **14/14 passed in 0.010 s**
- Przyrost vs Wave 4: **14 tests** (Wave 4 miał 4) — pełne pokrycie 4 VM'ów w 01.1.

## Acceptance Criteria

### Task 1 Acceptance Criteria (11/11 met)

| # | Criterion | Target | Result |
|---|-----------|--------|--------|
| 1 | `test -f DeluluDetoxTests/Features/Onboarding/Mocks/MockScreenTimeAuthRepository.swift` | exit 0 | **exit 0** |
| 2 | `test -f MockRequestScreenTimeAuthUseCase.swift` | exit 0 | **exit 0** |
| 3 | `test -f MockObserveScreenTimeAuthStatusUseCase.swift` | exit 0 | **exit 0** |
| 4 | `test -f MockRefreshScreenTimeAuthStatusUseCase.swift` | exit 0 | **exit 0** |
| 5 | `test ! -f DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift` | exit 0 | **exit 0** |
| 6 | MockRepo `: ScreenTimeAuthRepository` | ≥ 1 | **1** |
| 7 | MockRequest `: RequestScreenTimeAuthUseCase` | ≥ 1 | **1** |
| 8 | MockObserve `: ObserveScreenTimeAuthStatusUseCase` | ≥ 1 | **1** |
| 9 | MockRefresh `: RefreshScreenTimeAuthStatusUseCase` | ≥ 1 | **1** |
| 10 | MockObserve `let subject: CurrentValueSubject` | ≥ 1 | **1** |
| 11 | MockRefresh `var callCount` | ≥ 1 | **1** |

### Task 2 Acceptance Criteria (17/17 met)

| # | Criterion | Target | Result |
|---|-----------|--------|--------|
| 1 | W14 wildcard: `grep -A5 '^  DeluluDetoxTests:' project.yml \| grep -c '^      - path: DeluluDetoxTests$'` | ≥ 1 | **1** (plan spec `-A3` jest za wąskie; użyto `-A5` — underlying condition met) |
| 2 | 4 × test files exist | exit 0 | **exit 0 × 4** |
| 3 | Flat OnboardingViewModelTests.swift gone | exit 0 | **exit 0** |
| 4 | Flat AppRootViewModelTests.swift gone | exit 0 | **exit 0** (był już usunięty w Wave 4) |
| 5 | DIContainer.reset() w Onboarding/Denial/Root setUp | ≥ 1 każdy | **1/1/1** |
| 6 | `subject.send` w Root tests | ≥ 3 | **6** |
| 7 | `mockRefresh.callCount` w Root tests | ≥ 1 | **2** |
| 8 | `XCTAssertEqual(vm.destination` | ≥ 4 | **8** |
| 9 | W12 `Task.yield\|Task.sleep` w Root | ≥ 4 | **9** |
| 10 | W12 komentarz `W12 flakiness\|run-loop tick` | exit 0 | **exit 0** (2 hits) |
| 11 | W16 IUO komentarz gdzieś w Features/ | ≥ 1 | **3** (Denial, Root, Onboarding) |
| 12 | Zero `DependencyContainer\|onAuthorized` w DeluluDetoxTests/ | 0 | **0** |
| 13 | Zero `import.*Repository` w OnboardingVMTests.swift (VM→UC only) | 0 | **0** |
| 14 | `git diff db67ef8 -- AppRootViewModel.swift` (B5) | empty | **empty** |
| 15 | XcodeBuild build | exit 0 | **exit 0** |
| 16 | XcodeBuild test | 14/14 | **14/14** |
| 17 | ≥ 8 tests (Phase 1 minimum) | ≥ 8 | **14** |

### Total Acceptance Criteria: 28/28 met.

## Plan Notes Status

### W14 — project.yml wildcard verified

**Status:** `project.yml wildcard verified, no change`. DeluluDetoxTests target `sources: - path: DeluluDetoxTests` (wildcardowy katalog) — xcodegen łapie automatyczne nowe pliki w podkatalogach `Features/{Onboarding,Denial,Home,Root}/`. Plan spec użył `grep -A3` co jest za wąskie dla tego formatu yaml (linia `sources:` + pusta + `- path: …`); poprawka `-A5` potwierdza wildcard. Underlying condition met.

### W12 — Task.yield() flakiness mitigation

**Status:** `Task.yield() present in 9 occurrences across AppRootViewModelTests`. Testy używające flakiness mitigation:

- `testInitialDestinationForNotDetermined` — 1× yield (przed asercją initial)
- `testInitialDestinationForApproved` — 1× yield (przed asercją initial)
- `testEmissionOfApprovedRoutesToHome` — 2× yield (przed initial + post-emit)
- `testEmissionOfDeniedRoutesToDenial` — 1× yield (przed asercją)
- `testMultipleEmissionsUpdateDestination` — 3× yield (przed każdą z 3 asercji)
- `testRefreshStatusCallsUseCase` — **0× yield** (zgodnie z T-01.1-19: ten test nie angażuje Combine emisji, tylko bezpośrednie wywołanie synchronicznej metody)
- `testInitialDestinationForApproved` setUp re-register — nie liczy się (setup time)

**Suma:** 9 yields rozmieszczone w 5 testach. Sleep fallback (`Task.sleep(.milliseconds(1))`) nie był potrzebny — wszystkie testy zielone w pierwszym runie.

### W16 — IUO idiom comment

**Status:** `IUO idiom comment present in 3 test files`. Komentarze wyjaśniające safety w:

- `OnboardingViewModelTests.swift` — 2 linie (pełny komentarz z rationale)
- `DenialViewModelTests.swift` — 1 linia (short reference do W16)
- `AppRootViewModelTests.swift` — 1 linia (short reference do W16)
- `HomeViewModelTests.swift` — brak (nie używa IUO — HomeVM nie ma @LazyInjected, brak setUp z mockami)

### B5 — AppRootViewModel source untouched

**Status:** `AppRootViewModel.swift untouched (Equatable w Wave 4)`. `git diff db67ef8 -- DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` zwraca pustą diff. `: Equatable` conformance istnieje w linii 11 (pochodzi z Wave 4 commit `109ffac`). Plan 01.1-05 `files_modified` frontmatter zawiera WYŁĄCZNIE pliki w DeluluDetoxTests/.

## Decisions Made

Patrz `key-decisions` w frontmatter. Krótko: 5 kluczowych decyzji wyekstrahowanych z execution:

1. **W14 grep spec korekta** — plan spec `-A3` za wąskie, użyto `-A5` (pokrycie większe, underlying condition satisfied, project.yml niezmieniony).
2. **Mirror mocks w Onboarding/Mocks/** (D-28) — symetria source-side (Features/Onboarding/Repository/) = zero dualizmu reguł.
3. **VM tests używają direct UC mocks** (nie Repo+Impl UC) — izolacja, prostota, interface-based testing.
4. **MockObserveUC exposes public `let subject`** — testy kontrolują emisje z zewnątrz; alternatywa byłaby skomplikowana.
5. **Task.yield() zamiast XCTestExpectation** — 1 tick run-loop wystarczy dla Combine .sink na MainActor.

## Deviations from Plan

**1. [Rule 3 - Blocking (minor)] W14 pre-flight grep spec użył `-A3` zamiast `-A5`**

- **Found during:** Task 2 Step 0 (W14 pre-flight sanity check)
- **Issue:** Plan spec cytuje `grep -A3 "^  DeluluDetoxTests:" project.yml | grep -c "^      - path: DeluluDetoxTests$"` jako check. Dla tego formatu yaml (linia `sources:` plus pusta linia plus `- path: …`) `-A3` to za wąskie okno kontekstu — zwraca 0, nie 1. Underlying condition ("wildcard path for DeluluDetoxTests target") JEST spełniony — plik zawiera `- path: DeluluDetoxTests` w sources target'u testów.
- **Fix:** Użyto poszerzonego zapytania `grep -A5` które poprawnie raportuje `1` dla tego formatu. Plan intent (sanity-check, że xcodegen łapie nowe pliki automatycznie) spełniony. Zapisane w SUMMARY jako deviation dla precyzji.
- **Files modified:** zero (sanity check grep'em, nie zmiana project.yml)
- **Verification:** Po `xcodegen generate` + build test projekt zawiera wszystkie 8 nowych plików testowych w target'cie DeluluDetoxTests; 14/14 tests passed.
- **Plan alignment:** Plan intent spełniony, tylko literał grep'a skorygowany. Nie deviation od architektury — tylko od dokładnej komendy verification.

**Total deviations:** 1 (Rule 3 — grep command literal correction dla formatu yaml)
**Impact on plan:** Zerowy — W14 invariant (project.yml wildcard) spełniony; plan spec grep był niepoprawny literałowo, underlying check PASS. Nie zmienia żadnego source file.

## Issues Encountered

None. Build i testy zielone w pierwszym runie; zero flakiness widocznej w 14 testach (W12 mitigation działa preventively).

## User Setup Required

None.

## Threat Model Check

Wszystkie entry z threat register planu spełnione:

- **T-01.1-17 (Tampering: DIContainer.shared leak między testami):** **mitigated** — 3/3 plików testowych z @LazyInjected używa `DIContainer.shared.reset()` w `setUp` (zweryfikowane grep'em: 1 hit w Onboarding, 1 w Denial, 1 w Root; Home nie używa @LazyInjected, więc reset nie potrzebny). 14/14 tests passed przy każdym z 4 runów tego samego sorting/order — brak state leak.
- **T-01.1-18 (Spoofing: publiczny MockObserveUC.subject):** **accepted** — Subject jest instance property `MockObserveScreenTimeAuthStatusUseCase`, każdy test tworzy własną instancję w setUp. Po `DIContainer.reset()` stare mocki są zwalniane. Wszystkie testy `@MainActor` → brak cross-actor race.
- **T-01.1-19 (DoS: Combine emission + assertion flakiness):** **mitigated (W12)** — 9 × `try await Task.yield()` w AppRootVMTests (każdy Combine-dependent test). `testRefreshStatusCallsUseCase` **nie** używa yield zgodnie z planem — nie angażuje Combine emisji, tylko bezpośrednie wywołanie synchronicznej metody VM. Komentarz W12 na górze pliku uzasadnia pattern.

**Core value tests** (T-01.1-16 z Wave 4): pokryte przez `testRefreshStatusCallsUseCase` (D-13/D-14) + `testEmissionOfApprovedRoutesToHome` (Combine pipeline end-to-end). Real-device scenePhase w zakresie Wave 6 human-verify.

## Threat Flags

None — plan tylko tworzy pliki testowe + porządkuje layout. Zero nowych:
- Network endpoints
- Auth paths
- File access patterns
- Trust boundary schema changes

Test target izolowany od release build — `@testable import DeluluDetox` tylko w DeluluDetoxTests, zero leak do app bundle.

## Known Stubs

None.

Każdy test ma pełne assercje (patrz Test Matrix). MockRepo, MockRequestUC, MockObserveUC, MockRefreshUC implementują pełne protocol conformance — nie są stub'ami tylko test doubles ze stateful behavior (callCount, stubbedError, subject.send relay).

## Next Phase Readiness

- **Wave 6 (01.1-06 — docs updates via parallel executor `agent-a3b417ff` or similar) unblocked:** ten plan NIE dotyka `.claude/guides/**`, więc file overlap z planem 01.1-06 jest zerowy per parallel_execution constraint.
- **Wave 6/7 (01.1-07 — cleanup + extension regression + human-verify) unblocked:**
  - Test suite ma nowy baseline 14 testów, Wave 7 może porównać post-cleanup.
  - `to_integrate/` deletion zaplanowane (nietknięte w 01.1-05).
  - Extension baseline regression check (D-30 part b): `grep -rn "import DeluluDetox\|@testable import DeluluDetox" Extensions/` → 0 (niezmienione — ten plan NIE dotyka Extensions/).
  - Formal human-verify na real device: Phase 1 flow (cold-launch → Onboarding → grant → Home; denied → Denial → retry; scenePhase .active → refresh real w Settings).
- **Core value protected** — 6 testów AppRootVM weryfikują core-value logic (Combine + refreshStatus); Wave 6 empirically verifies on real device.
- **No blockers or concerns** dla subsequent waves.

## Self-Check

Verified post-write:

| Claim | Check | Result |
|-------|-------|--------|
| `Features/Onboarding/Mocks/MockScreenTimeAuthRepository.swift` exists | `test -f` | **FOUND** |
| `Features/Onboarding/Mocks/MockRequestScreenTimeAuthUseCase.swift` exists | `test -f` | **FOUND** |
| `Features/Onboarding/Mocks/MockObserveScreenTimeAuthStatusUseCase.swift` exists | `test -f` | **FOUND** |
| `Features/Onboarding/Mocks/MockRefreshScreenTimeAuthStatusUseCase.swift` exists | `test -f` | **FOUND** |
| `Features/Onboarding/OnboardingViewModelTests.swift` exists | `test -f` | **FOUND** |
| `Features/Denial/DenialViewModelTests.swift` exists | `test -f` | **FOUND** |
| `Features/Home/HomeViewModelTests.swift` exists | `test -f` | **FOUND** |
| `Features/Root/AppRootViewModelTests.swift` exists | `test -f` | **FOUND** |
| Old flat `DeluluDetoxTests/Mocks/` gone | `test ! -d` | **GONE** |
| Old flat `DeluluDetoxTests/OnboardingViewModelTests.swift` gone | `test ! -f` | **GONE** |
| Task 1 commit `af93a01` exists | `git log` | **FOUND** |
| Task 2 commit `165dc1b` exists | `git log` | **FOUND** |
| Zero `DependencyContainer\|onAuthorized` in DeluluDetoxTests/ | `grep -rc` | **0** |
| `AppRootViewModel.swift` untouched (B5) | `git diff db67ef8` | **empty** |
| 14/14 tests passed | xcodebuild test | **14/14** |

## Self-Check: PASSED

---

*Phase: 01.1-architecture-foundation*
*Completed: 2026-04-19*
