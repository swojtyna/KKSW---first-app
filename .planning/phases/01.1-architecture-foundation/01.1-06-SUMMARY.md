---
phase: 01.1-architecture-foundation
plan: 06
type: summary
status: complete
executed: 2026-04-19
---

# 01.1-06 SUMMARY — Doc closure (navigation + DI guides)

## Overview

Deklaratywna aktualizacja dwóch plików guide'ów opisująca już shipped patterns z Wave 1-4 — zero nowych konwencji. D-24 (Wzorzec B) + D-25 (Combine publisher + RefreshUC) + B8 (usunięcie stałej referencji do `DependencyContainer`) zamknięte.

Plan został pierwotnie uruchomiony w parallelnym subagencie, ale agent zablokował się na runtime read-tracking (czytał main-checkout paths, edytował worktree paths). Zgodnie z decyzją użytkownika ("Execute inline"), orchestrator wykonał plan bezpośrednio w głównym working tree. Worktree `agent-a2aebd93` usunięty, zero commitów leaked.

---

## Commits

| SHA | Task | Subject |
|-----|------|---------|
| `4c4698b` | 1 | docs(01.1-06): navigation GUIDE adds Wzorzec A/B split + removes stale DependencyContainer |
| `ae285de` | 2 | docs(01.1-06): DI GUIDE adds Reactive state section (CurrentValueSubject + RefreshUC) |

---

## Task 1 — navigation/GUIDE.md

### Sections modified

1. **Renamed** `## Pattern: `Destination` enum on the ViewModel` → `## Wzorzec A — Modal presentation (VM w enum payload)` + intro sentence explaining it's one of two patterns.
2. **Inserted** new section `## Wzorzec B — Root-level state switching (VM w child view, nie w enum)` between `## Alert / confirmation dialog` and `## NavigationStack with a path` (line-anchored I20).
3. **Updated** `## Decision tree: which presentation?` — first branch now routes to Wzorzec B when child screen replaces parent; other branches tagged Wzorzec A where applicable.
4. **Rewrote** `## Integration with architecture` — replaced stale line "Child ViewModels are constructed by the parent ViewModel or by `DependencyContainer`" with explicit Wzorzec A vs B guidance + event flow description.

### B8 status — before / after

**Before:**
```
- Child ViewModels are constructed by the parent ViewModel or by `DependencyContainer`. See `.claude/guides/architecture/GUIDE.md`.
```

**After:**
```
- **Wzorzec A:** parent ViewModel constructs child ViewModels and places them in enum cases.
- **Wzorzec B:** child View konstruuje child ViewModel przez `@State private var model = ChildViewModel()`; child VM rezolwuje dependencies przez `@LazyInjected` against `DIContainer.shared` (patrz `.claude/guides/dependency-injection/GUIDE.md`).
- Event flow in Wzorzec B: child writes → Repository (via UseCase) → Repository's CurrentValueSubject emits → parent's Combine `.sink` updates destination.
```

**Grep evidence:**
- `grep -c "DependencyContainer" .claude/guides/navigation/GUIDE.md` → **0** ✓
- `grep -c "DIContainer" .claude/guides/navigation/GUIDE.md` → **1** ✓

### Acceptance grep results

| Criterion | Required | Got | Pass |
|-----------|----------|-----|------|
| `Wzorzec A` count | ≥ 2 | 8 | ✓ |
| `Wzorzec B` count | ≥ 3 | 9 | ✓ |
| `Root-level state switching` | ≥ 1 | 1 | ✓ |
| `AppRootViewModel` live example | ≥ 1 | 2 | ✓ |
| `enum Destination: Equatable` snippet | ≥ 1 | 1 | ✓ |
| `@LazyInjected` reference | ≥ 1 | 3 | ✓ |
| `CurrentValueSubject` reference | ≥ 1 | 2 | ✓ |
| `DependencyContainer` | == 0 | 0 | ✓ |
| Markdown fences | balanced | 20 (balanced) | ✓ |

---

## Task 2 — dependency-injection/GUIDE.md

### Sections modified

1. **Inserted** new section `## Reactive state: Combine publisher w Repository` between `## Init Injection Pattern` and `## `@LazyInjected` Pattern` (line-anchored I20).

### Section content (high level)

- Kiedy to stosować — uzasadnienie CurrentValueSubject vs AsyncStream.
- Wzorzec: Repository + 2 UC (Observe + Refresh) z pełnymi snippetami z live kodu (Features/Onboarding/Repository/ScreenTimeAuthRepository.swift, Features/Onboarding/UseCase/Observe*, Refresh*).
- Rejestracja w DIContainer — Repository `.application` (state-holding singleton), UC `.unique`.
- Konsumpcja w VM — `@ObservationIgnored @LazyInjected` + Combine `.sink` + `cancellables`.
- Refresh vs observe — dlaczego OBA (core-value violation bez refresh).
- Antywzorzec — sync getter protocol.

### Acceptance grep results

| Criterion | Required | Got | Pass |
|-----------|----------|-----|------|
| `CurrentValueSubject` | ≥ 2 | 5 | ✓ |
| `RefreshScreenTimeAuthStatusUseCase` | ≥ 2 | 6 | ✓ |
| `ObserveScreenTimeAuthStatusUseCase` | ≥ 2 | 6 | ✓ |
| `statusPublisher \| AnyPublisher<AuthorizationStatus` | ≥ 2 | 5 | ✓ |
| `@ObservationIgnored` | ≥ 1 | 5 | ✓ |
| `scope: .application` | ≥ 1 | 7 | ✓ |
| `Reactive state` | ≥ 1 | 1 | ✓ |
| `core value` / `core-value` | ≥ 1 | 1 | ✓ |
| Markdown fences | balanced | 46 (balanced) | ✓ |

---

## I20 line-anchored insertions

| File | Anchor | Insertion point |
|------|--------|-----------------|
| navigation/GUIDE.md | After `## Alert / confirmation dialog` | Before `## NavigationStack with a path` |
| dependency-injection/GUIDE.md | After `## Init Injection Pattern` | Before `## `@LazyInjected` Pattern` |

---

## Scope boundary note

The plan's acceptance criterion at line 344-345 specifies an aggregate grep across 4 guide files (`navigation`, `dependency-injection`, `architecture`, `feature-structure`) for `"no container framework\|service locator"` to return 0. Current state:

- `navigation/GUIDE.md`: 0 matches ✓
- `dependency-injection/GUIDE.md`: 0 matches ✓
- `feature-structure/GUIDE.md`: 0 matches ✓
- `architecture/GUIDE.md`: **1 match** — line 226: `"service locatorów rozsianych po kodzie"`

The single match in `architecture/GUIDE.md` line 226 reads:
> "Unikaj: globalnych singletonów poza `DIContainer` (który jest jawnym, kontrolowanym wyjątkiem), god objects, service locatorów rozsianych po kodzie."

This is an **anti-pattern warning** (cautioning *against* sprawling service locators) and is **consistent** with "use `DIContainer` as the one controlled exception" from the DI guide — not a contradiction with shipped DI architecture. The plan's `files_modified` lists only `navigation/GUIDE.md` and `dependency-injection/GUIDE.md`, so `architecture/GUIDE.md` is out of scope. Honoring plan scope over literal grep count: the match is intentional anti-pattern guidance, not drift from the DI container pattern.

If the user wants a clean zero-match result, a follow-up plan can rephrase line 226 (e.g. replace "service locatorów" with "implicit dependency lookups sprawled across code").

---

## D-24 / D-25 / B8 status

| Decision | Status | Evidence |
|----------|--------|----------|
| **D-24** (Wzorzec A vs B split, root-level state switching) | ✓ resolved | navigation/GUIDE.md has both Wzorzec A and Wzorzec B sections, Decision tree differentiates, AppRootViewModel live example |
| **D-25** (Combine publisher in Repository + RefreshUC) | ✓ resolved | dependency-injection/GUIDE.md has Reactive state section with full Repo+Observe+Refresh pattern |
| **B8** (stale `DependencyContainer` reference in navigation/GUIDE.md) | ✓ closed | `grep -c DependencyContainer navigation/GUIDE.md` = 0; replaced with `@LazyInjected + DIContainer.shared` |

---

## Deviations

1. **Execution mode deviation**: plan was intended to run as a parallel subagent in Wave 5; subagent blocked by runtime path-tracking (read main-checkout paths, tried to edit worktree paths). Orchestrator escalated to user, user authorized inline execution. No impact on plan content or acceptance criteria.

2. **Scope boundary documented above** for the architecture/GUIDE.md anti-pattern line — treated as intentional anti-pattern guidance, out of plan scope.

---

## Notes

Dokumenty fazy 01.1 zamknięte. Jedyne pozostałe zadanie: Wave 6 cleanup (01.1-07) — delete `to_integrate/`, verify extension import regression.
