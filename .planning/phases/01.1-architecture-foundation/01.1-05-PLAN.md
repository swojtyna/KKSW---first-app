---
phase: 01.1-architecture-foundation
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - DeluluDetoxTests/Features/Onboarding/Mocks/MockScreenTimeAuthRepository.swift
  - DeluluDetoxTests/Features/Onboarding/Mocks/MockRequestScreenTimeAuthUseCase.swift
  - DeluluDetoxTests/Features/Onboarding/Mocks/MockObserveScreenTimeAuthStatusUseCase.swift
  - DeluluDetoxTests/Features/Onboarding/Mocks/MockRefreshScreenTimeAuthStatusUseCase.swift
  - DeluluDetoxTests/Features/Onboarding/OnboardingViewModelTests.swift
  - DeluluDetoxTests/Features/Denial/DenialViewModelTests.swift
  - DeluluDetoxTests/Features/Home/HomeViewModelTests.swift
  - DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift
  - DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift  # DELETED (moved)
  - DeluluDetoxTests/OnboardingViewModelTests.swift  # DELETED (moved+rewrite)
autonomous: true
requirements: []  # Covers D-27, D-28, D-29 — test mirror layout + DIContainer.reset() + shared mock → feature-owner
user_setup: []

must_haves:
  truths:
    - "Testy żyją w mirror layout: `DeluluDetoxTests/Features/<Feature>/{Mocks,<VM>Tests.swift}` zgodnie z D-27."
    - "Mocki współdzielone (ScreenTimeAuth*) żyją w `DeluluDetoxTests/Features/Onboarding/Mocks/` — feature-owner mock location (D-28) — **ten sam wzorzec co source-side `Features/Onboarding/Repository`**, zero dualizmu reguł (patrz specifics user feedback #2)."
    - "Każdy TestCase używający `@LazyInjected` ma `DIContainer.shared.reset()` w setUp + rejestruje mocki przed utworzeniem VM (D-29)."
    - "AppRootViewModelTests weryfikuje że zmiana emisji z mock ObserveUC powoduje update `destination` (zastępstwo za stare testy na Screen enum)."
    - "Każdy test który robi `subject.send(...)` a następnie asertuje `vm.destination` wstawia `try await Task.yield()` **bezpośrednio przed asercją** — W12: flakiness mitigation bo Combine sink + Observation notification wymagają run-loop tick."
    - "OnboardingViewModelTests + DenialViewModelTests + HomeViewModelTests istnieją w nowym layoucie i testują kontrakt VM bez onAuthorized callback."
    - "`DeluluDetoxTests/Mocks/` stary katalog usunięty. `DeluluDetoxTests/OnboardingViewModelTests.swift` płaski plik usunięty."
    - "Wszystkie testy passed: 4+3+1+3 = ~11+ testów (dokładna liczba zależy od write'u; min 8, ideał 11+)."
    - "`project.yml` DeluluDetoxTests target ma `sources:` wildcard (np. `- path: DeluluDetoxTests`) — xcodegen łapie nowe pliki testowe automatycznie; weryfikowane PRZED move'em (W14 sanity-check)."
  artifacts:
    - path: "DeluluDetoxTests/Features/Onboarding/Mocks/MockScreenTimeAuthRepository.swift"
      provides: "Mock Repository z CurrentValueSubject + stubbedStatus didSet propagation + refreshStatus counter"
      contains: "final class MockScreenTimeAuthRepository: ScreenTimeAuthRepository"
    - path: "DeluluDetoxTests/Features/Onboarding/Mocks/MockRequestScreenTimeAuthUseCase.swift"
      provides: "Protocol-conforming mock z stubbedError + callCount"
      contains: "final class MockRequestScreenTimeAuthUseCase: RequestScreenTimeAuthUseCase"
    - path: "DeluluDetoxTests/Features/Onboarding/Mocks/MockObserveScreenTimeAuthStatusUseCase.swift"
      provides: "Mock z controllable CurrentValueSubject — testy mogą wysyłać kolejne statusy"
      contains: "final class MockObserveScreenTimeAuthStatusUseCase: ObserveScreenTimeAuthStatusUseCase"
    - path: "DeluluDetoxTests/Features/Onboarding/Mocks/MockRefreshScreenTimeAuthStatusUseCase.swift"
      provides: "Mock z callCount — weryfikacja że scenePhase hook wołał refresh"
      contains: "final class MockRefreshScreenTimeAuthStatusUseCase: RefreshScreenTimeAuthStatusUseCase"
    - path: "DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift"
      provides: "6 testów: init z .notDetermined → .onboarding; init .approved → .home; emisja .approved → .home; emisja .denied → .denial; refreshStatus() wywołuje UC; multiple emissions update destination"
      contains: "final class AppRootViewModelTests"
    - path: "DeluluDetoxTests/Features/Onboarding/OnboardingViewModelTests.swift"
      provides: "3 testy: grantAccessSuccess, grantAccessFailure, isRequestingStartsFalse"
      contains: "final class OnboardingViewModelTests"
    - path: "DeluluDetoxTests/Features/Denial/DenialViewModelTests.swift"
      provides: "3 testy: retrySuccess, retryFailure, isRequestingStartsFalse"
      contains: "final class DenialViewModelTests"
    - path: "DeluluDetoxTests/Features/Home/HomeViewModelTests.swift"
      provides: "1 test: chooseAppsTapped is no-op in Phase 1 (D-15)"
      contains: "final class HomeViewModelTests"
  key_links:
    - from: "AppRootViewModelTests.testApprovedEmissionRoutesToHome"
      to: "MockObserveScreenTimeAuthStatusUseCase.subject.send(.approved)"
      via: "CurrentValueSubject controlled z testu → VM .sink aktualizuje destination → try await Task.yield() → XCTAssertEqual(vm.destination, .home)"
      pattern: "subject\\.send\\(\\.approved\\)"
    - from: "AppRootViewModelTests.testRefreshCallsUseCase"
      to: "MockRefreshScreenTimeAuthStatusUseCase.callCount"
      via: "vm.refreshStatus() → mock UC callCount inkrementowany"
      pattern: "refreshCallCount"
---

<objective>
Migracja testów do **mirror layout** zgodnie z D-27, D-28. Przenieść istniejący `OnboardingViewModelTests.swift` do `DeluluDetoxTests/Features/Onboarding/`. Przenieść `MockScreenTimeAuthRepository.swift` do `DeluluDetoxTests/Features/Onboarding/Mocks/` (feature-owner mock location per D-28 — ta sama reguła co source-side: shared → feature-owner; zero dualizmu reguł per `<specifics>` user feedback #2). Dodać 3 brakujące mocki UC (Request, Observe, Refresh) — potrzebne dla AppRootVM tests. Odtworzyć `AppRootViewModelTests.swift` w nowym layoucie testujący Destination + Combine flow + refreshStatus. Dopisać `DenialViewModelTests` i `HomeViewModelTests` (w Phase 1 nie istniały).

Purpose: Zapewnić że nowa architektura jest pokryta testami na poziomie Phase 1 lub lepszym. Wave 4 usunął stary AppRootViewModelTests — tutaj tworzymy nowy, który testuje Combine emission → destination update (co jest **sercem core-value protection** przez observe flow).

**B5 note:** `AppRootViewModel.Destination` ma już explicit `: Equatable` conformance dodane w Wave 4 (plan 01.1-04 Task 1). Ten plan **NIE** modyfikuje source-side AppRootViewModel.swift — `files_modified` zawiera tylko pliki testowe. Dead branch z wcześniejszej wersji ("jeśli okaże się że Equatable nie jest autoderived, dodaj ręcznie") jest usunięty z Task 2 poniżej.

**W12 note:** Testy AppRootVM które obserwują Combine emisje (`subject.send(...)` → asercja `vm.destination`) używają `try await Task.yield()` bezpośrednio przed asercją. Combine sink na MainActor + Observation tracking wymagają co najmniej jednego run-loop ticku żeby mutacja rozpropagowała się przed asercją. Documentowane jednym zdaniem na górze `AppRootViewModelTests.swift`.

**W14 note:** Przed move plików testowych Task 2 weryfikuje że `project.yml` DeluluDetoxTests target ma wildcard `sources:` — jeśli explicit file list, `xcodegen generate` nie złapie nowych plików. Projekt już to ma (verified), ale sanity-check przed move zapobiega debug w trakcie.

Output: 8 nowych plików w `DeluluDetoxTests/Features/`, 2 stare pliki usunięte, stary katalog `Mocks/` zniknięty.
</objective>

<execution_context>
@/Users/swojtyna/Documents/code/swojtyna/KKSW---first-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/swojtyna/Documents/code/swojtyna/KKSW---first-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md
@.planning/phases/01.1-architecture-foundation/01.1-PATTERNS.md
@.planning/phases/01.1-architecture-foundation/01.1-04-SUMMARY.md
@.claude/guides/dependency-injection/GUIDE.md
@.claude/guides/feature-structure/GUIDE.md

<interfaces>
<!-- VM-y do testowania (finalne po Wave 4): -->

```swift
@MainActor @Observable final class AppRootViewModel: @unchecked Sendable {
    @CasePathable enum Destination: Equatable { case onboarding, denial, home }  // Equatable dodane w Wave 4 (B5)
    var destination: Destination?
    @ObservationIgnored @LazyInjected private var observeStatus: ObserveScreenTimeAuthStatusUseCase
    @ObservationIgnored @LazyInjected private var refreshStatusUseCase: RefreshScreenTimeAuthStatusUseCase
    @ObservationIgnored private var cancellables: Set<AnyCancellable> = []
    init()  // subskrybuje observe() → destination = map(status)
    func refreshStatus()  // wywołuje refreshStatusUseCase()
}

@MainActor @Observable final class OnboardingViewModel: @unchecked Sendable {
    private(set) var isRequesting: Bool
    private(set) var error: String?
    @ObservationIgnored @LazyInjected private var requestAuth: RequestScreenTimeAuthUseCase
    init()
    func grantAccessTapped() async
}

@MainActor @Observable final class DenialViewModel: @unchecked Sendable {
    private(set) var isRequesting: Bool
    @ObservationIgnored @LazyInjected private var requestAuth: RequestScreenTimeAuthUseCase
    init()
    func retryTapped() async
}

@Observable final class HomeViewModel {
    init()
    func chooseAppsTapped()  // no-op w Phase 1 per D-15
}
```

<!-- Protokoły UC do mockowania: -->

```swift
protocol RequestScreenTimeAuthUseCase: Sendable { func callAsFunction() async throws }
protocol ObserveScreenTimeAuthStatusUseCase: Sendable { func callAsFunction() -> AnyPublisher<AuthorizationStatus, Never> }
protocol RefreshScreenTimeAuthStatusUseCase: Sendable { func callAsFunction() }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stwórz 4 mocki w mirror layout (feature-owner = Onboarding, D-28)</name>

  <read_first>
    - `DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift` (po Wave 4 — bez `authorizationStatus`, z `statusPublisher` + `refreshStatus`)
    - `DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift` (protokół referencyjny)
    - `DeluluDetox/Sources/Features/Onboarding/UseCase/RequestScreenTimeAuthUseCase.swift`
    - `DeluluDetox/Sources/Features/Onboarding/UseCase/ObserveScreenTimeAuthStatusUseCase.swift`
    - `DeluluDetox/Sources/Features/Onboarding/UseCase/RefreshScreenTimeAuthStatusUseCase.swift`
    - `.planning/phases/01.1-architecture-foundation/01.1-PATTERNS.md` §2.8 (mock struktura — `stubbedX`, `callCount`, `CurrentValueSubject` dla Observe UC)
    - `.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md` D-27, D-28
  </read_first>

  <files>
    DeluluDetoxTests/Features/Onboarding/Mocks/MockScreenTimeAuthRepository.swift (NEW — przeniesiony + rozszerzony)
    DeluluDetoxTests/Features/Onboarding/Mocks/MockRequestScreenTimeAuthUseCase.swift (NEW)
    DeluluDetoxTests/Features/Onboarding/Mocks/MockObserveScreenTimeAuthStatusUseCase.swift (NEW)
    DeluluDetoxTests/Features/Onboarding/Mocks/MockRefreshScreenTimeAuthStatusUseCase.swift (NEW)
    DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift (DELETE)
  </files>

  <action>
    1. Utwórz katalog: `mkdir -p DeluluDetoxTests/Features/Onboarding/Mocks`.

    2. **Stwórz `DeluluDetoxTests/Features/Onboarding/Mocks/MockScreenTimeAuthRepository.swift`** — przeniesiony + zsynchronizowany z protokołem po Wave 4:

    ```swift
    import Combine
    import FamilyControls
    @testable import DeluluDetox

    final class MockScreenTimeAuthRepository: ScreenTimeAuthRepository, @unchecked Sendable {
        var stubbedStatus: AuthorizationStatus = .notDetermined {
            didSet { statusSubject.send(stubbedStatus) }
        }
        var requestAuthorizationError: Error?
        var requestAuthorizationCallCount = 0
        var refreshStatusCallCount = 0

        private let statusSubject: CurrentValueSubject<AuthorizationStatus, Never>

        var statusPublisher: AnyPublisher<AuthorizationStatus, Never> {
            statusSubject.eraseToAnyPublisher()
        }

        init(initialStatus: AuthorizationStatus = .notDetermined) {
            self.stubbedStatus = initialStatus
            self.statusSubject = CurrentValueSubject(initialStatus)
        }

        func requestAuthorization() async throws {
            requestAuthorizationCallCount += 1
            if let error = requestAuthorizationError {
                throw error
            }
            stubbedStatus = .approved
        }

        func refreshStatus() {
            refreshStatusCallCount += 1
            statusSubject.send(stubbedStatus)
        }
    }
    ```

    3. **Stwórz `DeluluDetoxTests/Features/Onboarding/Mocks/MockRequestScreenTimeAuthUseCase.swift`**:

    ```swift
    @testable import DeluluDetox

    final class MockRequestScreenTimeAuthUseCase: RequestScreenTimeAuthUseCase, @unchecked Sendable {
        var stubbedError: Error?
        var callCount = 0

        func callAsFunction() async throws {
            callCount += 1
            if let error = stubbedError {
                throw error
            }
        }
    }
    ```

    4. **Stwórz `DeluluDetoxTests/Features/Onboarding/Mocks/MockObserveScreenTimeAuthStatusUseCase.swift`** — z publicznym `subject` żeby testy mogły kontrolować emisje:

    ```swift
    import Combine
    import FamilyControls
    @testable import DeluluDetox

    final class MockObserveScreenTimeAuthStatusUseCase: ObserveScreenTimeAuthStatusUseCase, @unchecked Sendable {
        /// Publicly exposed subject — tests call `.send(...)` to emit new statuses.
        let subject: CurrentValueSubject<AuthorizationStatus, Never>
        var callCount = 0

        init(initialStatus: AuthorizationStatus = .notDetermined) {
            self.subject = CurrentValueSubject(initialStatus)
        }

        func callAsFunction() -> AnyPublisher<AuthorizationStatus, Never> {
            callCount += 1
            return subject.eraseToAnyPublisher()
        }
    }
    ```

    5. **Stwórz `DeluluDetoxTests/Features/Onboarding/Mocks/MockRefreshScreenTimeAuthStatusUseCase.swift`**:

    ```swift
    @testable import DeluluDetox

    final class MockRefreshScreenTimeAuthStatusUseCase: RefreshScreenTimeAuthStatusUseCase, @unchecked Sendable {
        var callCount = 0

        func callAsFunction() {
            callCount += 1
        }
    }
    ```

    6. **USUŃ stary `DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift`**:
       - `rm DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift`
       - `rmdir DeluluDetoxTests/Mocks` (jeśli pusty)
  </action>

  <verify>
    <automated>
      # 4 mocki istnieją w mirror layout
      test -f DeluluDetoxTests/Features/Onboarding/Mocks/MockScreenTimeAuthRepository.swift
      test -f DeluluDetoxTests/Features/Onboarding/Mocks/MockRequestScreenTimeAuthUseCase.swift
      test -f DeluluDetoxTests/Features/Onboarding/Mocks/MockObserveScreenTimeAuthStatusUseCase.swift
      test -f DeluluDetoxTests/Features/Onboarding/Mocks/MockRefreshScreenTimeAuthStatusUseCase.swift
      # Stary mock i katalog znikły
      test ! -f DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift
      # Protocol conformance
      grep -q ": ScreenTimeAuthRepository" DeluluDetoxTests/Features/Onboarding/Mocks/MockScreenTimeAuthRepository.swift
      grep -q ": RequestScreenTimeAuthUseCase" DeluluDetoxTests/Features/Onboarding/Mocks/MockRequestScreenTimeAuthUseCase.swift
      grep -q ": ObserveScreenTimeAuthStatusUseCase" DeluluDetoxTests/Features/Onboarding/Mocks/MockObserveScreenTimeAuthStatusUseCase.swift
      grep -q ": RefreshScreenTimeAuthStatusUseCase" DeluluDetoxTests/Features/Onboarding/Mocks/MockRefreshScreenTimeAuthStatusUseCase.swift
      # MockObserveUC ma publicznie dostępny subject
      grep -q "let subject: CurrentValueSubject" DeluluDetoxTests/Features/Onboarding/Mocks/MockObserveScreenTimeAuthStatusUseCase.swift
    </automated>
  </verify>

  <acceptance_criteria>
    - `test -f DeluluDetoxTests/Features/Onboarding/Mocks/MockScreenTimeAuthRepository.swift` → **exit 0**
    - `test -f DeluluDetoxTests/Features/Onboarding/Mocks/MockRequestScreenTimeAuthUseCase.swift` → **exit 0**
    - `test -f DeluluDetoxTests/Features/Onboarding/Mocks/MockObserveScreenTimeAuthStatusUseCase.swift` → **exit 0**
    - `test -f DeluluDetoxTests/Features/Onboarding/Mocks/MockRefreshScreenTimeAuthStatusUseCase.swift` → **exit 0**
    - `test ! -f DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift` → **exit 0**
    - `grep -c ": ScreenTimeAuthRepository" DeluluDetoxTests/Features/Onboarding/Mocks/MockScreenTimeAuthRepository.swift` → **≥ 1**
    - `grep -c ": RequestScreenTimeAuthUseCase" DeluluDetoxTests/Features/Onboarding/Mocks/MockRequestScreenTimeAuthUseCase.swift` → **≥ 1**
    - `grep -c ": ObserveScreenTimeAuthStatusUseCase" DeluluDetoxTests/Features/Onboarding/Mocks/MockObserveScreenTimeAuthStatusUseCase.swift` → **≥ 1**
    - `grep -c ": RefreshScreenTimeAuthStatusUseCase" DeluluDetoxTests/Features/Onboarding/Mocks/MockRefreshScreenTimeAuthStatusUseCase.swift` → **≥ 1**
    - `grep -c "let subject: CurrentValueSubject" DeluluDetoxTests/Features/Onboarding/Mocks/MockObserveScreenTimeAuthStatusUseCase.swift` → **≥ 1** (kontrola z testu)
    - `grep -c "var callCount" DeluluDetoxTests/Features/Onboarding/Mocks/MockRefreshScreenTimeAuthStatusUseCase.swift` → **≥ 1**
  </acceptance_criteria>

  <done>
    4 mocki w mirror layout pod feature-ownerem (Onboarding). Wszystkie zgodne z protokołami po Wave 4. MockObserveUC eksponuje `subject` dla kontroli emisji z testu. Stary `Mocks/` usunięty.
  </done>
</task>

<task type="auto">
  <name>Task 2: Stwórz AppRootViewModelTests + DenialViewModelTests + HomeViewModelTests + zmigrowany OnboardingViewModelTests</name>

  <read_first>
    - `DeluluDetoxTests/OnboardingViewModelTests.swift` (po Wave 4 — DIContainer.reset+register w setUp, bez onAuthorized)
    - `DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` (po Wave 4 — finalny, referencyjny kontrakt z `enum Destination: Equatable` jużdodaną)
    - `DeluluDetox/Sources/Features/Onboarding/ViewModel/OnboardingViewModel.swift` (po Wave 4)
    - `DeluluDetox/Sources/Features/Denial/ViewModel/DenialViewModel.swift` (po Wave 4)
    - `DeluluDetox/Sources/Features/Home/ViewModel/HomeViewModel.swift` (empty VM per D-15)
    - `project.yml` — konkretnie sekcja DeluluDetoxTests (aby zweryfikować wildcard sources przed move — W14)
    - `.planning/phases/01.1-architecture-foundation/01.1-PATTERNS.md` §2.8 (DIContainer.reset + register w setUp, test pattern)
    - `.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md` D-29 (reset() w setUp)
  </read_first>

  <files>
    DeluluDetoxTests/Features/Onboarding/OnboardingViewModelTests.swift (NEW — migrated + rewrite)
    DeluluDetoxTests/Features/Denial/DenialViewModelTests.swift (NEW)
    DeluluDetoxTests/Features/Home/HomeViewModelTests.swift (NEW)
    DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift (NEW — odtworzony po Wave 4)
    DeluluDetoxTests/OnboardingViewModelTests.swift (DELETE)
  </files>

  <action>
    0. **W14 pre-flight — project.yml wildcard sanity check** (wykonaj PRZED tworzeniem nowych plików/move'ów):
       - Zwizualizuj sekcję `DeluluDetoxTests:` w `project.yml`.
       - DeluluDetoxTests target `sources:` MUSI być wildcard path (typowo `- path: DeluluDetoxTests`), NIE explicit file list.
       - Acceptance: `grep -A3 "^  DeluluDetoxTests:" project.yml | grep -c "^      - path: DeluluDetoxTests$"` → **≥ 1**.
       - Jeśli current `sources:` jest explicit file list (np. `- DeluluDetoxTests/FooTests.swift`):
         - **STOP** — ręcznie edytuj `project.yml` żeby DeluluDetoxTests `sources:` był wildcard `- path: DeluluDetoxTests`.
         - Uruchom `xcodegen generate` po edycji i zweryfikuj że projekt się buduje.
         - Zapisz zmianę w SUMMARY jako "W14: project.yml DeluluDetoxTests sources wildcardized".
       - Jeśli current `sources:` już wildcard — zapisz w SUMMARY "W14: project.yml wildcard verified, no change".

    1. Utwórz katalogi:
       - `mkdir -p DeluluDetoxTests/Features/Onboarding`
       - `mkdir -p DeluluDetoxTests/Features/Denial`
       - `mkdir -p DeluluDetoxTests/Features/Home`
       - `mkdir -p DeluluDetoxTests/Features/Root`

    2. **Stwórz `DeluluDetoxTests/Features/Onboarding/OnboardingViewModelTests.swift`** — rewrite używający MockRequestUC bezpośrednio (zamiast pełnego stacka Repo→Impl UC):

    ```swift
    import XCTest
    @testable import DeluluDetox

    @MainActor
    final class OnboardingViewModelTests: XCTestCase {
        // IUO pattern is safe in XCTest: setUp runs before every test and assigns the value; tearDown nils it.
        // Standard idiom — see `.claude/guides` for rationale.
        var mockUseCase: MockRequestScreenTimeAuthUseCase!

        override func setUp() {
            super.setUp()
            DIContainer.shared.reset()
            mockUseCase = MockRequestScreenTimeAuthUseCase()
            DIContainer.shared.register(RequestScreenTimeAuthUseCase.self, scope: .unique) { [mockUseCase] _ in
                mockUseCase!
            }
        }

        func testGrantAccessSuccess() async {
            let vm = OnboardingViewModel()

            await vm.grantAccessTapped()

            XCTAssertEqual(mockUseCase.callCount, 1)
            XCTAssertNil(vm.error)
            XCTAssertFalse(vm.isRequesting)
        }

        func testGrantAccessFailure() async {
            mockUseCase.stubbedError = NSError(
                domain: "FamilyControls", code: 2,
                userInfo: [NSLocalizedDescriptionKey: "Invalid account type"]
            )
            let vm = OnboardingViewModel()

            await vm.grantAccessTapped()

            XCTAssertEqual(mockUseCase.callCount, 1)
            XCTAssertNotNil(vm.error)
            XCTAssertEqual(vm.error, "Invalid account type")
            XCTAssertFalse(vm.isRequesting)
        }

        func testIsRequestingStartsFalse() async {
            let vm = OnboardingViewModel()
            XCTAssertFalse(vm.isRequesting)
        }
    }
    ```

    3. **Stwórz `DeluluDetoxTests/Features/Denial/DenialViewModelTests.swift`** — analog Onboarding (Denial używa tego samego RequestUC; mock żyje w Onboarding/Mocks per D-28):

    ```swift
    import XCTest
    @testable import DeluluDetox

    @MainActor
    final class DenialViewModelTests: XCTestCase {
        // IUO safe in XCTest: setUp runs before every test (W16).
        var mockUseCase: MockRequestScreenTimeAuthUseCase!

        override func setUp() {
            super.setUp()
            DIContainer.shared.reset()
            mockUseCase = MockRequestScreenTimeAuthUseCase()
            // Denial consumes UC registered by OnboardingInjection (D-17).
            // In tests we register directly — same protocol ID, same mock instance.
            DIContainer.shared.register(RequestScreenTimeAuthUseCase.self, scope: .unique) { [mockUseCase] _ in
                mockUseCase!
            }
        }

        func testRetrySuccess() async {
            let vm = DenialViewModel()

            await vm.retryTapped()

            XCTAssertEqual(mockUseCase.callCount, 1)
            XCTAssertFalse(vm.isRequesting)
        }

        func testRetryFailure() async {
            mockUseCase.stubbedError = NSError(
                domain: "FamilyControls", code: 3,
                userInfo: [NSLocalizedDescriptionKey: "Prompt dismissed"]
            )
            let vm = DenialViewModel()

            await vm.retryTapped()

            XCTAssertEqual(mockUseCase.callCount, 1)
            XCTAssertFalse(vm.isRequesting)
        }

        func testIsRequestingStartsFalse() async {
            let vm = DenialViewModel()
            XCTAssertFalse(vm.isRequesting)
        }
    }
    ```

    4. **Stwórz `DeluluDetoxTests/Features/Home/HomeViewModelTests.swift`** — trywialny test dla pustego VM (D-15):

    ```swift
    import XCTest
    @testable import DeluluDetox

    final class HomeViewModelTests: XCTestCase {
        func testChooseAppsTappedIsNoOpInPhase1() {
            let vm = HomeViewModel()
            // No-op in Phase 1 per D-15 — simply ensure it doesn't crash.
            vm.chooseAppsTapped()
            // No observable state to assert on; HomeVM is empty per design.
        }
    }
    ```

    5. **Stwórz `DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift`** — nowe testy pokrywające finalny AppRootViewModel (Destination + Combine + refreshStatus). To jest **kluczowy test pokrywający core-value logic**.

    **W12 uwaga w nagłówku pliku** — Combine sink + Observation mutation propagation wymagają run-loop ticku. Każdy test który robi `subject.send(...)` a potem asertuje na `vm.destination` MUSI yield'ować `try await Task.yield()` (lub `try await Task.sleep(for: .milliseconds(1))`) bezpośrednio przed asercją:

    ```swift
    // AppRootViewModelTests — Combine-driven destination flow.
    //
    // NOTE (W12 flakiness mitigation): every test that calls `mockObserve.subject.send(...)`
    // followed by an assertion on `vm.destination` inserts `try await Task.yield()` immediately
    // before the assertion. Combine .sink + Observation mutation propagation need at least one
    // run-loop tick on MainActor before the new value is visible to assertions.
    import XCTest
    import Combine
    import FamilyControls
    @testable import DeluluDetox

    @MainActor
    final class AppRootViewModelTests: XCTestCase {
        // IUO safe in XCTest: setUp runs before every test (W16).
        var mockObserve: MockObserveScreenTimeAuthStatusUseCase!
        var mockRefresh: MockRefreshScreenTimeAuthStatusUseCase!

        override func setUp() {
            super.setUp()
            DIContainer.shared.reset()
            mockObserve = MockObserveScreenTimeAuthStatusUseCase(initialStatus: .notDetermined)
            mockRefresh = MockRefreshScreenTimeAuthStatusUseCase()
            DIContainer.shared.register(ObserveScreenTimeAuthStatusUseCase.self, scope: .unique) { [mockObserve] _ in
                mockObserve!
            }
            DIContainer.shared.register(RefreshScreenTimeAuthStatusUseCase.self, scope: .unique) { [mockRefresh] _ in
                mockRefresh!
            }
        }

        func testInitialDestinationForNotDetermined() async throws {
            let vm = AppRootViewModel()
            try await Task.yield()
            // CurrentValueSubject initial .notDetermined → destination = .onboarding
            XCTAssertEqual(vm.destination, .onboarding)
        }

        func testInitialDestinationForApproved() async throws {
            mockObserve = MockObserveScreenTimeAuthStatusUseCase(initialStatus: .approved)
            DIContainer.shared.register(ObserveScreenTimeAuthStatusUseCase.self, scope: .unique) { [mockObserve] _ in
                mockObserve!
            }

            let vm = AppRootViewModel()
            try await Task.yield()

            XCTAssertEqual(vm.destination, .home)
        }

        func testEmissionOfApprovedRoutesToHome() async throws {
            let vm = AppRootViewModel()
            try await Task.yield()
            XCTAssertEqual(vm.destination, .onboarding)

            mockObserve.subject.send(.approved)
            try await Task.yield()

            XCTAssertEqual(vm.destination, .home)
        }

        func testEmissionOfDeniedRoutesToDenial() async throws {
            let vm = AppRootViewModel()

            mockObserve.subject.send(.denied)
            try await Task.yield()

            XCTAssertEqual(vm.destination, .denial)
        }

        func testRefreshStatusCallsUseCase() {
            let vm = AppRootViewModel()
            XCTAssertEqual(mockRefresh.callCount, 0)

            vm.refreshStatus()

            XCTAssertEqual(mockRefresh.callCount, 1)
        }

        func testMultipleEmissionsUpdateDestination() async throws {
            let vm = AppRootViewModel()

            mockObserve.subject.send(.approved)
            try await Task.yield()
            XCTAssertEqual(vm.destination, .home)

            mockObserve.subject.send(.denied)
            try await Task.yield()
            XCTAssertEqual(vm.destination, .denial)

            mockObserve.subject.send(.notDetermined)
            try await Task.yield()
            XCTAssertEqual(vm.destination, .onboarding)
        }
    }
    ```

    **B5 update:** `AppRootViewModel.Destination` ma już explicit `: Equatable` (dodane w Wave 4 plan 01.1-04). **Ten task NIE modyfikuje `AppRootViewModel.swift`** — branch "jeśli Equatable nie jest autoderived, dodaj ręcznie" jest usunięty. `files_modified` frontmatter tego planu zawiera TYLKO pliki testowe.

    6. **USUŃ stary `DeluluDetoxTests/OnboardingViewModelTests.swift`**:
       - `rm DeluluDetoxTests/OnboardingViewModelTests.swift`

    7. Build + test:
       - `xcodegen generate` (wildcard katalogowy `DeluluDetoxTests` — zweryfikowany w kroku 0 — łapie nowe podkatalogi automatycznie)
       - XcodeBuildMCP `build_sim` scheme=DeluluDetox → exit 0
       - XcodeBuildMCP `test_sim` scheme=DeluluDetox → **wszystkie zielone**:
         - AppRootViewModelTests (6/6)
         - OnboardingViewModelTests (3/3)
         - DenialViewModelTests (3/3)
         - HomeViewModelTests (1/1)
         - DeluluDetoxTests placeholder (1/1)
         - **łącznie 14/14 passed** (przyrost vs Phase 1: 7 testów → 14 testów; AppRootVM + Denial + Home dodane, Onboarding zachowany w nowej formie)

    8. **Flaky test remediation (jeśli potrzebne):** jeśli jakikolwiek Combine-dependent test pokazuje nietrwałe niepowodzenie (raz zielony, raz czerwony w kilku runach), zamień `try await Task.yield()` na `try await Task.sleep(for: .milliseconds(1))` w tym konkretnym teście. Nie zwiększaj sleep timu — 1ms wystarcza i nie wydłuża znacząco suite'u.
  </action>

  <verify>
    <automated>
      # W14 project.yml wildcard (sanity-check musi być pozytywny)
      grep -A3 "^  DeluluDetoxTests:" project.yml | grep -q "^      - path: DeluluDetoxTests$"
      # Nowe test files
      test -f DeluluDetoxTests/Features/Onboarding/OnboardingViewModelTests.swift
      test -f DeluluDetoxTests/Features/Denial/DenialViewModelTests.swift
      test -f DeluluDetoxTests/Features/Home/HomeViewModelTests.swift
      test -f DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift
      # Stare płaskie pliki usunięte
      test ! -f DeluluDetoxTests/OnboardingViewModelTests.swift
      test ! -f DeluluDetoxTests/AppRootViewModelTests.swift
      # DIContainer.reset() w każdym test case'ie używającym @LazyInjected
      grep -q "DIContainer.shared.reset()" DeluluDetoxTests/Features/Onboarding/OnboardingViewModelTests.swift
      grep -q "DIContainer.shared.reset()" DeluluDetoxTests/Features/Denial/DenialViewModelTests.swift
      grep -q "DIContainer.shared.reset()" DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift
      # HomeVM test nie potrzebuje reset() (brak @LazyInjected)
      # Importy Combine w RootTests (dla subject.send)
      grep -q "import Combine" DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift
      # Asercje na destination enum (Combine flow)
      grep -q "vm.destination" DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift
      grep -q "subject.send" DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift
      # Asercje callCount dla RefreshUC (D-13/D-14 konsumpcja)
      grep -q "mockRefresh.callCount" DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift
      # W12 flakiness mitigation present
      grep -q "Task.yield" DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift
    </automated>
  </verify>

  <acceptance_criteria>
    - **W14 pre-flight:** `grep -A3 "^  DeluluDetoxTests:" project.yml | grep -c "^      - path: DeluluDetoxTests$"` → **≥ 1** (wildcard confirmed pre-move)
    - `ls DeluluDetoxTests/Features/Onboarding/OnboardingViewModelTests.swift` → **exit 0**
    - `ls DeluluDetoxTests/Features/Denial/DenialViewModelTests.swift` → **exit 0**
    - `ls DeluluDetoxTests/Features/Home/HomeViewModelTests.swift` → **exit 0**
    - `ls DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift` → **exit 0**
    - `test ! -f DeluluDetoxTests/OnboardingViewModelTests.swift` → **exit 0**
    - `test ! -f DeluluDetoxTests/AppRootViewModelTests.swift` → **exit 0**
    - `grep -c "DIContainer.shared.reset()" DeluluDetoxTests/Features/Onboarding/OnboardingViewModelTests.swift` → **≥ 1** (D-29)
    - `grep -c "DIContainer.shared.reset()" DeluluDetoxTests/Features/Denial/DenialViewModelTests.swift` → **≥ 1**
    - `grep -c "DIContainer.shared.reset()" DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift` → **≥ 1**
    - `grep -c "subject.send" DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift` → **≥ 3** (.approved, .denied, .notDetermined lub podobne emisje)
    - `grep -c "mockRefresh.callCount" DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift` → **≥ 1** (D-13/D-14 test)
    - `grep -c "XCTAssertEqual(vm.destination" DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift` → **≥ 4** (testy multiple emisji + initial)
    - **W12 flakiness mitigation:**
      - `grep -c "Task.yield\|Task.sleep" DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift` → **≥ 4** (każdy Combine-emission-then-assert test ma yield/sleep przed asercją)
      - `grep -q "W12 flakiness\|run-loop tick" DeluluDetoxTests/Features/Root/AppRootViewModelTests.swift` → **exit 0** (documenting comment na górze pliku)
    - **W16 IUO comment:** `grep -c "IUO safe\|IUO pattern is safe\|setUp runs before every test" DeluluDetoxTests/Features/` (recursive) → **≥ 1** (at least one file documents the pattern)
    - `grep -c "DependencyContainer\|onAuthorized" DeluluDetoxTests/` (recursive) → **0** (zero starych referencji)
    - **Ten plan NIE modyfikuje `DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` (B5 — Equatable już dodane w Wave 4):**
      - `git diff DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` (w ramach tego planu — diff vs end-of-Wave-4) → **pusty** (ten plan NIE dotyka source-side VM; only test files modified)
    - XcodeBuildMCP `test_sim` scheme=DeluluDetox → **≥ 14 tests passed** (6 AppRootVM + 3 Onboarding + 3 Denial + 1 Home + 1 placeholder; akceptowalne minimum 12 jeśli 1-2 edge testy Root okażą się redundantne — ale NIE mniej niż 8 = Phase 1 count).
    - **VM→UseCase rule (greps, must = 0):**
      - `grep -rc "import.*ScreenTimeAuthRepository\|: ScreenTimeAuthRepository\b" DeluluDetoxTests/Features/` → **0 hits w test files** (testy też nie dotykają Repository bezpośrednio — wszystko przez UC mocki; ALE mocki w `Mocks/` SĄ dozwolone i mają `: ScreenTimeAuthRepository` — grep jest nad `*VMTests.swift`, nie nad `Mocks/`. Poprawka: `grep -c "import.*Repository" DeluluDetoxTests/Features/Onboarding/OnboardingViewModelTests.swift` → **0**)
  </acceptance_criteria>

  <done>
    4 nowe VMTests w mirror layout + 4 mocki (z Task 1) = pełne pokrycie architektury 01.1. AppRootVMTests weryfikuje core-value flow (Combine emission → destination update + refreshStatus → UC call) z flakiness mitigation (W12 `Task.yield()`). DenialVMTests + HomeVMTests nowe (w Phase 1 nie istniały). IUO pattern udokumentowany (W16). Build + min 14 testów zielonych. Zero referencji do DependencyContainer/onAuthorized w całym `DeluluDetoxTests/`. `project.yml` wildcard weryfikowany pre-move (W14). Ten plan NIE dotknął source AppRootViewModel — Equatable już w Wave 4 (B5).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Brak nowych trust boundaries w tym planie. Test targety są izolowane od release build — `@testable import DeluluDetox` istnieje wyłącznie w DeluluDetoxTests target, nie wycieka do app bundle.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-17 | Tampering | DIContainer.shared jest globalny — test A rejestruje mock, test B dziedziczy stan | mitigate | D-29: `DIContainer.shared.reset()` w setUp każdego test case'a. Egzekwowane greppem w acceptance criteria. Brak `reset()` = test widzi rejestracje poprzedniego testu → race. |
| T-01.1-18 | Spoofing | MockObserveUC `subject` publicznie dostępny → przypadkowa mutacja z innego testu | accept | Subject jest instance property `MockObserveScreenTimeAuthStatusUseCase`. Każdy test tworzy własną instancję mocka w setUp. Po `reset()` stare mocki są zwalniane. Write-write race tylko gdy test async mutuje mock poza MainActor — wszystkie testy są `@MainActor`. |
| T-01.1-19 | DoS | Testy AppRootVMTests sprawdzające Combine emisje mogą być flaky bez run-loop ticku | **mitigate (W12)** | Każdy test który robi `subject.send(...)` a następnie asertuje `vm.destination` MUSI wstawić `try await Task.yield()` (lub 1ms sleep) tuż przed asercją. Wyjątek: `testRefreshStatusCallsUseCase` nie angażuje Combine emisji (tylko bezpośrednie wywołanie metody synchronicznej na VM). Pattern udokumentowany w komentarzu na górze `AppRootViewModelTests.swift`. |

Core value tests (T-01.1-16 z Wave 4): pokryte przez `testRefreshStatusCallsUseCase` + `testEmissionOfApprovedRoutesToHome` — oba weryfikują że Combine pipeline + refresh działają jak zaprojektowano. Test *nie* weryfikuje real-device scenePhase (wymagałby UI test + real simulator lifecycle) — to jest zakres Wave 6 human-verify checkpoint.
</threat_model>

<verification>
1. Mirror layout: `DeluluDetoxTests/Features/{Onboarding,Denial,Home,Root}/` istnieje; Mocks w `Onboarding/Mocks/`.
2. Stary flat layout usunięty: `DeluluDetoxTests/Mocks/` i płaskie `*ViewModelTests.swift` znikły.
3. DIContainer.reset() w setUp: OnboardingVMTests, DenialVMTests, AppRootVMTests (HomeVMTests nie potrzebuje — brak @LazyInjected).
4. 4 mocki: MockScreenTimeAuthRepository (repo — transitional używany w Mocks infra; nie w VM tests bo VM testy używają MockUC bezpośrednio), MockRequestUC, MockObserveUC (z publicznym subject), MockRefreshUC (z callCount).
5. AppRootVMTests testuje: initial .notDetermined → .onboarding; initial .approved → .home; emisja .approved → .home; emisja .denied → .denial; refreshStatus() inkrementuje callCount; multiple emisje aktualizują destination. Każdy test z emisją ma Task.yield() przed asercją (W12).
6. IUO `var mockX: MockX!` pattern udokumentowany komentarzem (W16) — standard XCTest idiom, safe bo setUp przypisuje przed testem.
7. project.yml DeluluDetoxTests `sources:` jest wildcardem (W14 pre-flight zweryfikowany w kroku 0 Task 2).
8. Ten plan NIE modyfikuje `AppRootViewModel.swift` — Equatable dodane w Wave 4 (B5).
9. Min 14 testów przechodzi zielono.
10. Zero `DependencyContainer`, `onAuthorized`, `authorizedCalled` w `DeluluDetoxTests/`.
</verification>

<success_criteria>
- 8 plików w `DeluluDetoxTests/Features/` (4 mocki + 4 VMTests).
- Mirror layout zgodny z D-27 (Features/<Feature>/ subdirs).
- Shared mocki w feature-ownerze per D-28 (Onboarding/Mocks/).
- DIContainer.reset() w setUp każdego test case'a z @LazyInjected (D-29).
- AppRootVMTests pokrywa Combine flow + refreshStatus (core-value test) z W12 flakiness mitigation (`try await Task.yield()` przed każdą asercją post-emisja).
- IUO pattern (`var mockX: MockX!`) udokumentowany w testach (W16).
- project.yml DeluluDetoxTests `sources:` wildcard zweryfikowany pre-move (W14).
- Wszystkie min 14 testów zielone.
- Żadnej referencji do DependencyContainer ani onAuthorized w testach.
- Ten plan NIE dotknął source `AppRootViewModel.swift` (B5 — Equatable jużw Wave 4).
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-architecture-foundation/01.1-05-SUMMARY.md` zawierający:
- Pełną listę NEW/DELETED plików testowych.
- Tabelę: każdy test → przechodzi/fails + liczba assercji.
- Wynik XcodeBuildMCP test_sim: **N/N passed** (ideał 14+).
- Potwierdzenie że decyzje D-27, D-28, D-29 spełnione.
- Lista grep'ów acceptance criteria z wynikami.
- W14 status: "project.yml wildcard verified" albo "project.yml sources wildcardized (previously explicit list)".
- W12 status: "Task.yield() present in N tests" + enumeracja testów które używają flakiness mitigation.
- W16 status: "IUO idiom comment present in test files".
- B5 status: "AppRootViewModel.swift untouched (Equatable w Wave 4)" → `git diff DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` against end-of-Wave-4 SHOULD BE pusty.
</output>
</content>
