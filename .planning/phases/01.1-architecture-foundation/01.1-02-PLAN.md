---
phase: 01.1-architecture-foundation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift
  - DeluluDetox/Sources/Features/Onboarding/UseCase/RequestScreenTimeAuthUseCase.swift
  - DeluluDetox/Sources/Features/Onboarding/UseCase/ObserveScreenTimeAuthStatusUseCase.swift
  - DeluluDetox/Sources/Features/Onboarding/UseCase/RefreshScreenTimeAuthStatusUseCase.swift
  - DeluluDetox/Sources/Features/Onboarding/Injection/OnboardingInjection.swift
  - DeluluDetox/Sources/Domain/Repositories/ScreenTimeAuthRepository.swift  # DELETED
  - DeluluDetox/Sources/Domain/UseCases/RequestScreenTimeAuthUseCase.swift  # DELETED
  - DeluluDetox/Sources/Data/Repositories/ScreenTimeAuthRepositoryImpl.swift  # DELETED
  - DeluluDetox/Sources/App/DependencyContainer.swift  # MODIFIED (transitional)
  - DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift  # MODIFIED (W9: keep conformance in same commit as protocol change)
autonomous: true
requirements: []  # Covers D-07, D-08, D-09, D-11, D-12, D-13, D-17, D-22 (Onboarding jako feature-owner ScreenTimeAuth*), D-20 (OnboardingInjection.register)
user_setup: []

must_haves:
  truths:
    - "`ScreenTimeAuthRepository` + impl żyją jako JEDEN plik w `Features/Onboarding/Repository/ScreenTimeAuthRepository.swift` (feature-first, protocol+impl w jednym pliku wg feature-structure GUIDE)."
    - "Repository ma `var statusPublisher: AnyPublisher<AuthorizationStatus, Never>` eksponujący aktualny status + każdą jego zmianę (D-07, D-08)."
    - "Repository ma `func refreshStatus()` który czyta `AuthorizationCenter.shared.authorizationStatus` i wysyła do `statusSubject.send(...)` (D-13)."
    - "`RequestScreenTimeAuthUseCase`, `ObserveScreenTimeAuthStatusUseCase`, `RefreshScreenTimeAuthStatusUseCase` są w `Features/Onboarding/UseCase/` (Onboarding = feature-owner; D-17)."
    - "`OnboardingInjection.register(in:)` rejestruje Repo (scope `.application`) + 3 UC (scope `.unique`) — D-22. Inne feature'y konsumują tylko UC, nigdy Repo (D-17)."
    - "Ownership semantics of D-17 preserved: Onboarding is the feature-owner of ScreenTimeAuthRepository and its UseCases regardless of Simple-vs-Split layout. Denial consumes only via UseCase, never the repository."
    - "Stare lokalizacje `Domain/Repositories/`, `Domain/UseCases/`, `Data/Repositories/` są usunięte."
    - "App nadal buduje się i Phase 1 testy (AppRootViewModelTests, OnboardingViewModelTests) nadal zielone — `DependencyContainer` przejściowo dostosowany żeby kompilował się z nowymi ścieżkami importu (nadal używa init injection, bez DIContainer bootstrap)."
    - "MockScreenTimeAuthRepository zaktualizowany w TYM SAMYM commicie żeby nadal conformował do rozszerzonego protokołu (W9) — żadnej przerwy w zielonym test target."
  artifacts:
    - path: "DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift"
      provides: "Protocol + Impl w jednym pliku; CurrentValueSubject; statusPublisher; refreshStatus(); requestAuthorization()"
      contains: "protocol ScreenTimeAuthRepository"
      also_contains: "final class ScreenTimeAuthRepositoryImpl"
      also_contains: "CurrentValueSubject<AuthorizationStatus, Never>"
      also_contains: "AnyPublisher<AuthorizationStatus, Never>"
    - path: "DeluluDetox/Sources/Features/Onboarding/UseCase/RequestScreenTimeAuthUseCase.swift"
      provides: "Request UC (protocol + Impl) — async throws"
      contains: "protocol RequestScreenTimeAuthUseCase"
    - path: "DeluluDetox/Sources/Features/Onboarding/UseCase/ObserveScreenTimeAuthStatusUseCase.swift"
      provides: "Observe UC (protocol + Impl) — zwraca AnyPublisher<AuthorizationStatus, Never>"
      contains: "protocol ObserveScreenTimeAuthStatusUseCase"
    - path: "DeluluDetox/Sources/Features/Onboarding/UseCase/RefreshScreenTimeAuthStatusUseCase.swift"
      provides: "Refresh UC (protocol + Impl) — sync, wywołuje repository.refreshStatus()"
      contains: "protocol RefreshScreenTimeAuthStatusUseCase"
    - path: "DeluluDetox/Sources/Features/Onboarding/Injection/OnboardingInjection.swift"
      provides: "Distributed registration: Repo + 3 UC"
      contains: "enum OnboardingInjection"
      also_contains: "static func register(in container: DIContainer)"
  key_links:
    - from: "ScreenTimeAuthRepositoryImpl.requestAuthorization()"
      to: "statusSubject.send(...)"
      via: "po udanym `AuthorizationCenter.shared.requestAuthorization(for: .individual)`"
      pattern: "statusSubject\\.send"
    - from: "ScreenTimeAuthRepositoryImpl.refreshStatus()"
      to: "statusSubject.send(AuthorizationCenter.shared.authorizationStatus)"
      via: "ręczne wywołanie z ViewModelu (Wave 5 scenePhase hook)"
      pattern: "statusSubject\\.send"
    - from: "ObserveScreenTimeAuthStatusUseCase.callAsFunction()"
      to: "repository.statusPublisher"
      via: "passthrough — UC jest cienką warstwą enkapsulacji"
      pattern: "repository\\.statusPublisher"
    - from: "OnboardingInjection.register(in:)"
      to: "DIContainer.register(..., scope: .application)"
      via: "factory dla Repo; UC z `.unique`"
      pattern: "scope: \\.application"
---

<objective>
Przeprowadzić migrację współdzielonych komponentów ScreenTimeAuth* do feature-first layoutu wg **feature-structure GUIDE Simple variant** (protocol+impl w jednym pliku, `UseCase/` bezpośrednio pod feature — nie `Common/`). Onboarding jest feature-ownerem — repo i wszystkie 3 UC żyją tutaj (D-17). Denial i Root będą konsumować UC z tego katalogu w kolejnych falach.

**Reconciliation D-17 (CONTEXT/PATTERNS) vs feature-structure GUIDE:** CONTEXT D-17 i PATTERNS §2.9 opisują *ownership semantics* wariantem Split (`Common/Repository/`, `Common/UseCase/`) — ale `feature-structure/GUIDE.md` (kanoniczne źródło zgodnie z CLAUDE.md Project Rules) definiuje dla Onboarding po 01.1 wariant **Simple** bez `Common/`. **Guide wygrywa**; D-17 `Common/` jest czytane jako ownership (Onboarding jest feature-ownerem), nie jako physical path. Używamy Simple layout — Onboarding wciąż jest feature-ownerem ScreenTimeAuth*, Denial konsumuje tylko przez UC (D-17 rule zachowana niezależnie od layoutu).

Zmienić repository z synchronicznego `var authorizationStatus: AuthorizationStatus` na Combine-based `statusPublisher: AnyPublisher<AuthorizationStatus, Never>` z CurrentValueSubject w środku (D-07, D-08, D-09). Dodać `refreshStatus()` (D-13) — to jest obrona core value (user cofa Screen Time w Settings → AppRoot wie bez konieczności restartu app).

Stworzyć `OnboardingInjection.swift` z `static func register(in container: DIContainer)` rejestrującym Repo (`.application`) + 3 UC (`.unique`). Plik jest gotowy do wywołania — ale **nikt go jeszcze nie woła**. Bootstrap w `DeluluDetoxApp.init()` pojawia się w Wave 5 (`01.1-05`). W tym planie `DependencyContainer` przejściowo zostaje żywy (używa nowych ścieżek importów; produkuje VM po staremu) żeby app nadal kompilował się.

**Co do `MockScreenTimeAuthRepository` (W9):** protokół dostaje 2 nowych członków (`statusPublisher`, `refreshStatus()`) — mock MUSI zostać zaktualizowany w tym samym commicie żeby test target nadal kompilował się między Wave 2 i Wave 3. Robimy to w Tasku 1 jako ostatni krok przed run testów.

Purpose: Odblokować Wave 3+. Wszystkie VM refactors (Onboarding, Denial, Root) zależą od istnienia UC w feature-first lokalizacji i zarejestrowanych w `OnboardingInjection`.

Output: 5 plików NEW w `Features/Onboarding/`, 3 stare pliki `Domain/Repositories/`, `Domain/UseCases/`, `Data/Repositories/` usunięte, `App/DependencyContainer.swift` zaktualizowany (przejściowy), `DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift` zaktualizowany (W9).

Jeśli przy implementacji pojawi się wątpliwość, executor ma zatrzymać się i zgłosić do usera **przed** rozpoczęciem refaktoru.
</objective>

<execution_context>
@/Users/swojtyna/Documents/code/swojtyna/KKSW---first-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/swojtyna/Documents/code/swojtyna/KKSW---first-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md
@.planning/phases/01.1-architecture-foundation/01.1-PATTERNS.md
@.planning/phases/01.1-architecture-foundation/01.1-01-SUMMARY.md
@.claude/guides/architecture/GUIDE.md
@.claude/guides/feature-structure/GUIDE.md
@.claude/guides/dependency-injection/GUIDE.md

<interfaces>
<!-- Źródłowy stan Phase 1 (do rozbiórki): -->

From DeluluDetox/Sources/Domain/Repositories/ScreenTimeAuthRepository.swift:
```swift
import FamilyControls

protocol ScreenTimeAuthRepository: Sendable {
    var authorizationStatus: AuthorizationStatus { get }
    func requestAuthorization() async throws
}
```

From DeluluDetox/Sources/Data/Repositories/ScreenTimeAuthRepositoryImpl.swift:
```swift
import FamilyControls

final class ScreenTimeAuthRepositoryImpl: ScreenTimeAuthRepository, @unchecked Sendable {
    var authorizationStatus: AuthorizationStatus {
        AuthorizationCenter.shared.authorizationStatus
    }

    func requestAuthorization() async throws {
        try await AuthorizationCenter.shared.requestAuthorization(for: .individual)
    }
}
```

From DeluluDetox/Sources/Domain/UseCases/RequestScreenTimeAuthUseCase.swift:
```swift
import FamilyControls

protocol RequestScreenTimeAuthUseCase: Sendable {
    func callAsFunction() async throws
}

final class RequestScreenTimeAuthUseCaseImpl: RequestScreenTimeAuthUseCase {
    private let repository: ScreenTimeAuthRepository
    init(repository: ScreenTimeAuthRepository)
    func callAsFunction() async throws
}
```

From DeluluDetox/Sources/App/DependencyContainer.swift (do aktualizacji — importy plików po przeniesieniu muszą nadal się resolvewać, bo to jest ten sam Swift module):
```swift
struct DependencyContainer {
    let screenTimeAuthRepository: ScreenTimeAuthRepository
    init(screenTimeAuthRepository: ScreenTimeAuthRepository = ScreenTimeAuthRepositoryImpl())
    func makeRequestScreenTimeAuthUseCase() -> RequestScreenTimeAuthUseCase
    func makeAppRootViewModel() -> AppRootViewModel
    func makeOnboardingViewModel(onAuthorized: (() -> Void)?) -> OnboardingViewModel
    func makeDenialViewModel(onAuthorized: (() -> Void)?) -> DenialViewModel
    func makeHomeViewModel() -> HomeViewModel
}
```

<!-- Protokół po migracji (docelowo, ten plan): -->

```swift
protocol ScreenTimeAuthRepository: Sendable {
    var statusPublisher: AnyPublisher<AuthorizationStatus, Never> { get }  // D-08
    func requestAuthorization() async throws
    func refreshStatus()                                                     // D-13
}
```

<!-- VMs (Onboarding, Denial, AppRoot) nadal używają `authorizationStatus` przez `DependencyContainer` w tym planie — VM refaktor jest Wave 3+, nie w tym planie.
     Dlatego repository w tym planie musi KOMPATYBILNIE wspierać obecnych konsumentów:
     - AppRootViewModel linia 26, 42: `container.screenTimeAuthRepository.authorizationStatus`
     - Żaden inny konsument `authorizationStatus` nie istnieje w Phase 1.
     Strategia: dodajemy w Impl `var authorizationStatus: AuthorizationStatus` jako computed property zwracające `AuthorizationCenter.shared.authorizationStatus` (dokładnie jak stare; NIE w protokole bo to zniknie w Wave 3 gdy AppRootVM przejdzie na Observe UC). Alternatywnie: zostawiamy `authorizationStatus` w protokole do Wave 3 i wtedy znika. Wybór: ZOSTAWIAMY w protokole jako deprecated transitional — usuwany w Wave 5 jako część atomic cleanup gdy AppRootVM przechodzi na Observe UC. Komentarz `// TODO(01.1-05): remove — replaced by statusPublisher` w protokole. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stwórz feature-first Repository i UseCase'y (move + extend) + zaktualizuj MockScreenTimeAuthRepository (W9)</name>

  <read_first>
    - `DeluluDetox/Sources/Domain/Repositories/ScreenTimeAuthRepository.swift` (6 linii — pełen plik)
    - `DeluluDetox/Sources/Data/Repositories/ScreenTimeAuthRepositoryImpl.swift` (11 linii — pełen plik)
    - `DeluluDetox/Sources/Domain/UseCases/RequestScreenTimeAuthUseCase.swift` (17 linii — pełen plik)
    - `DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift` (pełen plik — 20 linii; MUSI zostać zaktualizowany w tym tasku, W9)
    - `.planning/phases/01.1-architecture-foundation/01.1-PATTERNS.md` §2.4 (CurrentValueSubject w repository — gotowy snippet), §2.5 (ObserveScreenTimeAuthStatusUseCase — gotowy snippet), §2.9 (layout feature — WAŻNE: pomimo że PATTERNS pokazuje Split/Common, stosujemy Simple wg guide; patrz `<objective>` reconciliation paragraph)
    - `.claude/guides/feature-structure/GUIDE.md` linie 23-50 (Simple variant — kanoniczny layout dla Onboarding po 01.1)
    - `.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md` decyzje D-07, D-08, D-09, D-11, D-12, D-13, D-17
  </read_first>

  <files>
    DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift (NEW — protocol + impl w jednym pliku)
    DeluluDetox/Sources/Features/Onboarding/UseCase/RequestScreenTimeAuthUseCase.swift (NEW — move + rename lokalizacji)
    DeluluDetox/Sources/Features/Onboarding/UseCase/ObserveScreenTimeAuthStatusUseCase.swift (NEW)
    DeluluDetox/Sources/Features/Onboarding/UseCase/RefreshScreenTimeAuthStatusUseCase.swift (NEW)
    DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift (MODIFIED — W9, conformance z rozszerzonym protokołem)
    DeluluDetox/Sources/Domain/Repositories/ScreenTimeAuthRepository.swift (DELETE)
    DeluluDetox/Sources/Data/Repositories/ScreenTimeAuthRepositoryImpl.swift (DELETE)
    DeluluDetox/Sources/Domain/UseCases/RequestScreenTimeAuthUseCase.swift (DELETE)
  </files>

  <action>
    1. Utwórz katalogi:
       - `mkdir -p DeluluDetox/Sources/Features/Onboarding/Repository`
       - `mkdir -p DeluluDetox/Sources/Features/Onboarding/UseCase`

    2. **Stwórz `DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift`** (protocol + impl w jednym pliku — feature-structure GUIDE). Treść DOKŁADNIE:

    ```swift
    import Combine
    import FamilyControls

    // MARK: - Protocol

    protocol ScreenTimeAuthRepository: Sendable {
        var statusPublisher: AnyPublisher<AuthorizationStatus, Never> { get }

        // Transitional: kept for DependencyContainer synchronous wiring in Phase 1 VMs.
        // Removed in 01.1-04 when AppRootViewModel switches to ObserveScreenTimeAuthStatusUseCase.
        var authorizationStatus: AuthorizationStatus { get }

        func requestAuthorization() async throws
        func refreshStatus()
    }

    // MARK: - Implementation

    final class ScreenTimeAuthRepositoryImpl: ScreenTimeAuthRepository, @unchecked Sendable {
        private let statusSubject: CurrentValueSubject<AuthorizationStatus, Never>

        var statusPublisher: AnyPublisher<AuthorizationStatus, Never> {
            statusSubject.eraseToAnyPublisher()
        }

        var authorizationStatus: AuthorizationStatus {
            AuthorizationCenter.shared.authorizationStatus
        }

        init() {
            self.statusSubject = CurrentValueSubject(AuthorizationCenter.shared.authorizationStatus)
        }

        func requestAuthorization() async throws {
            try await AuthorizationCenter.shared.requestAuthorization(for: .individual)
            statusSubject.send(AuthorizationCenter.shared.authorizationStatus)
        }

        func refreshStatus() {
            statusSubject.send(AuthorizationCenter.shared.authorizationStatus)
        }
    }
    ```

    3. **Stwórz `DeluluDetox/Sources/Features/Onboarding/UseCase/RequestScreenTimeAuthUseCase.swift`** — kopia istniejącego pliku **bez zmian treści** (tylko przeniesienie lokalizacji):

    ```swift
    import FamilyControls

    protocol RequestScreenTimeAuthUseCase: Sendable {
        func callAsFunction() async throws
    }

    final class RequestScreenTimeAuthUseCaseImpl: RequestScreenTimeAuthUseCase {
        private let repository: ScreenTimeAuthRepository

        init(repository: ScreenTimeAuthRepository) {
            self.repository = repository
        }

        func callAsFunction() async throws {
            try await repository.requestAuthorization()
        }
    }
    ```

    4. **Stwórz `DeluluDetox/Sources/Features/Onboarding/UseCase/ObserveScreenTimeAuthStatusUseCase.swift`** (D-09, analog z PATTERNS §2.5):

    ```swift
    import Combine
    import FamilyControls

    protocol ObserveScreenTimeAuthStatusUseCase: Sendable {
        func callAsFunction() -> AnyPublisher<AuthorizationStatus, Never>
    }

    final class ObserveScreenTimeAuthStatusUseCaseImpl: ObserveScreenTimeAuthStatusUseCase {
        private let repository: ScreenTimeAuthRepository

        init(repository: ScreenTimeAuthRepository) {
            self.repository = repository
        }

        func callAsFunction() -> AnyPublisher<AuthorizationStatus, Never> {
            repository.statusPublisher
        }
    }
    ```

    5. **Stwórz `DeluluDetox/Sources/Features/Onboarding/UseCase/RefreshScreenTimeAuthStatusUseCase.swift`** (D-13):

    ```swift
    import FamilyControls

    protocol RefreshScreenTimeAuthStatusUseCase: Sendable {
        func callAsFunction()
    }

    final class RefreshScreenTimeAuthStatusUseCaseImpl: RefreshScreenTimeAuthStatusUseCase {
        private let repository: ScreenTimeAuthRepository

        init(repository: ScreenTimeAuthRepository) {
            self.repository = repository
        }

        func callAsFunction() {
            repository.refreshStatus()
        }
    }
    ```

    6. **Usuń stare pliki** (po potwierdzeniu że nowe istnieją):
       - `rm DeluluDetox/Sources/Domain/Repositories/ScreenTimeAuthRepository.swift`
       - `rm DeluluDetox/Sources/Data/Repositories/ScreenTimeAuthRepositoryImpl.swift`
       - `rm DeluluDetox/Sources/Domain/UseCases/RequestScreenTimeAuthUseCase.swift`
       - `rmdir DeluluDetox/Sources/Domain/Repositories` (jeśli pusty)
       - `rmdir DeluluDetox/Sources/Domain/UseCases` (jeśli pusty)
       - `rmdir DeluluDetox/Sources/Domain` (jeśli pusty)
       - `rmdir DeluluDetox/Sources/Data/Repositories` (jeśli pusty)
       - `rmdir DeluluDetox/Sources/Data` (jeśli pusty)
       Uwaga: pliki są tym samym Swift module (DeluluDetox target) — identyfikatory `ScreenTimeAuthRepository`, `ScreenTimeAuthRepositoryImpl`, `RequestScreenTimeAuthUseCase`, `RequestScreenTimeAuthUseCaseImpl` NIE są w module'owanym namespace (no `import` statements needed w konsumentach).

    7. **W9 — Zaktualizuj `DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift`** w TYM SAMYM commicie, żeby mock conformował do rozszerzonego protokołu. Dodaj `statusPublisher: AnyPublisher<AuthorizationStatus, Never>` oparte na `CurrentValueSubject<AuthorizationStatus, Never>` zainicjowanym na `.notDetermined` (mutable przez test helper `set(status:)` albo stubbedStatus z didSet) oraz `refreshStatus()` incrementujące `refreshStatusCallCount` (dla późniejszych assercji). Docelowa treść:

    ```swift
    import Combine
    import FamilyControls
    @testable import DeluluDetox

    final class MockScreenTimeAuthRepository: ScreenTimeAuthRepository, @unchecked Sendable {
        var stubbedStatus: AuthorizationStatus = .notDetermined {
            didSet { statusSubject.send(stubbedStatus) }
        }
        var requestAuthorizationError: Error?
        var requestAuthorizationCallCount = 0
        var refreshStatusCallCount = 0

        private let statusSubject: CurrentValueSubject<AuthorizationStatus, Never>

        var statusPublisher: AnyPublisher<AuthorizationStatus, Never> {
            statusSubject.eraseToAnyPublisher()
        }

        var authorizationStatus: AuthorizationStatus {
            stubbedStatus
        }

        init() {
            self.statusSubject = CurrentValueSubject(.notDetermined)
        }

        /// Test helper — explicit status mutation (alternative to setting `stubbedStatus`).
        func set(status: AuthorizationStatus) {
            stubbedStatus = status
        }

        func requestAuthorization() async throws {
            requestAuthorizationCallCount += 1
            if let error = requestAuthorizationError {
                throw error
            }
            stubbedStatus = .approved
        }

        func refreshStatus() {
            refreshStatusCallCount += 1
            statusSubject.send(stubbedStatus)
        }
    }
    ```

    8. `xcodegen generate` — regeneracja .xcodeproj pod nowy layout źródeł (wildcard katalogowy `- path: DeluluDetox/Sources` automatycznie łapie nowe pliki).

    9. Build przez XcodeBuildMCP `build_sim` scheme=DeluluDetox.
       - Oczekuj kompilacji zielonej. `DependencyContainer.swift` linia 6, 11: `init(screenTimeAuthRepository: ScreenTimeAuthRepository = ScreenTimeAuthRepositoryImpl())` — nadal kompiluje, bo typy żyją. `makeRequestScreenTimeAuthUseCase` wraca `RequestScreenTimeAuthUseCase` — nadal kompiluje.
       - `OnboardingView.swift:67-71` i `DenialView.swift:65-71` (`#Preview` tworzy `RequestScreenTimeAuthUseCaseImpl(repository: ScreenTimeAuthRepositoryImpl())`) — nadal powinien działać bo inicjalizator Impl jest bezargumentowy.
       - Jeśli są błędy, zatrzymaj się i zdiagnozuj zanim przejdziesz dalej. Task 2 tego planu załata `DependencyContainer.swift` jeśli cokolwiek się rozjedzie.

    10. **W9 — uruchom testy żeby potwierdzić że test target nadal zielony:**
        - XcodeBuildMCP `test_sim` scheme=DeluluDetox → wszystkie testy muszą przejść (OnboardingViewModelTests nadal używa init injection `OnboardingViewModel(requestAuth: useCase, ...)` — bo VM nie zmienia się w tym planie, dopiero Wave 3 — więc setup testów jest kompatybilny).
        - Jeśli testy są czerwone z powodu MockScreenTimeAuthRepository — krok 7 nie został wykonany poprawnie.
  </action>

  <verify>
    <automated>
      # Nowe pliki istnieją
      test -f DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift
      test -f DeluluDetox/Sources/Features/Onboarding/UseCase/RequestScreenTimeAuthUseCase.swift
      test -f DeluluDetox/Sources/Features/Onboarding/UseCase/ObserveScreenTimeAuthStatusUseCase.swift
      test -f DeluluDetox/Sources/Features/Onboarding/UseCase/RefreshScreenTimeAuthStatusUseCase.swift
      # Stare zniknęły
      test ! -f DeluluDetox/Sources/Domain/Repositories/ScreenTimeAuthRepository.swift
      test ! -f DeluluDetox/Sources/Data/Repositories/ScreenTimeAuthRepositoryImpl.swift
      test ! -f DeluluDetox/Sources/Domain/UseCases/RequestScreenTimeAuthUseCase.swift
      # Protocol + Impl w jednym pliku (feature-structure GUIDE wymóg)
      grep -q "protocol ScreenTimeAuthRepository" DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift
      grep -q "final class ScreenTimeAuthRepositoryImpl" DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift
      # CurrentValueSubject + AnyPublisher (D-07, D-08)
      grep -q "CurrentValueSubject<AuthorizationStatus, Never>" DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift
      grep -q "AnyPublisher<AuthorizationStatus, Never>" DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift
      # refreshStatus (D-13)
      grep -q "func refreshStatus" DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift
      # 3 UC istnieją jako protocol + Impl
      grep -q "protocol ObserveScreenTimeAuthStatusUseCase" DeluluDetox/Sources/Features/Onboarding/UseCase/ObserveScreenTimeAuthStatusUseCase.swift
      grep -q "final class ObserveScreenTimeAuthStatusUseCaseImpl" DeluluDetox/Sources/Features/Onboarding/UseCase/ObserveScreenTimeAuthStatusUseCase.swift
      grep -q "protocol RefreshScreenTimeAuthStatusUseCase" DeluluDetox/Sources/Features/Onboarding/UseCase/RefreshScreenTimeAuthStatusUseCase.swift
      grep -q "final class RefreshScreenTimeAuthStatusUseCaseImpl" DeluluDetox/Sources/Features/Onboarding/UseCase/RefreshScreenTimeAuthStatusUseCase.swift
      # W9: MockScreenTimeAuthRepository rozszerzony
      grep -q "statusPublisher: AnyPublisher<AuthorizationStatus, Never>" DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift
      grep -q "CurrentValueSubject" DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift
      grep -q "func refreshStatus" DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "CurrentValueSubject<AuthorizationStatus, Never>" DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift` → **≥ 1** (D-07)
    - `grep -c "var statusPublisher: AnyPublisher<AuthorizationStatus, Never>" DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift` → **≥ 1** (D-08)
    - `grep -c "func refreshStatus" DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift` → **≥ 1** (D-13; protocol + impl = 2 zazwyczaj)
    - `grep -c "statusSubject.send" DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift` → **≥ 2** (po requestAuthorization + w refreshStatus)
    - `grep -rn "GetScreenTimeAuthStatusUseCase" DeluluDetox/Sources/` → **0 hits** (D-12: one-shot UC wycofany)
    - `grep -c "protocol ScreenTimeAuthRepository" DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift` → **1** (tylko w tym pliku — feature-structure GUIDE)
    - `grep -c "final class ScreenTimeAuthRepositoryImpl" DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift` → **1** (tylko w tym pliku)
    - `find DeluluDetox/Sources/Domain` → **nic nie znaleziono** (stary katalog Domain/ usunięty)
    - `find DeluluDetox/Sources/Data` → **nic nie znaleziono** (stary katalog Data/ usunięty)
    - **D-17 dependency rule — no cross-repo import (Special Check A):**
      - `grep -E "^import .*Repository$" DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift` → **0 matches** (plik nie importuje innych repositories)
      - `grep -cE ": *[A-Z][A-Za-z]*Repository($|[, <])" DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift | grep -v "ScreenTimeAuthRepository"` → **0 matches** (no references to any OTHER repository type inside the repository file; trivially satisfied — tylko jedno repo w projekcie — ale acceptance zabezpiecza przy rozroście)
    - **W9: MockScreenTimeAuthRepository w tym samym commicie conforme z protokołem:**
      - `grep -c "statusPublisher: AnyPublisher<AuthorizationStatus, Never>" DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift` → **≥ 1**
      - `grep -c "CurrentValueSubject" DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift` → **≥ 1**
      - `grep -c "func refreshStatus" DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift` → **≥ 1**
      - `grep -c "refreshStatusCallCount" DeluluDetoxTests/Mocks/MockScreenTimeAuthRepository.swift` → **≥ 1**
    - XcodeBuildMCP `build_sim` scheme=DeluluDetox → **exit 0** (nadal kompiluje — DependencyContainer i VM kod używają `authorizationStatus` i `RequestScreenTimeAuthUseCase` — obie nazwy są nadal dostępne w module).
    - **W9: test target zielony na koniec tego planu:**
      - XcodeBuildMCP `test_sim -only-testing:DeluluDetoxTests/OnboardingViewModelTests` scheme=DeluluDetox → **exit 0** (OnboardingViewModelTests Phase 1 kontrakt: init injection z `OnboardingViewModel(requestAuth:, onAuthorized:)` — VM nie zmieniony w tym planie, więc testy pozostają zielone).
  </acceptance_criteria>

  <done>
    Pięć plików w `Features/Onboarding/` istnieje i kompiluje się. Trzy stare pliki z `Domain/` i `Data/` usunięte razem z pustymi katalogami. MockScreenTimeAuthRepository zaktualizowany w tym samym commicie — zero przerwy w zielonym test target (W9). App nadal buduje się zielony bo `DependencyContainer` i wszystkie VM nadal mogą rozwiązać typy przez `authorizationStatus` i `RequestScreenTimeAuthUseCase` — nic nie przerwaliśmy w tym kroku.
  </done>
</task>

<task type="auto">
  <name>Task 2: Stwórz OnboardingInjection.swift + zachowaj transitional DependencyContainer</name>

  <read_first>
    - `DeluluDetox/Sources/App/DependencyContainer.swift` (36 linii — pełen plik; będzie modyfikowany w następnych planach, tutaj musi tylko nadal kompilować)
    - `DeluluDetox/Sources/Core/DependencyInjection/DIContainer.swift` (z Wave 1; sygnatury `register`, `resolve`)
    - `.claude/guides/dependency-injection/GUIDE.md` sekcja `<Feature>Injection.swift` (szablon rejestracji)
    - `.planning/phases/01.1-architecture-foundation/01.1-PATTERNS.md` §2.3 (OnboardingInjection shape — gotowy snippet, 4 rejestracje)
    - `.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md` D-20, D-22 (scope `.application` dla Repo, `.unique` dla UC)
    - `DeluluDetoxTests/AppRootViewModelTests.swift` (używa `DependencyContainer(screenTimeAuthRepository:)` — sprawdź że signature nadal żyje po tym planie)
  </read_first>

  <files>
    DeluluDetox/Sources/Features/Onboarding/Injection/OnboardingInjection.swift (NEW)
    DeluluDetox/Sources/App/DependencyContainer.swift (MODIFIED — transitional: dodaje 2 factory dla nowych UC, żeby Wave 3+ mogły testować przez init injection albo przez DIContainer wymiennie)
  </files>

  <action>
    1. Utwórz katalog: `mkdir -p DeluluDetox/Sources/Features/Onboarding/Injection`.

    2. **Stwórz `DeluluDetox/Sources/Features/Onboarding/Injection/OnboardingInjection.swift`** — distributed registration wg PATTERNS §2.3 i D-22:

    ```swift
    // Features/Onboarding/Injection/OnboardingInjection.swift
    //
    // Distributed DI registration for the Onboarding feature (feature-owner of ScreenTimeAuth*).
    // Called from DeluluDetoxApp.init() in Wave 5 bootstrap. Not wired yet in this plan.

    enum OnboardingInjection {
        static func register(in container: DIContainer) {
            container.register(ScreenTimeAuthRepository.self, scope: .application) { _ in
                ScreenTimeAuthRepositoryImpl()
            }
            container.register(RequestScreenTimeAuthUseCase.self, scope: .unique) { c in
                RequestScreenTimeAuthUseCaseImpl(repository: c.resolve())
            }
            container.register(ObserveScreenTimeAuthStatusUseCase.self, scope: .unique) { c in
                ObserveScreenTimeAuthStatusUseCaseImpl(repository: c.resolve())
            }
            container.register(RefreshScreenTimeAuthStatusUseCase.self, scope: .unique) { c in
                RefreshScreenTimeAuthStatusUseCaseImpl(repository: c.resolve())
            }
        }
    }
    ```

    3. **Zmodyfikuj `DeluluDetox/Sources/App/DependencyContainer.swift`** — zachowaj istniejące API (żeby istniejące VMs w Wave 1 jeszcze kompilowały się), ale dodaj 2 nowe factory methods dla Observe i Refresh UC (będą potrzebne w Wave 3-5 przejściowo jako alternatywa do `@LazyInjected` dla testów). **Zachowaj** wszystkie istniejące metody bez zmian.

    Docelowa treść:

    ```swift
    import Observation

    // TRANSITIONAL — replaced by DIContainer + distributed <Feature>Injection in 01.1-04.
    // Keeps Phase 1 AppRootViewModel, OnboardingViewModel, DenialViewModel compilable
    // while Repository/UseCase files migrate to feature-first layout.
    //
    // DELETED in 01.1-04 after all VMs use @LazyInjected and DeluluDetoxApp.init() bootstraps DIContainer.shared.

    struct DependencyContainer {
        let screenTimeAuthRepository: ScreenTimeAuthRepository

        init(screenTimeAuthRepository: ScreenTimeAuthRepository = ScreenTimeAuthRepositoryImpl()) {
            self.screenTimeAuthRepository = screenTimeAuthRepository
        }

        func makeRequestScreenTimeAuthUseCase() -> RequestScreenTimeAuthUseCase {
            RequestScreenTimeAuthUseCaseImpl(repository: screenTimeAuthRepository)
        }

        func makeObserveScreenTimeAuthStatusUseCase() -> ObserveScreenTimeAuthStatusUseCase {
            ObserveScreenTimeAuthStatusUseCaseImpl(repository: screenTimeAuthRepository)
        }

        func makeRefreshScreenTimeAuthStatusUseCase() -> RefreshScreenTimeAuthStatusUseCase {
            RefreshScreenTimeAuthStatusUseCaseImpl(repository: screenTimeAuthRepository)
        }

        func makeAppRootViewModel() -> AppRootViewModel {
            AppRootViewModel(container: self)
        }

        func makeOnboardingViewModel(onAuthorized: (() -> Void)? = nil) -> OnboardingViewModel {
            OnboardingViewModel(
                requestAuth: makeRequestScreenTimeAuthUseCase(),
                onAuthorized: onAuthorized
            )
        }

        func makeDenialViewModel(onAuthorized: (() -> Void)? = nil) -> DenialViewModel {
            DenialViewModel(
                requestAuth: makeRequestScreenTimeAuthUseCase(),
                onAuthorized: onAuthorized
            )
        }

        func makeHomeViewModel() -> HomeViewModel {
            HomeViewModel()
        }
    }
    ```

    4. `xcodegen generate` (wildcard katalogowy łapie nowy plik).

    5. Build + test przez XcodeBuildMCP:
       - `build_sim` scheme=DeluluDetox → exit 0
       - `test_sim` scheme=DeluluDetox → wszystkie testy zielone (AppRootViewModelTests 4/4, OnboardingViewModelTests 3/3, DeluluDetoxTests 1/1 = 8/8)

    6. **NIE wywołuj** `OnboardingInjection.register(in: DIContainer.shared)` nigdzie — plik istnieje jako referencja i będzie wywołany z `DeluluDetoxApp.init()` w Wave 5 (`01.1-05`). Obecny app nadal używa `DependencyContainer` — `DIContainer.shared` jest pusty przez całą tę fazę.
  </action>

  <verify>
    <automated>
      # OnboardingInjection istnieje w docelowej lokalizacji
      test -f DeluluDetox/Sources/Features/Onboarding/Injection/OnboardingInjection.swift
      # Struktura wg guide
      grep -q "enum OnboardingInjection" DeluluDetox/Sources/Features/Onboarding/Injection/OnboardingInjection.swift
      grep -q "static func register(in container: DIContainer)" DeluluDetox/Sources/Features/Onboarding/Injection/OnboardingInjection.swift
      # 4 rejestracje
      grep -c "container.register" DeluluDetox/Sources/Features/Onboarding/Injection/OnboardingInjection.swift
      # Scope zgodne z D-22
      grep -q "ScreenTimeAuthRepository.self, scope: .application" DeluluDetox/Sources/Features/Onboarding/Injection/OnboardingInjection.swift
      grep -q "RequestScreenTimeAuthUseCase.self, scope: .unique" DeluluDetox/Sources/Features/Onboarding/Injection/OnboardingInjection.swift
      grep -q "ObserveScreenTimeAuthStatusUseCase.self, scope: .unique" DeluluDetox/Sources/Features/Onboarding/Injection/OnboardingInjection.swift
      grep -q "RefreshScreenTimeAuthStatusUseCase.self, scope: .unique" DeluluDetox/Sources/Features/Onboarding/Injection/OnboardingInjection.swift
      # DependencyContainer nadal żyje (do Wave 5)
      test -f DeluluDetox/Sources/App/DependencyContainer.swift
      grep -q "makeObserveScreenTimeAuthStatusUseCase" DeluluDetox/Sources/App/DependencyContainer.swift
      grep -q "makeRefreshScreenTimeAuthStatusUseCase" DeluluDetox/Sources/App/DependencyContainer.swift
      # Żaden wywoływacz OnboardingInjection.register nie istnieje jeszcze
      ! grep -rq "OnboardingInjection.register" DeluluDetox/Sources/App/DeluluDetoxApp.swift
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "container.register" DeluluDetox/Sources/Features/Onboarding/Injection/OnboardingInjection.swift` → **== 4** (D-20: Repo + 3 UC)
    - `grep -c "scope: .application" DeluluDetox/Sources/Features/Onboarding/Injection/OnboardingInjection.swift` → **== 1** (tylko Repo, D-22)
    - `grep -c "scope: .unique" DeluluDetox/Sources/Features/Onboarding/Injection/OnboardingInjection.swift` → **== 3** (3 UC, D-22)
    - `grep -c "static func register(in container: DIContainer)" DeluluDetox/Sources/Features/Onboarding/Injection/OnboardingInjection.swift` → **== 1** (konwencja guide)
    - `grep -c "makeObserveScreenTimeAuthStatusUseCase" DeluluDetox/Sources/App/DependencyContainer.swift` → **≥ 1** (transitional factory dla Wave 3-4)
    - `grep -c "makeRefreshScreenTimeAuthStatusUseCase" DeluluDetox/Sources/App/DependencyContainer.swift` → **≥ 1**
    - `grep -rq "OnboardingInjection.register" DeluluDetox/Sources/App/` → **0 hits** (bootstrap dopiero w Wave 5)
    - `grep -rq "OnboardingInjection.register" DeluluDetox/Sources/Features/` → **0 hits** (distributed registration caller żyje w App/, nie w Features/)
    - XcodeBuildMCP `build_sim` scheme=DeluluDetox → **exit 0**
    - XcodeBuildMCP `test_sim` scheme=DeluluDetox → **8/8 testów zielonych** (4 AppRootViewModelTests + 3 OnboardingViewModelTests + 1 DeluluDetoxTests placeholder; zero regresji vs Wave 1)
  </acceptance_criteria>

  <done>
    `OnboardingInjection.swift` istnieje w `Features/Onboarding/Injection/` z 4 rejestracjami w poprawnych scope'ach zgodnie z D-22. `DependencyContainer.swift` ma dodatkowe factory dla Observe+Refresh UC (transitional). App buduje się i testy Phase 1 są zielone. `DIContainer.shared` pozostaje pusty — bootstrap dopiero w Wave 5.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Granica auth iOS (ScreenTime authorization prompt) — jedyna istniejąca z Phase 1. **Niezmieniona** przez ten plan: repo nadal wywołuje `AuthorizationCenter.shared.requestAuthorization(for: .individual)` przez ten sam kod Swift (tylko przeniesiony do feature-first lokalizacji).

CurrentValueSubject + AnyPublisher są emitowane wyłącznie wewnątrz modułu DeluluDetox — nie cross-process, nie przez App Group, nie przez network. Żadnej nowej trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-04 | Tampering | ScreenTimeAuthRepositoryImpl.requestAuthorization | accept | Ten sam system call co Phase 1 (`AuthorizationCenter.shared.requestAuthorization(for: .individual)`) — boundary iOS niezmieniony. Test `OnboardingViewModelTests.testGrantAccessSuccess` weryfikuje happy path. |
| T-01.1-05 | Spoofing | CurrentValueSubject stan  | mitigate | Subject jest `private let`, dostęp tylko przez `statusPublisher: AnyPublisher<…>` (read-only) i `send(...)` wewnątrz repo. Nie eksponujemy `CurrentValueSubject` jako public — caller nie może podstawić fake statusu. |
| T-01.1-06 | Information Disclosure | statusPublisher emituje AuthorizationStatus | accept | `AuthorizationStatus` to enum z FamilyControls (notDetermined/denied/approved). Wartość publiczna, nie PII. Publisher żyje w-module. |
| T-01.1-07 | DoS | Swift 6.2 strict concurrency w Combine + Sendable | mitigate | `@unchecked Sendable` na Impl + `CurrentValueSubject<AuthorizationStatus, Never>` — `AuthorizationStatus` jest implicitly Sendable (FamilyControls). Build pod Swift 6.2 weryfikowany w Task 1 krok 9 i Task 2 krok 5. |

Phase 1 TCC prompt invariant: niezmieniony (ten sam `AuthorizationCenter.shared.requestAuthorization(for: .individual)` call). Brak nowych extension targets w tym planie → D-30 regression check obowiązuje w Wave 6.
</threat_model>

<verification>
1. Layout feature-first: `Features/Onboarding/{Repository,UseCase,Injection}/` istnieje; `Domain/` i `Data/` usunięte.
2. Protokół Repo zawiera: `statusPublisher: AnyPublisher<AuthorizationStatus, Never>`, `authorizationStatus` (transitional), `requestAuthorization() async throws`, `refreshStatus()`.
3. Impl zawiera: `CurrentValueSubject<AuthorizationStatus, Never>` jako `private let`, `statusPublisher` computed zwracający `statusSubject.eraseToAnyPublisher()`, `refreshStatus()` wołające `statusSubject.send(AuthorizationCenter.shared.authorizationStatus)`, `requestAuthorization()` wołające `statusSubject.send(...)` po udanym prompcie.
4. 3 UC (Request, Observe, Refresh) — protocol + Impl w jednym pliku każdy.
5. OnboardingInjection: enum z `static func register(in container: DIContainer)` i 4 rejestracjami (1×`.application`, 3×`.unique`).
6. `DependencyContainer` przejściowo zachowany z 2 dodatkowymi factory methods.
7. `DIContainer.shared` pozostaje pusty (OnboardingInjection.register nie wywołane).
8. MockScreenTimeAuthRepository zaktualizowany w tym samym commicie (W9) — test target zielony bez przerwy.
9. Build zielony. Wszystkie 8 testów Phase 1 zielonych.
10. D-17 dependency rule potwierdzona: brak importów cross-repo (Special Check A) — grep po `^import .*Repository$` w pliku repo → 0.
</verification>

<success_criteria>
- 5 nowych plików w `Features/Onboarding/{Repository,UseCase,Injection}/`.
- 3 stare pliki z `Domain/` i `Data/` usunięte; katalogi `Domain/`, `Data/` usunięte.
- `DependencyContainer.swift` zaktualizowany o 2 transitional factory; istniejące API zachowane.
- `OnboardingInjection.register(in:)` istnieje ale **nie jest wywoływane** — bootstrap w Wave 5.
- `MockScreenTimeAuthRepository` rozszerzony w tym samym commicie (W9) — test target zielony.
- Build + testy Phase 1 zielone (zero regresji funkcjonalnej).
- D-07, D-08, D-09, D-11, D-12, D-13, D-17 (ownership + Special Check A), D-20, D-22 zrealizowane.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-architecture-foundation/01.1-02-SUMMARY.md` zawierający:
- Listę utworzonych/zmodyfikowanych/usuniętych plików.
- Fragment zawartości `Features/Onboarding/Repository/ScreenTimeAuthRepository.swift` (protokół + impl, first 30 lines) — żeby verifier i Wave 3-5 executorzy mieli szybką referencję.
- Wynik XcodeBuildMCP build + test (8/8 passed).
- Oznaczenie: "DIContainer.shared is not bootstrapped yet — next bootstrap step is Wave 5 (`01.1-05`)." — actually bootstrap transitional happens Wave 3 (`01.1-03`), full bootstrap Wave 4 (`01.1-04`); patrz Wave 3 plan szczegóły.
- Potwierdzenie, że decyzje D-07, D-08, D-09, D-11, D-12, D-13, D-17 (+Special Check A), D-20, D-22 są spełnione (tabela decyzja → plik/linia).
- Notatkę D-17 reconciliation: Simple layout użyty per feature-structure GUIDE; ownership semantics (Onboarding feature-owner ScreenTimeAuth*) zachowana niezależnie od physical layoutu.
- Notatkę W9: MockScreenTimeAuthRepository zaktualizowany w tym samym commicie — zero przerwy w zielonym test target.
</output>
</content>
