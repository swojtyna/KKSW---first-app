---
phase: 01.1-architecture-foundation
plan: 06
type: execute
wave: 5
depends_on: [04]
files_modified:
  - .claude/guides/navigation/GUIDE.md
  - .claude/guides/dependency-injection/GUIDE.md
autonomous: true
requirements: []  # Covers D-24, D-25 — docs updates; plus B8 (stale DependencyContainer line rewrite)
user_setup: []

must_haves:
  truths:
    - "`.claude/guides/navigation/GUIDE.md` ma sekcję 'Root-level state switching (Wzorzec B)' z kryterium decyzji A vs B (D-24)."
    - "Navigation guide explicitly dokumentuje różnicę: Wzorzec A (modal — VM w enum case) vs Wzorzec B (root-level switch — enum bez payloadu VM, child view owns VM)."
    - "`.claude/guides/dependency-injection/GUIDE.md` ma przykład z realiami 01.1: CurrentValueSubject w Repository + RefreshUC (D-25)."
    - "Stała referencja do `DependencyContainer` (na linii ~197 navigation/GUIDE.md) jest przepisana na `@LazyInjected` + DIContainer.shared (B8 — po Phase 01.1 DependencyContainer już nie istnieje)."
    - "Guide'y są wewnętrznie spójne — żadnych zdań typu 'no container framework', 'no service locator', ani referencji do nieistniejącego `DependencyContainer` po fazie 01.1."
    - "Guide'y wzajemnie spójne — feature-structure, architecture, dependency-injection, navigation nie sprzeczają się."
  artifacts:
    - path: ".claude/guides/navigation/GUIDE.md"
      provides: "Dopisek 'Root-level state switching' z Wzorcem B; decision tree A vs B; stara linia 197 przepisana (B8)"
      contains: "Root-level state switching"
      also_contains: "Wzorzec B"
      also_contains: "enum bez payloadu"
    - path: ".claude/guides/dependency-injection/GUIDE.md"
      provides: "Uzupełnienie o CurrentValueSubject + RefreshUC pattern na przykładzie ScreenTimeAuth (D-25)"
      contains: "CurrentValueSubject"
      also_contains: "RefreshScreenTimeAuthStatusUseCase"
  key_links:
    - from: "navigation/GUIDE.md (Wzorzec B)"
      to: "PATTERNS.md §2.6 (AppRootViewModel)"
      via: "wspólny opis enum bez VM payload"
      pattern: "Destination.*case onboarding"
    - from: "dependency-injection/GUIDE.md (Combine example)"
      to: "Features/Onboarding/Repository/ScreenTimeAuthRepository.swift"
      via: "pokazuje jak projekt używa CurrentValueSubject"
      pattern: "statusSubject\\.send"
---

<objective>
Domknąć dokumentację: dopisać do `navigation/GUIDE.md` sekcję **Wzorzec B — Root-level state switching** (D-24), żeby reguła wyboru między Wzorcem A (modal, VM w enum payload) a Wzorcem B (root-level switch, enum bez payloadu VM, child view owns VM) była explicitna. Uzupełnić `dependency-injection/GUIDE.md` o praktyczny przykład z realiami 01.1 — CurrentValueSubject w Repository + RefreshUC pattern (D-25). Dodatkowo **przepisać stałą referencję do `DependencyContainer` w navigation/GUIDE.md linii ~197** (B8) — po Phase 01.1 `DependencyContainer` już nie istnieje, ta linia wprowadzałaby w błąd. To jest dokumentacja **deklaratywna** odzwierciedlająca już zastosowane w kodzie wzorce z Wave 1-4 — nie wprowadzamy nowych konwencji.

Purpose: Zamknąć lukę między kodem a dokumentacją. Po tym planie: Phase 2 executor czytający navigation/GUIDE.md dowie się że dla root-level flow (np. "active blocking session vs nothing active" w Phase 3) używa Wzorca B; Phase 3 executor czytający dependency-injection/GUIDE.md zobaczy jak projekt używa Combine dla globalnych stanów; żaden executor nie będzie widział martwego `DependencyContainer` jako "container do child VMs". Zero dualności reguł.

Output: 2 pliki MODIFIED w `.claude/guides/`.
</objective>

<execution_context>
@/Users/swojtyna/Documents/code/swojtyna/KKSW---first-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/swojtyna/Documents/code/swojtyna/KKSW---first-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md
@.planning/phases/01.1-architecture-foundation/01.1-PATTERNS.md
@.claude/guides/architecture/GUIDE.md
@.claude/guides/feature-structure/GUIDE.md
@.claude/guides/dependency-injection/GUIDE.md
@.claude/guides/navigation/GUIDE.md
@CLAUDE.md

<interfaces>
<!-- navigation/GUIDE.md obecny stan (po Phase 1): -->
<!-- Sekcje: Why state-driven, Setup, Core concepts, Pattern: Destination enum on VM (Wzorzec A — VM w case), Sheet/fullScreenCover, Alert, NavigationStack, Decision tree, Common pitfalls, Integration with architecture, Related -->
<!-- Line ~197: "- Child ViewModels are constructed by the parent ViewModel or by `DependencyContainer`." — STALE po 01.1 (B8). Do przepisania. -->
<!-- MISSING: sekcja o root-level switchu (case bez VM payloadu, child-owned VM). -->

<!-- dependency-injection/GUIDE.md obecny stan (po commit 4645f70): -->
<!-- Pokrywa: DIContainer API, Init Injection, @LazyInjected, Distributed registration, Testing. -->
<!-- MISSING: Combine + CurrentValueSubject przykład (dziś użyty w ScreenTimeAuthRepository — żywy wzorzec, ale brak w guide). -->

<!-- CLAUDE.md Project Rules Architecture sekcja: referuje navigation guide + state-driven via swift-navigation. Nie wymaga zmian. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dopisz sekcję "Root-level state switching (Wzorzec B)" do navigation/GUIDE.md (D-24) + przepisanie stale linii DependencyContainer (B8)</name>

  <read_first>
    - `.claude/guides/navigation/GUIDE.md` (cały — 210 linii; zlokalizuj: (a) koniec sekcji "Alert / confirmation dialog" (linie ~113), (b) linia ~197 zawierająca "by `DependencyContainer`" — B8 rewrite target)
    - `.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md` D-24 (pełny opis Wzorca A/B + kryterium)
    - `.planning/phases/01.1-architecture-foundation/01.1-PATTERNS.md` §2.6 (AppRootViewModel target — używany jako przykład Wzorca B)
    - `DeluluDetox/Sources/Features/Root/ViewModel/AppRootViewModel.swift` (po Wave 4 — live example)
    - `DeluluDetox/Sources/Features/Root/View/AppRootView.swift` (po Wave 4 — live example)
  </read_first>

  <files>
    .claude/guides/navigation/GUIDE.md (MODIFIED — nowa sekcja + aktualizacja Decision tree + B8 rewrite linii ~197)
  </files>

  <action>
    1. Otwórz `.claude/guides/navigation/GUIDE.md`.

    2. **Zlokalizuj** sekcję "Pattern: `Destination` enum on the ViewModel" (linia 44). W obecnej wersji pokazuje enum Z `(AddItemViewModel)` payloadu → **to jest Wzorzec A**. Nie usuwaj tej sekcji. Pod jej koniec (przed sekcją "Sheet / fullScreenCover / popover" na linii ~74) wstaw nową sekcję.

    3. **Przepisz nagłówek** istniejącej sekcji z "Pattern: `Destination` enum on the ViewModel" na "Wzorzec A — Modal presentation (VM w enum payload)" i dopisz zdanie wstępne tłumaczące że to jeden z dwóch wzorców:

    Obecna sekcja (linie 44-65):
    ```markdown
    ## Pattern: `Destination` enum on the ViewModel

    ```swift
    import SwiftUINavigation

    @Observable
    final class HomeViewModel {
        @CasePathable
        enum Destination {
            case addItem(AddItemViewModel)
            ...
    ```

    Zamień nagłówek (linia 44) na:
    ```markdown
    ## Wzorzec A — Modal presentation (VM w enum payload)

    Dla prezentacji modalnych (sheet, alert, confirmation dialog, fullScreenCover dla side-task) — child VM żyje **w** enum case. Parent VM tworzy child VM, enum trzyma referencję, system resetuje enum do nil po zamknięciu modala → child VM umiera.
    ```

    (Zachowaj resztę kodu i komentarzy wewnątrz Wzorca A bez zmian.)

    4. **Wstaw** nową sekcję **bezpośrednio po sekcji "Alert / confirmation dialog"** (I20 line-anchored: Wykryj punkt wstawienia przez `grep -n '^## Alert / confirmation dialog$' .claude/guides/navigation/GUIDE.md` → nowy block zaczyna się na linii po pustej linii kończącej tę sekcję, PRZED nagłówkiem "NavigationStack with a path"). Treść:

    ```markdown
    ---

    ## Wzorzec B — Root-level state switching (VM w child view, nie w enum)

    Dla app-level flow gdzie child ZASTĘPUJE ekran (onboarding vs main, paywall vs app, logged-out vs logged-in) — enum **bez payloadu VM**. Parent trzyma tylko `Destination?`, child view tworzy własny VM przez `@State private var model` lub rodzic trzyma każdego child VM jako `@State`.

    ### Decyzja: A czy B?

    | Child ekran | Wzorzec |
    |-------------|---------|
    | Otwiera się **nad** aktualnym ekranem (sheet, alert, popover) | A |
    | **Zastępuje** aktualny ekran (root switch: onboarding ↔ main; paywall przekrywa app) | B |

    ### Przykład (DeluluDetox AppRoot):

    ```swift
    import Observation
    import Combine
    import FamilyControls
    import SwiftUINavigation

    @MainActor
    @Observable
    final class AppRootViewModel: @unchecked Sendable {

        @CasePathable
        enum Destination: Equatable {
            case onboarding
            case denial
            case home
        }

        var destination: Destination?  // NIE private(set) — case-path bindings potrzebują setteru

        @ObservationIgnored
        @LazyInjected private var observeStatus: ObserveScreenTimeAuthStatusUseCase

        @ObservationIgnored
        private var cancellables: Set<AnyCancellable> = []

        init() {
            observeStatus()
                .sink { [weak self] status in
                    self?.destination = Self.map(status)
                }
                .store(in: &cancellables)
        }

        private static func map(_ status: AuthorizationStatus) -> Destination { ... }
    }
    ```

    View-side (Wzorzec B):

    ```swift
    struct AppRootView: View {
        @Bindable var model: AppRootViewModel

        @State private var onboardingModel = OnboardingViewModel()
        @State private var denialModel = DenialViewModel()
        @State private var homeModel = HomeViewModel()

        var body: some View {
            Group {
                switch model.destination {
                case .onboarding: OnboardingView(model: onboardingModel)
                case .denial:     DenialView(model: denialModel)
                case .home:       NavigationStack { HomeView(model: homeModel) }
                case .none:       Color(Theme.background)
                }
            }
            .animation(.easeInOut(duration: 0.35), value: model.destination)
        }
    }
    ```

    ### Reguły Wzorca B

    - Enum case'y **nie trzymają** VM: `case home` a NIE `case home(HomeViewModel)`.
    - Parent posiada tylko `Destination?` — decyduje **którą** gałąź pokazać.
    - Child VM żyje po stronie View (lub jako `@State` u rodzica jeśli lifetime VM ma przekraczać pojedynczy cykl body — jak w AppRootView powyżej).
    - Child VM rezolwuje zależności przez `@LazyInjected` (nie przyjmuje ich w init) — dzięki temu `@State private var model = ChildViewModel()` działa bez fabryki w rodzicu.
    - Event flow child → parent idzie przez **dzielony publisher** (Combine `CurrentValueSubject` w Repository), NIE przez callbacki `onX` na VM. Rodzic subskrybuje publisher i reaguje na zmiany. Child tylko wywołuje UC (Repository wysyła do subject).

    ### Antywzorzec — Wzorzec B z payloadem VM

    ```swift
    // ❌ ŹLE
    enum Destination {
        case home(HomeViewModel)
    }
    ```

    To łamie regułę "parent nie trzyma referencji do child VM w root-level switchu". Konsekwencja: przy zmianie destination stary VM nie jest zwalniany momentalnie (trzymany w Combine pipeline referencji). Sheety radzą sobie z tym bo system sam resetuje; root switch wymaga jawnego lifecycle.

    ### Kiedy Wzorzec B?

    - Onboarding vs main app flow (DeluluDetox AppRoot).
    - Paywall blokujący całe API (przed subskrypcją).
    - Logged-out vs logged-in root.
    - Kill switch / server-forced update screen.

    ### Kiedy NIE Wzorzec B?

    - Nawigacja drill-down → **NavigationStack + path** (nie root switch).
    - Otwarcie edycji elementu listy → **Wzorzec A, sheet/push**.
    - Confirmation modal → **Wzorzec A, alert/confirmationDialog**.
    ```

    **I20 line-anchored insertion protocol:**
    - Wykryj: `grep -n '^## Alert / confirmation dialog$' .claude/guides/navigation/GUIDE.md` → zwróci linię (np. 97).
    - Znajdź następny nagłówek tego samego poziomu po niej (np. `^## NavigationStack`) → np. linia 117.
    - Wstaw nową sekcję (z leading `---` separatorem) **na linii `nav_stack_line - 1`** (czyli na pustej linii przed nagłówkiem "NavigationStack"). Nowa sekcja kończy się pustą linią + `---`.

    5. **Zaktualizuj Decision tree** (obecnie linie 162-179 w oryginale) — dodaj wiersz dla root-level switch. Nowa treść sekcji Decision tree:

    ```markdown
    ## Decision tree: which presentation?

    ```
    Child ekran **ZASTĘPUJE** parent (onboarding vs main, paywall, logged-in/out)?
        └─ Wzorzec B — root-level state switch (enum bez VM payload)

    Short side-task with a "done" moment (add, edit, confirm)?
        └─ Wzorzec A — .sheet(item:)

    Drill-down within the same flow?
        └─ NavigationStack + path

    Blocking, must-complete flow (onboarding, paywall)?
        └─ Jeśli zastępuje ekran: Wzorzec B. Jeśli nad ekranem: .fullScreenCover(item:) (Wzorzec A)

    Tiny contextual UI tied to a specific control?
        └─ .popover(item:) (Wzorzec A)

    Y/N decision (destructive option)?
        └─ .confirmationDialog(item:) / .alert(item:) (Wzorzec A)
    ```
    ```

    6. **B8 — przepisanie linii ~197 "by `DependencyContainer`"**:

    **Zlokalizuj linię:**
    ```bash
    grep -n "DependencyContainer" .claude/guides/navigation/GUIDE.md
    ```
    Spodziewany output: **exactly 1 match** (linia ~197 w sekcji "Integration with architecture"). Aktualna treść:
    ```
    - Child ViewModels are constructed by the parent ViewModel or by `DependencyContainer`. See `.claude/guides/architecture/GUIDE.md`.
    ```

    **Przepisz na nowy wariant** (łączący B8 fix + Wzorzec A vs B clarification, zgodnie z oryginalnym W krokiem 6 tego tasku):

    Jeżeli linia odnosi się do resolwowania zależności dla child VM → **zmień na:**
    ```
    - **Wzorzec A:** parent ViewModel constructs child ViewModels and places them in enum cases.
    - **Wzorzec B:** child View konstruuje child ViewModel przez `@State private var model = ChildViewModel()`; child VM rezolwuje dependencies przez `@LazyInjected` against `DIContainer.shared` (patrz `.claude/guides/dependency-injection/GUIDE.md`).
    - Event flow in Wzorzec B: child writes → Repository (via UseCase) → Repository's CurrentValueSubject emits → parent's Combine `.sink` updates destination.
    ```

    **Acceptance:** `grep -c "DependencyContainer" .claude/guides/navigation/GUIDE.md` → **== 0** (po B8 rewrite zero referencji).

    Jeżeli w innym miejscu guide'a zobaczysz jakąkolwiek inną wystąpienie `DependencyContainer` (przy przeglądzie) — przepisz analogicznie: referencja do `DIContainer.shared + @LazyInjected` dla resolwowania, albo usuń zdanie jeśli dotyczyło nieistniejącego typu.

    7. **Zaktualizuj sekcję "Integration with architecture"** kompleksowo (obejmuje krok 6 i dodaje B-pattern context). Końcowa treść sekcji "Integration with architecture":

    ```markdown
    ## Integration with architecture

    - `Destination` lives on the Presentation layer (ViewModel). Domain and Data know nothing about it.
    - **Wzorzec A:** parent ViewModel constructs child ViewModels and places them in enum cases.
    - **Wzorzec B:** child View konstruuje child ViewModel przez `@State private var model = ChildViewModel()`; child VM rezolwuje dependencies przez `@LazyInjected` against `DIContainer.shared` (patrz `.claude/guides/dependency-injection/GUIDE.md`).
    - Event flow in Wzorzec B: child writes → Repository (via UseCase) → Repository's CurrentValueSubject emits → parent's Combine `.sink` updates destination.
    ```

    8. **Zaktualizuj Last Updated** (ostatnia linia): `**Last Updated**: 2026-04-18` pozostaje jeśli edytujesz w tym samym dniu, albo zaktualizuj do bieżącej daty.

    9. Zweryfikuj że plik się parsuje jako markdown (kontrola wizualna — nagłówki zgodne z hierarchią, brak zerwanych fences ```).
  </action>

  <verify>
    <automated>
      # Nowa sekcja obecna
      grep -q "Wzorzec B" .claude/guides/navigation/GUIDE.md
      grep -q "Root-level state switching" .claude/guides/navigation/GUIDE.md
      # Stara sekcja przemianowana na Wzorzec A
      grep -q "Wzorzec A" .claude/guides/navigation/GUIDE.md
      # Kryterium decyzji
      grep -q "ZASTĘPUJE" .claude/guides/navigation/GUIDE.md
      # Przykład AppRoot obecny
      grep -q "AppRootViewModel" .claude/guides/navigation/GUIDE.md
      grep -q "case onboarding" .claude/guides/navigation/GUIDE.md
      # Decision tree zawiera Wzorzec B
      grep -c "Wzorzec B" .claude/guides/navigation/GUIDE.md
      # B8: zero stałych referencji do DependencyContainer
      ! grep -q "DependencyContainer" .claude/guides/navigation/GUIDE.md
      # @LazyInjected w sekcji Integration
      grep -q "@LazyInjected" .claude/guides/navigation/GUIDE.md
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Wzorzec A" .claude/guides/navigation/GUIDE.md` → **≥ 2** (nagłówek sekcji + decision tree)
    - `grep -c "Wzorzec B" .claude/guides/navigation/GUIDE.md` → **≥ 3** (nagłówek sekcji + decision tree + integration)
    - `grep -c "Root-level state switching" .claude/guides/navigation/GUIDE.md` → **≥ 1**
    - `grep -c "AppRootViewModel" .claude/guides/navigation/GUIDE.md` → **≥ 1** (live example)
    - `grep -c "enum Destination: Equatable" .claude/guides/navigation/GUIDE.md` → **≥ 1** (real snippet z project)
    - `grep -c "case onboarding\s*$\|case onboarding$" .claude/guides/navigation/GUIDE.md` — **≥ 1** (enum BEZ VM payloadu w przykładzie Wzorca B)
    - `grep -c "case .*(.*ViewModel)" .claude/guides/navigation/GUIDE.md` — **≥ 1** (Wzorzec A przykład NADAL obecny, np. `case addItem(AddItemViewModel)`)
    - `grep -c "@LazyInjected" .claude/guides/navigation/GUIDE.md` → **≥ 1** (Wzorzec B link do DI guide + B8 rewrite Integration)
    - `grep -c "CurrentValueSubject" .claude/guides/navigation/GUIDE.md` → **≥ 1** (Event flow explanation)
    - **B8 cleanup:**
      - `grep -c "DependencyContainer" .claude/guides/navigation/GUIDE.md` → **== 0** (zero referencji do nieistniejącego typu po 01.1)
      - `grep -c "DIContainer.shared\|DIContainer\b" .claude/guides/navigation/GUIDE.md` → **≥ 1** (replacement — wskazanie właściwego container)
    - **B8 + consistency across ALL guides (grep = 0 na sprzeczności):**
      - `grep -c "no container framework\|service locator" .claude/guides/navigation/GUIDE.md .claude/guides/dependency-injection/GUIDE.md .claude/guides/architecture/GUIDE.md .claude/guides/feature-structure/GUIDE.md` → **0** (aggregate across all 4 guides)
    - Markdown parsing OK: wywołaj `node --eval "const fs = require('fs'); const content = fs.readFileSync('.claude/guides/navigation/GUIDE.md', 'utf8'); const fences = (content.match(/\`\`\`/g) || []).length; if (fences % 2 !== 0) { process.exit(1); }"` → **exit 0** (równa liczba ``` open/close)
  </acceptance_criteria>

  <done>
    `navigation/GUIDE.md` ma kompletny Wzorzec A (modal) i Wzorzec B (root switch) z przykładami, Decision tree pokazuje oba wzorce, przykład Wzorca B używa live kodu z AppRootViewModel. D-24 spełnione. B8 zrealizowany: stała linia ~197 "by `DependencyContainer`" przepisana na `@LazyInjected + DIContainer.shared`; zero referencji do nieistniejącego typu `DependencyContainer` w całym guide. Insercja Wzorca B wykonana line-anchored (I20): po sekcji "Alert / confirmation dialog", przed "NavigationStack with a path".
  </done>
</task>

<task type="auto">
  <name>Task 2: Dopisz CurrentValueSubject + RefreshUC przykład do dependency-injection/GUIDE.md (D-25)</name>

  <read_first>
    - `.claude/guides/dependency-injection/GUIDE.md` (cały — zaplanuj wstawkę w logicznym miejscu: po sekcji "Init Injection Pattern" (linia ~34+) lub po sekcji Testing — wybierz miejsce które najbardziej ciąży kontekstowo. Sugestia: po Init Injection Pattern, przed @LazyInjected — bo Combine publisher żyje w Repository (Init Injection layer), potem UC go konsumują.)
    - `.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md` D-25
    - `DeluluDetox/Sources/Features/Onboarding/Repository/ScreenTimeAuthRepository.swift` (po Wave 4 — live example do wstawienia jako snippet)
    - `DeluluDetox/Sources/Features/Onboarding/UseCase/ObserveScreenTimeAuthStatusUseCase.swift` (live example)
    - `DeluluDetox/Sources/Features/Onboarding/UseCase/RefreshScreenTimeAuthStatusUseCase.swift` (live example)
  </read_first>

  <files>
    .claude/guides/dependency-injection/GUIDE.md (MODIFIED — nowa sekcja "Reactive state: Combine publisher w Repository")
  </files>

  <action>
    1. Otwórz `.claude/guides/dependency-injection/GUIDE.md`.

    2. **I20 line-anchored insertion:**
       - Wykryj: `grep -n '^## Init Injection Pattern$' .claude/guides/dependency-injection/GUIDE.md` → zwróci np. linię 33.
       - Znajdź następny `^## ` po tej linii — to jest punkt przed którym wstawiamy (np. linia 62 "## @LazyInjected" lub podobny nagłówek).
       - Wstaw nową sekcję dokładnie na tej linii (separator `---` + nagłówek + content + separator `---` zakończający).

    3. **Wstaw** nową sekcję. Treść:

    ```markdown
    ---

    ## Reactive state: Combine publisher w Repository

    Dla globalnych stanów aplikacji (status autoryzacji, active sessions, user preferences) Repository wewnętrznie trzyma `CurrentValueSubject` i eksponuje `AnyPublisher` read-only. Klienci subskrybują przez dedykowany UseCase.

    ### Kiedy to stosować

    - Stan który ma **wielu subskrybentów** (Onboarding VM, Denial VM, AppRoot VM wszystkie obserwują ten sam ScreenTime status).
    - Stan który powinien być dostępny **natychmiast przy subskrypcji** (ostatnia znana wartość, nie trzeba osobnego "get current").
    - Źródło prawdy żyje w Repository — UC są cienkimi przejścówkami (pass-through + enkapsulacja protokołu).

    `CurrentValueSubject` > `AsyncStream.makeStream()` — ten drugi jest single-subscriber, wymagałby broadcastu.

    ### Wzorzec: Repository + 2 UC (Observe + Refresh)

    ```swift
    // Features/Onboarding/Repository/ScreenTimeAuthRepository.swift
    import Combine
    import FamilyControls

    protocol ScreenTimeAuthRepository: Sendable {
        var statusPublisher: AnyPublisher<AuthorizationStatus, Never> { get }
        func requestAuthorization() async throws
        func refreshStatus()
    }

    final class ScreenTimeAuthRepositoryImpl: ScreenTimeAuthRepository, @unchecked Sendable {
        private let statusSubject: CurrentValueSubject<AuthorizationStatus, Never>

        var statusPublisher: AnyPublisher<AuthorizationStatus, Never> {
            statusSubject.eraseToAnyPublisher()
        }

        init() {
            self.statusSubject = CurrentValueSubject(AuthorizationCenter.shared.authorizationStatus)
        }

        func requestAuthorization() async throws {
            try await AuthorizationCenter.shared.requestAuthorization(for: .individual)
            statusSubject.send(AuthorizationCenter.shared.authorizationStatus)
        }

        func refreshStatus() {
            statusSubject.send(AuthorizationCenter.shared.authorizationStatus)
        }
    }
    ```

    ```swift
    // Features/Onboarding/UseCase/ObserveScreenTimeAuthStatusUseCase.swift
    protocol ObserveScreenTimeAuthStatusUseCase: Sendable {
        func callAsFunction() -> AnyPublisher<AuthorizationStatus, Never>
    }

    final class ObserveScreenTimeAuthStatusUseCaseImpl: ObserveScreenTimeAuthStatusUseCase {
        private let repository: ScreenTimeAuthRepository

        init(repository: ScreenTimeAuthRepository) {
            self.repository = repository
        }

        func callAsFunction() -> AnyPublisher<AuthorizationStatus, Never> {
            repository.statusPublisher
        }
    }
    ```

    ```swift
    // Features/Onboarding/UseCase/RefreshScreenTimeAuthStatusUseCase.swift
    protocol RefreshScreenTimeAuthStatusUseCase: Sendable {
        func callAsFunction()
    }

    final class RefreshScreenTimeAuthStatusUseCaseImpl: RefreshScreenTimeAuthStatusUseCase {
        private let repository: ScreenTimeAuthRepository

        init(repository: ScreenTimeAuthRepository) {
            self.repository = repository
        }

        func callAsFunction() {
            repository.refreshStatus()
        }
    }
    ```

    ### Rejestracja w DIContainer

    ```swift
    // Features/Onboarding/Injection/OnboardingInjection.swift
    enum OnboardingInjection {
        static func register(in container: DIContainer) {
            container.register(ScreenTimeAuthRepository.self, scope: .application) { _ in
                ScreenTimeAuthRepositoryImpl()
            }
            container.register(ObserveScreenTimeAuthStatusUseCase.self, scope: .unique) { c in
                ObserveScreenTimeAuthStatusUseCaseImpl(repository: c.resolve())
            }
            container.register(RefreshScreenTimeAuthStatusUseCase.self, scope: .unique) { c in
                RefreshScreenTimeAuthStatusUseCaseImpl(repository: c.resolve())
            }
        }
    }
    ```

    Repository musi mieć scope `.application` — to **singleton trzymający stan subjectu**. Gdyby scope był `.unique`, każdy resolve tworzyłby nowy repo → nowy subject → subskrybenci pracujący na różnych streamach. UC mogą być `.unique` (bezstanowe przejścówki).

    ### Konsumpcja w ViewModelu

    ```swift
    @MainActor
    @Observable
    final class AppRootViewModel: @unchecked Sendable {
        @ObservationIgnored @LazyInjected private var observeStatus: ObserveScreenTimeAuthStatusUseCase
        @ObservationIgnored @LazyInjected private var refreshStatus: RefreshScreenTimeAuthStatusUseCase
        @ObservationIgnored private var cancellables: Set<AnyCancellable> = []

        var destination: SomeEnum?

        init() {
            observeStatus()
                .sink { [weak self] status in
                    self?.destination = Self.map(status)
                }
                .store(in: &cancellables)
        }

        /// Wywoływane np. z `.onChange(of: scenePhase)` w View — user cofa permission w Settings → repo emituje → destination się aktualizuje.
        func refresh() {
            refreshStatus()
        }
    }
    ```

    **Uwaga na `@ObservationIgnored`** przed `@LazyInjected` i przed `cancellables` — inaczej każdy dostęp invaliduje observation. Swift Observation wymóg.

    ### Refresh vs observe — dlaczego OBA

    - **Observe** — stream przyszłych zmian (prompt grant → repo emituje nowy status → subskrybenci reagują).
    - **Refresh** — wymuszenie odczytu **teraz** z systemowego źródła (user cofa Screen Time w Settings iOS → poza app → brak powiadomienia → trzeba "zapytać ponownie" przy `scenePhase == .active`).

    Bez Refresh dziura w logice: user może cofnąć zgodę poza app, AppRoot myśli że nadal .approved → pokaże Home choć blokada nie działa. To **core value violation** w apce blokującej. Refresh jest obroną.

    ### Antywzorzec — sync `var status: AuthorizationStatus { get }`

    ```swift
    // ❌ ŹLE — synchroniczny getter bez notyfikacji zmian
    protocol BadRepository {
        var status: SomeStatus { get }
        func request() async throws
    }
    ```

    Subskrybenci musieliby pollować (timer co 1s?) lub ręcznie dostawać powiadomienia (NotificationCenter?). Zamiast — CurrentValueSubject daje *both* "last known value on subscribe" i "emit future changes" w jednym API.

    ---
    ```

    4. **Sprawdź markdown fence balance** — liczba ``` open musi być równa close'om. Użyj node eval.

    5. **Zaktualizuj Last Updated** na dole pliku do bieżącej daty, jeśli w guide jest taka stopka. W obecnej wersji dependency-injection/GUIDE.md linia 1-7 ma frontmatter z `last_updated: 2026-04-18` — zostaw jeśli 2026-04-18, zaktualizuj jeśli inna data.
  </action>

  <verify>
    <automated>
      # Nowa sekcja obecna
      grep -q "Reactive state" .claude/guides/dependency-injection/GUIDE.md
      grep -q "Combine publisher w Repository" .claude/guides/dependency-injection/GUIDE.md
      # Kluczowe koncepty
      grep -q "CurrentValueSubject" .claude/guides/dependency-injection/GUIDE.md
      grep -q "statusPublisher" .claude/guides/dependency-injection/GUIDE.md
      grep -q "RefreshScreenTimeAuthStatusUseCase" .claude/guides/dependency-injection/GUIDE.md
      grep -q "ObserveScreenTimeAuthStatusUseCase" .claude/guides/dependency-injection/GUIDE.md
      # Uzasadnienie refresh
      grep -q "core value" .claude/guides/dependency-injection/GUIDE.md
      # Antywzorzec
      grep -q "Antywzorzec\|❌" .claude/guides/dependency-injection/GUIDE.md
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "CurrentValueSubject" .claude/guides/dependency-injection/GUIDE.md` → **≥ 2** (w snippetach Repository i konsumpcji)
    - `grep -c "RefreshScreenTimeAuthStatusUseCase" .claude/guides/dependency-injection/GUIDE.md` → **≥ 2** (protokół + rejestracja w przykładzie)
    - `grep -c "ObserveScreenTimeAuthStatusUseCase" .claude/guides/dependency-injection/GUIDE.md` → **≥ 2** (protokół + rejestracja)
    - `grep -c "statusPublisher\|AnyPublisher<AuthorizationStatus" .claude/guides/dependency-injection/GUIDE.md` → **≥ 2**
    - `grep -c "@ObservationIgnored" .claude/guides/dependency-injection/GUIDE.md` → **≥ 1** (uwaga na precedencję przed @LazyInjected)
    - `grep -c "scope: .application" .claude/guides/dependency-injection/GUIDE.md` → **≥ 1** (uzasadnienie scope'u)
    - `grep -c "Reactive state" .claude/guides/dependency-injection/GUIDE.md` → **≥ 1** (nagłówek sekcji)
    - `grep -c "core value\|core-value" .claude/guides/dependency-injection/GUIDE.md` → **≥ 1** (uzasadnienie Refresh)
    - Markdown fences zbalansowane: wywołanie node eval zwraca exit 0.
  </acceptance_criteria>

  <done>
    `dependency-injection/GUIDE.md` ma sekcję "Reactive state: Combine publisher w Repository" z live przykładem ScreenTimeAuth (D-25). Wzorzec (Repo + ObserveUC + RefreshUC + VM `.sink`) udokumentowany. Pokazuje scope `.application` dla repo, `.unique` dla UC. Wspomina o `@ObservationIgnored` wymaganiu. Antywzorzec (sync getter) obecny. Insercja line-anchored (I20) po Init Injection Pattern.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Brak zmian w trust boundaries. Plan modyfikuje wyłącznie pliki markdown w `.claude/guides/` — nie wpływa na żaden code path w aplikacji. Pliki guide są czytane przez Claude Code jako kontekst — nie są kompilowane ani nie mają odpowiednika w bundle.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-20 | Information Disclosure | Guide snippety zawierają API keys/secrets? | accept | Wszystkie snippety to publicznie dostępny kod z repo (Features/Onboarding/Repository/ScreenTimeAuthRepository.swift jest w git). Brak sekretów, brak URL endpointów, brak credentials. |
| T-01.1-21 | Tampering | Niespójność guide'ów po edycji (np. stary wątek "DependencyContainer" wciąż straszy) | mitigate | Acceptance criteria wymaga: `grep -c "DependencyContainer" .claude/guides/navigation/GUIDE.md` → 0 (B8); agregowany grep po wszystkich 4 guide'ach na "no container framework / service locator" → 0; markdown fences zbalansowane; snippety zgodne z live kodem (diff mental po Wave 4). |

Zero threatów kodowych. To jest plan dokumentacyjny.
</threat_model>

<verification>
1. navigation/GUIDE.md:
   - Wzorzec A nagłówek + Wzorzec B nagłówek (≥ 2 wystąpień "Wzorzec A", ≥ 3 wystąpień "Wzorzec B")
   - Decision tree aktualizowany
   - Przykład Wzorca B używa AppRootViewModel, enum BEZ VM payloadu
   - Integration with architecture odnosi się do DI guide
   - **B8: zero wystąpień "DependencyContainer"; ≥ 1 wystąpienie "DIContainer" jako replacement**
2. dependency-injection/GUIDE.md:
   - Sekcja "Reactive state: Combine publisher w Repository"
   - Live snippety Repository/Observe/Refresh
   - Rejestracja w DIContainer z scope `.application`/`.unique`
   - Konsumpcja w VM z @ObservationIgnored + @LazyInjected
   - Uzasadnienie Refresh (core value)
   - Antywzorzec sync getter
3. Spójność guide'ów:
   - Zero "no container framework" / "no service locator" referencji w ANY guide (agregowany grep)
   - Zero "DependencyContainer" w navigation/GUIDE.md (B8)
   - feature-structure + architecture + DI + navigation nie sprzeczają się (ręczna inspekcja krzyżowa — snippety z Features/Onboarding/* wszędzie zgodne)
4. Markdown poprawny: fences zbalansowane, hierarchia nagłówków ma sens.
5. Insercje wykonane line-anchored (I20) — po znanych nagłówkach, nie "znajdź sekcję X".
</verification>

<success_criteria>
- `navigation/GUIDE.md` ma Wzorzec A (modal) + Wzorzec B (root switch) jako rówieśnicze sekcje.
- Decision tree odsyła do Wzorca B dla root-level zastąpień ekranu.
- **B8:** zero stałych referencji do `DependencyContainer` w navigation/GUIDE.md; zamienione na `@LazyInjected + DIContainer.shared`.
- `dependency-injection/GUIDE.md` ma sekcję "Reactive state" z Combine + CurrentValueSubject + RefreshUC.
- Oba guide'y są spójne z kodem po Wave 4 (snippety odzwierciedlają AppRootVM + Onboarding Repository).
- Zero sprzeczności typu "no container framework" w żadnym guide'ie (agregowany grep po 4 guide'ach = 0).
- Insercje line-anchored (I20).
- D-24 i D-25 spełnione; B8 zamknięty.
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-architecture-foundation/01.1-06-SUMMARY.md` zawierający:
- Listę zmodyfikowanych sekcji w obu plikach (diff-friendly).
- Pełny tekst nowych sekcji (Wzorzec B + Reactive state) — inline.
- B8 status: diff linii ~197 navigation/GUIDE.md (before → after); grep confirming zero DependencyContainer references.
- I20 status: które linie-anchor użyto do insercji w każdym pliku.
- Wynik grep acceptance kryteriów.
- Potwierdzenie że D-24 i D-25 zrealizowane, B8 zamknięty.
- Notatkę: "Dokumenty fazy 01.1 zamknięte. Jedyne pozostałe zadanie: Wave 6 cleanup (01.1-07)."
</output>
</content>
