import SwiftUI

extension Theme {
    /// Background color for unselected preset chips on the Session Start screen.
    static let chipBackground = Color(.systemGray6)
    /// Track ring background for the CountdownView progress ring.
    static var ringTrack: Color { Color(.systemGray5) }
    /// Destructive / early-end button tint.
    static var destructive: Color { Color(.systemRed) }
}
