---
phase: 01.1-architecture-foundation
plan: 07
type: execute
wave: 6
depends_on: [05, 06]
files_modified:
  - to_integrate/  # DELETED (cały katalog, D-23)
  - .planning/phases/01.1-architecture-foundation/EXTENSION_REGRESSION_DIFF.txt  # NEW (evidence)
autonomous: false  # zawiera checkpoint:human-verify
requirements: []  # Covers D-23 (to_integrate cleanup), D-26 (CLAUDE.md no-op verify, W11), D-30 (extension regression part b), final Phase 01.1 verification
user_setup: []

must_haves:
  truths:
    - "`to_integrate/` katalog usunięty z repo (D-23)."
    - "Extension regression: evidence block z `BASELINE_EXTENSION_IMPORTS.txt` (Wave 1) i evidence block captured NOW (w tym samym canonical format) są **identyczne**; diff evidence-to-evidence pusty (B2 symmetric format zapewnia że diff nie jest zakłócony różnicą header/empty-comment)."
    - "3 extension targety (DeviceActivityMonitor, ShieldAction, ShieldConfiguration) budują się zielone przez XcodeBuildMCP po całym refaktorze."
    - "Full test run scheme=DeluluDetox: wszystkie testy zielone (min 14)."
    - "`xcodegen generate` nie generuje zmian — `project.yml` stabilny po całej fazie 01.1."
    - "`CLAUDE.md` nietknięty przez fazę 01.1 (D-26 / W11): `git diff 4645f70 -- CLAUDE.md` pusty."
    - "Human verify: app startuje na symulatorze, Phase 1 flow działa identycznie (cold-launch → Onboarding/Denial/Home zgodnie z aktualnym Screen Time status; tap Grant Access / Let's Try Again → prompt → poprawny routing po akcji)."
    - "10 user must-haves (patrz planning context) spełnione."
  artifacts:
    - path: ".planning/phases/01.1-architecture-foundation/EXTENSION_REGRESSION_DIFF.txt"
      provides: "Evidence że extension imports nie zmieniły się vs BASELINE (D-30 part b)"
    - path: "to_integrate/"
      provides: "USUNIĘTY — źródłowy katalog DI zmigrowany w Wave 1, więc already-integrated, more useful not"
      contains: "— usunięty —"
  key_links:
    - from: "BASELINE_EXTENSION_IMPORTS.txt (Wave 1) evidence block"
      to: "current grep result captured w tym samym canonical format"
      via: "evidence-to-evidence diff (awk-extracted blocks)"
      pattern: "identical\\|empty diff"
---

<objective>
Zamknąć fazę 01.1. Cztery kroki:
1. Usunięcie `to_integrate/` katalogu (D-23).
2. Regression check na extensions (D-30 part b) — evidence-block capture w tym samym canonical format co Wave 1 baseline, diff evidence-to-evidence musi być pusty.
3. CLAUDE.md no-op check (D-26 / W11) — `git diff` vs commit pre-01.1 ma być pusty dla CLAUDE.md.
4. Human verify checkpoint — potwierdzenie że Phase 1 flow działa identycznie na simulatorze po całym refaktorze.

Purpose: Ostatnia linia obrony. Do tej pory każda Wave miała swoje acceptance criteria, ale dopiero ten plan sprawdza **końcowy stan całej fazy** — czy core value (app blokujący) nie uległ regresji przez pośrednie stany migracji, czy extensions nie wyłapały nieprzewidzianych zależności, czy CLAUDE.md nie został przypadkowo zmieniony (D-26), czy przepływ UX jest identyczny.

D-30 regression check jest szczególnie ważny: Phase 01.1 ma **zero planowanych zmian w extensions**, więc baseline i po-refaktorze muszą być identyczne. **B2 fix:** baseline z Wave 1 używa canonical format z markerami `---EVIDENCE---` / `---END-EVIDENCE---`; capture w tym planie używa **identycznego** formatu → diff między evidence blockami jest prawdziwym signałem zmiany, nie artefakt różnicy "comment vs empty". D-31 gate z Wave 1 (jeśli extension coś importuje z main) musi być tutaj zamknięty — export żyje i działa.

D-17 ownership note: CONTEXT D-17 używa słownictwa "Common/" dla opisu ownership semantics, ale Phase 01.1 physically wybrało Simple layout (`Features/Onboarding/{Repository,UseCase}/` bez `Common/`) per feature-structure GUIDE. Ownership invariant niezmieniony: Onboarding jest feature-ownerem ScreenTimeAuth*, inne feature'y konsumują tylko przez UC. Ta notka będzie zapisana w SUMMARY dla czytelności post-phase.

Output: `to_integrate/` usunięty, evidence file w planning/, CLAUDE.md potwierdzone nietknięte, human verify approval.
</objective>

<execution_context>
@/Users/swojtyna/Documents/code/swojtyna/KKSW---first-app/.claude/get-shit-done/workflows/execute-plan.md
@/Users/swojtyna/Documents/code/swojtyna/KKSW---first-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md
@.planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt
@.planning/phases/01.1-architecture-foundation/01.1-01-SUMMARY.md
@.planning/phases/01.1-architecture-foundation/01.1-02-SUMMARY.md
@.planning/phases/01.1-architecture-foundation/01.1-03-SUMMARY.md
@.planning/phases/01.1-architecture-foundation/01.1-04-SUMMARY.md
@.planning/phases/01.1-architecture-foundation/01.1-05-SUMMARY.md
@.planning/phases/01.1-architecture-foundation/01.1-06-SUMMARY.md
@.claude/guides/xcodebuild-mcp/GUIDE.md

<interfaces>
<!-- Stan po Wave 5:
     - to_integrate/ nadal istnieje (zawiera DIContainer.swift + PropertyWrappers) — duplikat z Features/Core/DependencyInjection/
     - Phase 01.1 code refactor: DONE (Waves 1-4)
     - Tests mirror layout: DONE (Wave 5)
     - Docs updated: DONE (Wave 5 plan 06, including B8 rewrite)
     - BASELINE_EXTENSION_IMPORTS.txt istnieje w .planning/phases/01.1-architecture-foundation/ w canonical format z ---EVIDENCE--- / ---END-EVIDENCE--- markerami (B2 fix w Wave 1)
     - Extensions/ nietknięte
     - project.yml: extension targety mają `sources: - path: Extensions/<Ext>` — nadal wildcard katalogowy, nie zmienione
     - CLAUDE.md: nietknięte przez całą fazę 01.1 (D-26)

     Co zostało do zrobienia:
     1. Usunąć to_integrate/ (trzyma zduplikowany kod — 3 pliki Swift które żyją też w Features/Core/DependencyInjection/).
     2. Regression check: evidence-block diff vs BASELINE (identyczny canonical format); build extensions.
     3. D-26 / W11 CLAUDE.md no-op verify.
     4. Human verify: app UX. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Usunięcie `to_integrate/` + extension regression check (D-23, D-30 part b) + CLAUDE.md no-op verify (D-26 / W11)</name>

  <read_first>
    - `.planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt` (z Wave 1 — plik z canonical format baseline w `---EVIDENCE---` / `---END-EVIDENCE---` markerami, B2)
    - `.planning/phases/01.1-architecture-foundation/01.1-CONTEXT.md` D-23, D-26, D-30, D-31
    - `to_integrate/` listing katalogu — weryfikuj czego dokładnie pozbywasz się (`ls -R to_integrate/`)
    - `Extensions/DeviceActivityMonitorExtension/` — lista plików Swift
    - `Extensions/ShieldActionExtension/`
    - `Extensions/ShieldConfigurationExtension/`
    - `project.yml` linie 60-127 (3 extension targety — `sources:` mają zostać nietknięte)
    - `.claude/guides/xcodebuild-mcp/GUIDE.md` — jak budować per-target przez XcodeBuildMCP
    - `CLAUDE.md` (cały — dla porównania vs git 4645f70; D-26 / W11)
  </read_first>

  <files>
    to_integrate/ (DELETED — cały katalog, D-23)
    .planning/phases/01.1-architecture-foundation/EXTENSION_REGRESSION_DIFF.txt (NEW — evidence file)
  </files>

  <action>
    1. **Pre-delete safety check** — zweryfikuj, że nie ma aktywnych referencji do `to_integrate/`:
       ```bash
       grep -rn "to_integrate" .claude/ DeluluDetox/ DeluluDetoxTests/ .planning/ 2>/dev/null
       ```
       Oczekuj: referencje tylko w `.planning/` (CONTEXT.md, PATTERNS.md, plany 01-04 — OK, to historyczne referencje do tego co było). **Zero referencji w `.claude/`, `DeluluDetox/`, `DeluluDetoxTests/`**.
       - Jeśli są w `DeluluDetox/` lub `DeluluDetoxTests/` — STOP, zgłoś (oznacza że migracja z Wave 1 nie była kompletna).
       - Jeśli są w `.claude/` (np. guide'y wspominają `to_integrate`) — poprawnie zaktualizuj lub zgłoś.

    2. **Usuń `to_integrate/`** (D-23):
       ```bash
       rm -rf to_integrate/
       ```

    3. **Extension regression grep — canonical format capture** (D-30 part b, B2):

       Krok 3a: Wykonaj grep z ZAWĘŻONYM wzorcem (identyczny jak Wave 1 po B2 fix) i capture w canonical format:
       ```bash
       ISO_DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
       EVIDENCE=$(grep -rnE "(^|[[:space:]])@?testable? *import[[:space:]]+DeluluDetox([[:space:]]|$)" Extensions/ --include="*.swift" 2>/dev/null || true)
       CURRENT_CANONICAL=$(cat <<EOF
       # DeluluDetox extension → main-target import CURRENT capture
       # Captured: ${ISO_DATE}
       # Pattern: import DeluluDetox\b OR @testable import DeluluDetox\b in Extensions/**/*.swift
       # Empty evidence block means "no hits" (expected happy-path).
       ---EVIDENCE---
       ${EVIDENCE}
       ---END-EVIDENCE---
       EOF
       )
       echo "$CURRENT_CANONICAL" > /tmp/current-capture.txt
       ```

       Krok 3b: Sanity check że BASELINE ma canonical markery (B2):
       ```bash
       grep -n "^---EVIDENCE---$" .planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt
       grep -n "^---END-EVIDENCE---$" .planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt
       ```
       Oba muszą zwrócić po jednej linii. Jeśli nie → BLOCKER: baseline z Wave 1 nie był w canonical format (regression plan jest niemożliwy do wykonania poprawnie).

       Krok 3c: Ekstraktuj evidence block z obu plików i porównaj (B2 — diff evidence-to-evidence, NIE cały plik):
       ```bash
       awk '/^---EVIDENCE---$/{flag=1;next} /^---END-EVIDENCE---$/{flag=0} flag' .planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt > /tmp/baseline-evidence.txt
       awk '/^---EVIDENCE---$/{flag=1;next} /^---END-EVIDENCE---$/{flag=0} flag' /tmp/current-capture.txt > /tmp/current-evidence.txt

       diff /tmp/baseline-evidence.txt /tmp/current-evidence.txt > /tmp/evidence-diff.txt
       ```

       **Interpretacja:**
       - `/tmp/evidence-diff.txt` **pusty** = zero zmian w extension imports, PASS.
       - Niepusty = extension imports uległy zmianie przez refaktor → **BLOCKER**, zatrzymaj się i zgłoś do usera zawartość diff.
       - Jeśli diff niepusty:
         - Pokaż usera zawartość diff.
         - Przeanalizuj: czy zmiana jest akceptowalna (np. Extension importuje Repository który został przeniesiony — D-31 scenariusz) czy to bug?
         - W większości scenariuszy Wave 1-4 nie dotykały extensions więc diff MUSI być pusty. Jakikolwiek diff jest SURPRISE.

    4. **Build 3 extension targetów przez XcodeBuildMCP** (D-30 part b):
       - `build_sim` scheme=DeluluDetox → zbuduje main target + embedded extensions.
       - Alternatywnie jeśli XcodeBuildMCP wspiera per-target: `build_sim` per `DeviceActivityMonitorExtension`, `ShieldActionExtension`, `ShieldConfigurationExtension`.
       - **WYMAGANE:** wszystkie 3 extension targety budują się zielone. Jeśli jakakolwiek extension się nie kompiluje — zatrzymaj się.

    5. **Full test run** — XcodeBuildMCP `test_sim` scheme=DeluluDetox:
       - Wszystkie testy z Wave 5 (min 14) muszą przejść zielone.

    6. **xcodegen generate dry-run**:
       ```bash
       xcodegen generate --spec project.yml
       git diff project.yml DeluluDetox.xcodeproj/ 2>/dev/null | head -20
       ```
       Po generate: `DeluluDetox.xcodeproj/` może mieć zmiany (to OK — XcodeGen normalnie generuje). Ale `project.yml` **nie powinien** się zmienić od Wave 1 (W14 w Wave 5 mógł zrobić jednorazową korektę wildcard sources jeśli była potrzebna — w takim wypadku to zmiana jednorazowa, nie regresja; weryfikuj zgodność z Wave 5 SUMMARY). Jeśli `project.yml` diff pokazuje UNEXPECTED zmiany → zatrzymaj się.

    7. **D-26 / W11 — CLAUDE.md no-op verify:**
       ```bash
       git diff 4645f70 -- CLAUDE.md > /tmp/claudemd-diff.txt
       wc -l /tmp/claudemd-diff.txt
       ```
       Oczekuj: **0 linii** (diff pusty — CLAUDE.md nietknięty w fazie 01.1, zgodnie z D-26).
       - Jeśli diff pokazuje zmiany → zgłoś jako BLOCKER dla tej fazy (któryś Wave cicho dotknął CLAUDE.md, łamiąc D-26). Przeanalizuj co się zmieniło, zatrzymaj się.
       - Jeśli pusty → zapisz w SUMMARY: "D-26 CLAUDE.md no-op: PASS".
       - Commit hash `4645f70` to ostatni commit przed 01.1 fazą (docs feature-structure+DI guide, architecture rewrite) — używamy go jako cutoff.

    8. **Uaktualnij `EXTENSION_REGRESSION_DIFF.txt`** z evidence:
       Jeśli evidence diff był pusty, zamień zawartość pliku na:
       ```
       # Extension Regression Check — Phase 01.1 (D-30 part b)
       # Baseline: .planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt (captured Wave 1, canonical format with ---EVIDENCE--- markers)
       # Current: identical canonical format, captured Wave 6
       # Method: awk-extracted evidence blocks (between ---EVIDENCE--- and ---END-EVIDENCE---), diff'd directly.
       # Result: IDENTICAL (zero changes in Extensions/ imports)
       #
       # Extension builds (XcodeBuildMCP, captured Wave 6):
       # - DeviceActivityMonitorExtension: PASS
       # - ShieldActionExtension: PASS
       # - ShieldConfigurationExtension: PASS
       #
       # D-26 / W11 CLAUDE.md no-op check:
       # - git diff 4645f70 -- CLAUDE.md: EMPTY (0 lines; PASS)
       #
       # Conclusion: D-23 + D-26 + D-30 + D-31 closure checks ALL PASSED. Phase 01.1 did not touch Extensions/ or CLAUDE.md.
       ```

    9. **Git state sanity check**:
       ```bash
       git status --short
       git diff --stat
       ```
       Oczekuj długiej listy zmian (cała faza 01.1 — docs, source, tests). Sprawdź że nie ma przypadkowego tracked file'a spoza scope (np. Info.plist w Extensions/ zmodyfikowany, albo `.xcodeproj` trafił do commitu).
  </action>

  <verify>
    <automated>
      # to_integrate/ usunięty (D-23)
      test ! -d to_integrate
      # BASELINE nadal istnieje (historyczna evidence)
      test -f .planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt
      # BASELINE ma canonical markery (B2 — sanity check)
      grep -q "^---EVIDENCE---$" .planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt
      grep -q "^---END-EVIDENCE---$" .planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt
      # EXTENSION_REGRESSION_DIFF.txt istnieje z wynikiem
      test -f .planning/phases/01.1-architecture-foundation/EXTENSION_REGRESSION_DIFF.txt
      # Zero referencji do to_integrate w active code
      ! grep -rq "to_integrate" .claude/ DeluluDetox/ DeluluDetoxTests/
      # Extensions nadal mają swoje sources w project.yml (nietknięte)
      grep -q "Extensions/DeviceActivityMonitorExtension" project.yml
      grep -q "Extensions/ShieldActionExtension" project.yml
      grep -q "Extensions/ShieldConfigurationExtension" project.yml
      # Features/Core/DependencyInjection/ (z Wave 1) nadal żyje
      test -f DeluluDetox/Sources/Core/DependencyInjection/DIContainer.swift
      test -f DeluluDetox/Sources/Core/DependencyInjection/PropertyWrappers/Injected.swift
      test -f DeluluDetox/Sources/Core/DependencyInjection/PropertyWrappers/LazyInjected.swift
    </automated>
  </verify>

  <acceptance_criteria>
    - `test ! -d to_integrate` → **exit 0** (D-23)
    - `grep -rc "to_integrate" .claude/ DeluluDetox/ DeluluDetoxTests/` → **0 hits** (brak martwych referencji)
    - `wc -l .planning/phases/01.1-architecture-foundation/EXTENSION_REGRESSION_DIFF.txt` → **≥ 5** (plik zawiera evidence, nie jest pusty)
    - `grep -q "IDENTICAL\|PASS" .planning/phases/01.1-architecture-foundation/EXTENSION_REGRESSION_DIFF.txt` → **exit 0** (diff był pusty)
    - **B2 evidence-to-evidence diff (symmetric format zapewnia że diff jest prawdziwy):**
      - `grep -c "^---EVIDENCE---$" .planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt` → **== 1**
      - `grep -c "^---END-EVIDENCE---$" .planning/phases/01.1-architecture-foundation/BASELINE_EXTENSION_IMPORTS.txt` → **== 1**
      - `awk '/^---EVIDENCE---$/{flag=1;next} /^---END-EVIDENCE---$/{flag=0} flag' BASELINE > /tmp/baseline-evidence.txt; diff /tmp/baseline-evidence.txt /tmp/current-evidence.txt` → **empty** (diff command exits 0 with no stdout)
    - Git diff extensions: `git diff Extensions/` → **pusty** (zero zmian przez całą fazę 01.1)
    - XcodeBuildMCP `build_sim` scheme=DeluluDetox → **exit 0** (main + 3 extensions build green)
    - XcodeBuildMCP `test_sim` scheme=DeluluDetox → **≥ 14 testy passed**
    - `git diff project.yml` → **pusty lub tylko trywialne whitespace / Wave 5 W14 wildcard fix** (project.yml nietknięty od Wave 1; jeśli W14 wildcardized sources — zgodne z Wave 5 SUMMARY)
    - DIContainer files żyją w Core/ (nie zostały przypadkiem usunięte razem z to_integrate/).
    - **D-26 / W11 CLAUDE.md no-op:**
      - `git diff 4645f70 -- CLAUDE.md` → **empty output** (diff pusty, PASS)
      - `wc -l <(git diff 4645f70 -- CLAUDE.md)` → **== 0**
      - EXTENSION_REGRESSION_DIFF.txt zawiera wiersz "D-26 / W11 CLAUDE.md no-op check: ... EMPTY (0 lines; PASS)"
  </acceptance_criteria>

  <done>
    `to_integrate/` usunięty z repo. Extension regression check: evidence-block diff vs baseline pusty (B2 canonical format zapewnia prawdziwy signal); 3 extension targety zielone. Full test run: wszystkie testy zielone. `project.yml` stabilny (lub jednorazowy W14 wildcard fix zgodny z Wave 5 SUMMARY). `CLAUDE.md` niezmieniony vs 4645f70 (D-26 / W11). Evidence plik w `.planning/` dla audytu (z sekcją CLAUDE.md no-op check).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — Phase 1 flow działa identycznie po refaktorze</name>

  <what-built>
    Cała faza 01.1 zakończona na poziomie kodu i testów:
    - Feature-first layout w `DeluluDetox/Sources/Features/{Onboarding,Denial,Home,Root}/{ViewModel,View,Injection,...}/`
    - DIContainer.shared bootstrapped z 4 Injection plików w `DeluluDetoxApp.init()`
    - AppRootViewModel: Destination enum (Equatable) + Combine subscription + refreshStatus() wołane przez scenePhase hook
    - OnboardingVM/DenialVM używają @LazyInjected; callback `onAuthorized` usunięty
    - ScreenTimeAuthRepository: CurrentValueSubject + statusPublisher + refreshStatus()
    - `DependencyContainer.swift` usunięty, `to_integrate/` usunięty
    - 14+ testów jednostkowych zielonych w mirror layout
    - Dokumentacja navigation/GUIDE.md (z B8 rewrite) + dependency-injection/GUIDE.md zaktualizowana
    - CLAUDE.md nietknięty (D-26 / W11 potwierdzone)

    **Expectation:** User experience w Phase 1 flow MUSI być **identyczna** z stanem przed 01.1. Refaktor nie dotyka designu ani copy — tylko architektury pod spodem.
  </what-built>

  <how-to-verify>
    **Wymagany setup:**
    - Simulator iPhone z iOS 26.0+
    - Scheme DeluluDetox, `build_run_sim` przez XcodeBuildMCP (automated step przed tym checkpointem)
    - Do manual verify: stopper w ręce (10 min max)

    **Krok 1: Cold-launch happy path (na czystym simulatorze / po Erase All Content and Settings albo po deletion app)**

    1. Delete app z simulatora jeśli instalowana.
    2. `build_run_sim` scheme=DeluluDetox via XcodeBuildMCP — app startuje.
    3. **Oczekuj:** ekran Onboarding — "Your Phone Is Winning" + lock.iphone icon w electric violet (#7C3AED) + przycisk "Grant Access".
       - Tło: Theme.background (jasny).
       - Accent: #7C3AED (electric violet).
       - Zero glitch / placeholder / loading screen na początek (może być pojedynczy frame `.none` destination, ale nie więcej).
    4. Tap "Grant Access".
    5. **Oczekuj:** system Screen Time prompt "DeluluDetox Would Like to Access Screen Time".
    6. Tap "Continue" (albo "Allow", zależnie od iOS label).
    7. **Oczekuj:** animowane przejście do ekranu Home — "No Apps Blocked Yet" + apps.iphone icon + przycisk "Choose Apps to Block".
       - Animacja .easeInOut(duration: 0.35) na swap (subtelny fade/scale).

    **Krok 2: Denial path**

    8. Tap "Decline" na system prompt zamiast Continue (jeśli Krok 1 skończyliśmy z .approved i chcesz zresetować stan — idź do Settings → DeluluDetox → cofnij Screen Time, potem re-run app). LUB: jeśli system prompt już dał approved i nie możemy cofnąć, **ten krok można pominąć w symulatorze** (denial path jest trudno trigger'ować bez Reset All Content) i zweryfikować go na realnym device przed Phase 2.
    9. **Oczekuj (jeśli denial przetestowany):** ekran Denial — "Nice Try" + hand.raised.fill icon + przycisk "Let's Try Again".
    10. Tap "Let's Try Again".
    11. **Oczekuj:** system prompt ponownie pojawia się, tap Continue.
    12. **Oczekuj:** Home screen (approved).

    **Krok 3: scenePhase refresh (D-14 core-value test)** — **WAŻNE**

    13. Background app (swipe up / Cmd+Shift+H na simulatorze).
    14. *(Simulator limitation: cofnięcie Screen Time w Settings iOS może nie działać w simulatorze bo Screen Time jest restricted. Jeśli tak — pomiń ten krok lub zmień na realne device test.)* W Settings iOS → Screen Time → wyłącz Screen Time for This iPhone → Turn Off Screen Time.
    15. Foreground app.
    16. **Oczekuj:** po chwili (max 1s) app przeszło z Home → Onboarding (lub Denial, zależnie od final status). **To jest D-13/D-14 — core value protection.**
        - Jeśli app został na Home mimo że Screen Time cofnięty w Settings, **core value zagrożona** → BLOCKER dla fazy.

    **Krok 4: Testy jednostkowe przechodzą na świeżym schemacie**
    17. Uruchom `test_sim` scheme=DeluluDetox via XcodeBuildMCP — wszystkie testy zielone.

    **Krok 5: Performance sanity — brak regresji**
    18. Cold-launch → pierwsze pixele na ekranie. Subiektywnie powinno być **nie wolniej** niż przed 01.1. DIContainer bootstrap w `init()` + 4 `register` calls + `@LazyInjected` resolve dodają mikrosekundy, nieodczuwalne dla usera. Jeśli odczuwasz laga > 1s — STOP, zgłoś.

    **Krok 6: Visual regression — pixel-level porównanie (opcjonalne)**
    19. Jeśli masz zapasowe screenshoty z Phase 1 (Plan 01-03 human-verify), porównaj visualnie z Phase 01.1 po-refaktorze. Powinny być piksel-w-piksel identyczne (ten sam design system, zero zmian w View'sach poza #Preview blokami).
  </how-to-verify>

  <resume-signal>
    Type "approved" jeśli:
    1. Cold-launch → Onboarding z prawidłowym designem
    2. Grant Access → system prompt → Home animowane przejście
    3. (Jeśli testowalne) Denial path działa identycznie Phase 1
    4. (Jeśli testowalne) scenePhase .active → refresh działa: cofnięcie Screen Time w Settings → app wraca do Onboarding/Denial po foregroundzie
    5. Wszystkie unit testy zielone
    6. Subiektywnie zero visual/performance regresji

    Type konkretny problem jeśli coś nie gra. Format: "BLOCKER: [opis]" lub "NIT: [opis]".
    Typowe BLOCKERS:
    - App crashes on cold-launch (fatalError z DIContainer bo któryś register się nie wykonał)
    - Grant Access → Home nie pokazuje się (Combine flow nie emituje — problem w repo send lub sink subscription)
    - Home screen ciągle widoczny po cofnięciu Screen Time w Settings (D-13 fail)
    - Unit testy flaky (async race w AppRootViewModelTests — patrz W12 `Task.yield()` mitigation)

    Typowe NITs (nie-blocker):
    - Animacja wolniejsza / szybsza niż Phase 1 — zanotuj ale nie blocker
    - Drobny accessibility regression (hitbox, contrast) — zanotuj, załat w Phase 2 jeśli minor
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Ten plan nie wprowadza nowych trust boundaries. Usuwanie `to_integrate/` jest bezpieczne — kod już żyje w `Features/Core/DependencyInjection/` (Wave 1). Extension regression check jest read-only grep + build.

Human verification NIE modyfikuje repo — tylko obserwuje zachowanie na simulatorze/device.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-22 | Tampering | `rm -rf to_integrate/` — literówka w ścieżce mogłaby usunąć pół repo | mitigate | Krok 1 akcji: `grep -rn "to_integrate"` pokazuje że ścieżka istnieje (nie literówka). Acceptance criteria: `test ! -d to_integrate` potwierdza usunięcie konkretnej ścieżki. Git history — jakby coś poszło nie tak, przywrócenie przez `git checkout HEAD -- to_integrate/`. |
| T-01.1-23 | DoS | Extension targety nieoczekiwanie uszkodzone przez migrację feature'ów | mitigate | D-30 part b evidence-to-evidence diff + build. Acceptance criteria: diff **empty** + 3 builds **PASS**. Canonical format (B2) zapewnia że diff jest prawdziwy, nie artefakt różnicy formatu. Jakakolwiek zmiana = BLOCKER do debug. |
| T-01.1-24 | **Core Value Violation** | User cofa Screen Time w Settings → app nie reaguje | mitigate | Krok 3 human-verify testuje dokładnie ten flow. Jeśli nie działa, implementacja D-13/D-14 jest buggy (repo.refreshStatus nie wysyła do subject, lub VM.refreshStatus nie jest wywoływane przez scenePhase). To jest najważniejszy test fazy. |
| T-01.1-25 | Spoofing | Fake approved status przez mock dla prod build | accept | Mock istnieje tylko w `@testable import DeluluDetox` — nie w app target. `DeluluDetoxTests` jest osobnym schematem. `@testable` wymaga debug build — release builds nie mają accessu. |
| T-01.1-26 | Tampering | CLAUDE.md ukradkiem zmodyfikowany przez Wave 1-5 (łamiąc D-26) | mitigate | W11: `git diff 4645f70 -- CLAUDE.md` musi być pusty. Acceptance criteria sprawdza 0 linii diff. Jeśli zmiana — BLOCKER (nie ufamy żadnemu kolejnemu ruchowi fazy). |

**TCC invariant:** niezmieniony. `AuthorizationCenter.shared.requestAuthorization(for: .individual)` w repo — ten sam kod co Phase 1.

**App Group sandbox:** niezmieniony. Żaden kod w 01.1 nie wchodzi do `group.com.kksw.DeluluDetox` shared container (to Phase 2+).

**Entitlement:** `com.apple.developer.family-controls` — nie dotykany (wymaga Apple approval, status Phase 1).
</threat_model>

<verification>
1. `to_integrate/` usunięty; zero referencji poza `.planning/phases/01.1-architecture-foundation/*.md` (historyczne).
2. `EXTENSION_REGRESSION_DIFF.txt` istnieje w `.planning/`, zawiera evidence że diff był pusty + że 3 extension targety zbudowały się + CLAUDE.md no-op verified.
3. Main target build zielony; 3 extension targets zielone; 14+ testów passed.
4. `project.yml` nietknięty przez całą fazę (pusty git diff na project.yml od commit przed Wave 1; jednorazowy W14 wildcard fix w Wave 5 dokumentowany tam).
5. **B2 evidence-to-evidence diff pusty** (canonical format po obu stronach) — baseline + current oba używają `---EVIDENCE---` / `---END-EVIDENCE---` markerów.
6. **D-26 / W11:** `git diff 4645f70 -- CLAUDE.md` pusty.
7. Human verify checkpoint: 6 kroków opisanych powyżej, user musi typować "approved" lub BLOCKER/NIT.
8. Wszystkie 10 user must-haves z planning context spełnione:
   1. ✓ Existing unit tests pass (AppRootVMTests, OnboardingVMTests mirror layout)
   2. ✓ XcodeBuildMCP build_run_sim succeeds
   3. ✓ Manual walkthrough Phase 1 flow passes (Krok 1-2)
   4. ✓ VM ↛ Repository grep = 0 (weryfikowane przez Wave 4 acceptance)
   5. ✓ Repo ↛ Repo grep = 0 (tylko 1 repo w projekcie)
   6. ✓ DeluluDetox/Sources/App/DependencyContainer.swift removed (Wave 4)
   7. ✓ to_integrate/ removed (ten plan Task 1)
   8. ✓ Guides mutual consistency (Wave 5 docs + B8 cleanup + manual inspection)
   9. ✓ CLAUDE.md no "service locator" anti-pattern (commit 4645f70); D-26 no-op verified (W11)
   10. ✓ Extension regression: identical evidence blocks pre vs post (B2 canonical format) + 3 builds PASS (ten plan Task 1, D-30 part b)
</verification>

<success_criteria>
- `to_integrate/` USUNIĘTY (D-23 zamknięty).
- Extension regression: evidence-to-evidence diff **empty** (B2 canonical format symmetric comparison, D-30 zamknięty).
- 3 extension targety budują się zielone.
- Full test run: min 14 testów zielonych.
- `project.yml` stabilny (tylko ewentualny W14 wildcard fix z Wave 5, dokumentowany tam).
- **D-26 / W11 CLAUDE.md no-op:** `git diff 4645f70 -- CLAUDE.md` pusty.
- Human verify checkpoint approved przez usera — Phase 1 flow działa identycznie (cold-launch + grant + home; denial path jeśli testowany; scenePhase refresh jeśli testowany).
- Evidence `EXTENSION_REGRESSION_DIFF.txt` zachowany w repo jako audit trail (z sekcją CLAUDE.md no-op).

**Phase 01.1 goal satisfied:** "Przestawić projekt na układ feature-first z DIContainer i jasnymi regułami zależności (Repo ↛ Repo, UC → UC/Repo, VM → tylko UC) zanim dołożymy kolejne feature'y."

**D-17 Common/ reconciliation (post-phase note):** CONTEXT D-17 używa słownictwa "Common/" dla opisu ownership semantics (shared → feature-owner). Phase 01.1 physically wybrało Simple layout per feature-structure GUIDE — `Features/Onboarding/{Repository,UseCase}/` bez `Common/`. Ownership invariant niezmieniony: Onboarding jest feature-ownerem ScreenTimeAuth*, Denial i Root konsumują tylko przez UC. Dokumentacja → architecture GUIDE + feature-structure GUIDE (Wave 5 + commit 4645f70).

Post-phase cleanup (nie w tym planie, ale do zrobienia przez usera po approve):
- Usunąć `.planning/phases/02-app-selection/02-01-PLAN.md` (stary draft — CONTEXT deferred #6)
- Wywołać `/gsd-plan-phase 02` na nowej architekturze
</success_criteria>

<output>
After completion (user typed "approved"), create `.planning/phases/01.1-architecture-foundation/01.1-07-SUMMARY.md` zawierający:
- Tabelę: każde z 10 user must-haves z planning context → PASS/FAIL/notes
- Tabelę: każda decyzja D-01..D-31 → DONE + plan-reference (który Wave ją zrealizował)
- Wynik final XcodeBuildMCP build + test
- Extension regression evidence: **evidence-to-evidence diff empty / non-empty** (B2 canonical format)
- D-26 / W11 CLAUDE.md no-op: `git diff 4645f70 -- CLAUDE.md` output (should be empty)
- D-17 Common/ reconciliation note: "Simple layout fizycznie, ownership semantics zachowane" — link do feature-structure GUIDE.
- Human verify result: "approved" / ograniczenia (np. "denial path not testable on simulator, skip to real device before Phase 2")
- Lista post-phase todos dla usera:
  - Delete `.planning/phases/02-app-selection/02-01-PLAN.md` (stary draft)
  - Run `/gsd-plan-phase 02` on new architecture
- Finalne zamknięcie: "Phase 01.1 complete. Architecture foundation ready for Phase 2+."
</output>
</content>
